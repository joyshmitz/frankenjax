# Rust build artifacts
/target
**/*.rs.bk

# Proptest regression files
**/proptest-regressions/

# Python virtual environment
.venv/
__pycache__/
*.pyc
*.pyo

# Beads local state
.beads/

# BV local config and caches
.bv/

# macOS resource forks
._*
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Legacy JAX oracle (large upstream clone, not part of this project)
legacy_jax_code/

# Exemplar reference specs (not part of this project)
references/

# Environment and secrets
.env
.env.*

# Original PNG (converted to webp)
*.png

# Ephemeral agent/runtime artifacts
storage.sqlite3
storage.sqlite3-*
*.sqlite3.bak
rustc-ice-*.txt
a.out
perf.data
perf.data.*
--graph-format.svg

# Remote compilation helper artifacts
.rch-target/
.rch-deploy-root/
rch-bin/
rch-install/

# Agent mail project identity
.agent-mail-project-id

# Codex build artifacts
target-codex*/
target_*/

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
test_meminfo

# Core dumps and agent config backups
core.*
core
*.mcp.json.*.bak
cline.mcp.json
opencode.json
windsurf.mcp.json
clippy.txt
clippy.json
clippy_results.json
clippy_unwrap.json
clippy_unwrap_clean.txt
check_output.txt
.next_backup*/
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3.bak
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*

# CI/E2E test output artifacts
artifacts/
