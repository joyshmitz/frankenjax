# Fuzz Corpus Layout

- `seed/<target>/` contains hand-authored starter inputs (>=3 per target).
- `generated/` is for minimized/generated corpus entries from fuzz runs.
- `crashes/` stores triaged crash artifacts and metadata index.
