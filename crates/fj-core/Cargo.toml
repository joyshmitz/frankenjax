[package]
name = "fj-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.218", features = ["derive"] }
smallvec = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
