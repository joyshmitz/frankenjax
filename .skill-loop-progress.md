# Skill Loop Progress

Skill: repeated-apply-skill -> extreme-software-optimization
Target: profile-backed perf beads

## Passes

- Pass 1: complete - rank-1 overwrite direct scatter kept; midpoint 21.746 ms -> 19.824 ms on ts2.
- Pass 2: complete - egraph saturation refresh rejected as no-code pass; fresh `fj-egraph` bench did not reproduce the 5 ms scoreboard hotspot.
- Pass 3: complete - fj-ad forward tape candidates rejected; best midpoint 228.46 us -> 225.10 us/225.18 us across workers was below the score threshold, so no source lever was kept.
- Pass 4: complete - fj-dispatch non-egraph dispatch now borrows the root Jaxpr; `api_jit_add` midpoint 5.2806 us -> 5.1010 us on ts2.
- Pass 5: complete - fj-api default API wrapper backends now borrow the `"cpu"` backend name; `jit/scalar_add` midpoint 5.1753 us -> 5.1017 us and `jit_vmap_grad/compose_helper` midpoint 10.144 us -> 9.6290 us.
- Pass 6: complete - fj-dispatch batched-operand gather flattening rejected; candidate regressed `vmap_gather/batched_operand_shared_indices` to 42.699 ms and `vmap_gather/batched_operand_batched_indices` to 33.012 ms on `ts2`, so no source lever was kept.
- Pass 7: complete - fj-cache hit lookup now compares SHA-256 digest bytes directly against stored lowercase hex; `cache_lookup/hit/in_memory` midpoint 448.63 ns -> 365.86 ns on `ts2`.
- Pass 8: complete - fj-backend-cpu singleton ready waves now bypass tensor-cost/rayon gate checks; `backend_execute/dependency_chain_512` mean 114.139 us -> 102.162 us and `backend_scheduler_cutover/dependency_chain/128` mean 29.090 us -> 25.479 us.
- Pass 16: complete - fj-ffi tensor unmarshalling now decodes fixed-width tensor buffers through dtype-specialized paths; `marshal/1k_buffer_to_tensor` mean 5.4424 us -> 0.91168 us.
- Pass 17: complete - fj-ffi tensor marshalling now encodes tensors through dtype-specialized paths; `marshal/1k_tensor_to_buffer` mean 4.4347 us -> 0.97132 us.
- Pass 18: rejected - fj-core manual `Equation` clone empty-container fast path regressed `core/jaxpr_clone_large` from 2.3611 us to 2.4225 us on `vmi1227854`; no source diff kept.
- Pass 19: complete - fj-lax rank-2 F64 3x3 SAME reduce-window now uses an order-preserving specialized interior path; `eval/reduce_window_64x64_3x3_same_f64` mean 138.35 us -> 84.355 us on `vmi1149989`.
- Pass 21: complete - fj-egraph Add/Mul-only supported segments now use a cached arithmetic-only rewrite slice; `egraph/optimize_aggressive_config` mean 203.90 us -> 111.64 us on `vmi1149989`.
- Pass 22: rejected - fj-cache `bytes_to_hex` byte-buffer candidate preserved behavior but regressed key construction; `cache_key/owned/empty_program` 273.92 ns -> 544.06 ns and source diff was reverted.
- Pass 23: complete - fj-egraph safe/default single-literal Add/Mul segments now bypass egg with the existing literal-first canonical no-op form; `optimize_add_zero` mean 8.9266 us -> 1.4878 us and `optimize_mul_one` mean 9.2561 us -> 1.4836 us on `vmi1153651`.
- Pass 24: complete - fj-interpreters DCE now uses forward-order retained cloning for 256+ equation graphs; `dce/all_used/1000eq` mean 132.14 us -> 107.47 us on `vmi1153651`.
- Pass 25: complete - fj-ad forward/reverse AD value stores now use dense VarId-indexed storage with sparse BTreeMap fallback; `value_and_grad_runtime/shared/deep_100_nodes` same-worker mean 264.24 us -> 226.19 us on `vmi1153651`.
- Pass 27: complete - fj-trace tracer avals and VarId assignment now use checked dense TracerId slots; same-worker `trace/make_jaxpr_vector_add` mean 1.4765 us -> 673.79 ns on `vmi1149989`, while `chain_5ops` remained a residual target at 2.3527 us -> 2.3471 us.
- Pass 29: complete - fj-dispatch tail-free elementwise nonzero-axis vmap now uses BatchTrace instead of loop-and-stack; `dispatch_latency/vmap/rank2_add_one_in_axes_1` mean 499.94 us on `vmi1153651` -> 277.88 us on `vmi1227854`.
- Pass 30: rejected - fj-dispatch rank2 axis-1 move/fused-add attempts regressed or stayed below threshold; `rank2_add_one_in_axes_1` baseline 141.07 us, rejected candidates 271.88 us / 168.68 us / 189.45 us, so no source lever was kept.
- Pass 31: complete - fj-lax same-shape Complex128 tensor multiply now uses a bit-identical direct complex multiply path; `eval/complex_mul_1k_complex128` mean 44.239 us on `vmi1156319` -> 1.6893 us on `vmi1227854`.
- Pass 32: complete - fj-lax same-shape I64 tensor Add now uses the same wrapping-add closure in a direct row-major path; `eval/add_1k_i64_vec` mean 21.968 us -> 1.7441 us on `vmi1149989`.
- Pass 33: rejected - fj-dispatch axis-1 scalar elementwise direct path regressed `dispatch_latency/vmap/rank2_add_one_in_axes_1` from 301.74 us to 463.43 us, so no source lever was kept.
- Pass 34: complete - fj-lax rank-2 F64 Dot/DotGeneral now routes standard matmul through the safe-Rust `matmul_2d` kernel; `eval/dot_256x256x256_f64` mean 493.61 ms -> 7.2340 ms.
- Pass 35: rejected - fj-lax `matmul_2d` manual 8-wide inner `j` unroll regressed raw GEMM evidence from 2.9222 ms to 6.0492 ms across remote Criterion runs, so the source hunk was restored and no source lever was kept.
- Pass 36: rejected - fj-dispatch batched-operand gather direct path regressed focused same-worker rows from 3.1843/3.0936 ms to 21.349/22.152 ms on `vmi1227854`; borrow-refined rerun on `vmi1156319` still failed at 29.900/23.993 ms, so no source lever was kept.
- Pass 37: rejected - fj-dispatch batched scatter all-equal operand initialization fast path passed behavior proof but regressed `vmap_scatter/batched_indices_updates`; pass baseline was 9.5462 ms [9.2955, 9.8333] on `vmi1293453`, candidate was 26.234 ms [25.522, 27.001] on `vmi1153651`, also worse than the earlier same-worker profile row at 23.703 ms [22.884, 24.647], so no source lever was kept.
- Pass 38: rejected - fj-lax same-shape F64 Add indexed loop passed bit-isomorphism proof but regressed the profile-backed large-F64 row; `eval/add_64k_f64_vec` baseline was 1.5259 ms [1.4182, 1.6194] on `vmi1149989` with dense reference 35.638 us [33.802, 38.317], while the candidate was 3.6207 ms [3.5077, 3.7282] on `vmi1153651`, so no source lever was kept.
- Pass 39: complete - fj-lax same-shape F64 tensor Nextafter now uses the existing `next_after_f64` helper in a direct row-major path; `eval/nextafter_1k_f64` mean 22.912 us [22.328, 23.439] on `vmi1153651` -> 2.3604 us [2.1872, 2.5276] on `vmi1149989`.
- Pass 42: complete - fj-core TensorValue elements now use Arc-backed `LiteralBuffer` copy-on-write storage; `eval/add_64k_f64_vec` mean 905.07 us -> 160.15 us, and the normalized vec/ref ratio improved 9.96x.
- Pass 43: rejected - fj-lax residual same-shape F64 Add micro-specializations did not clear the profile gate after pass42; baseline `eval/add_64k_f64_vec` was 72.467 us [67.809, 76.693] on `vmi1149989`, exact-size `Option<Vec<_>>` collection regressed to 120.60 us [113.46, 126.93] on the same worker, and Add-only specialization measured 163.30 us [159.12, 167.97] on `vmi1153651`. Source was restored; no optimization lever kept.
- Pass 44: complete - fj-core `Value::vector_f64` now uses dense F64 `LiteralBuffer` storage, enabling the existing same-shape F64 route to avoid Literal input materialization; `eval/add_64k_f64_vec` midpoint 76.890 us -> 29.070 us via RCH Criterion.
- Pass 45: complete - fj-backend-cpu already-topological dependency chains now bypass producer/consumer graph construction; `backend_scheduler_cutover/dependency_chain/512` mean 107.76 us -> 52.595 us and `/1000` mean 198.36 us -> 111.72 us via RCH Criterion. Local fallback timing was discarded.
- Pass 46: complete - fj-lax dense-backed F64 scalar/tensor broadcast now routes Add/Sub/Mul/Div through `dense::scalar_op`; same-worker `eval/scalar_mul_1k_f64_vec` midpoint 1.2194 us -> 299.66 ns and `eval/tensor_sub_scalar_1k_f64_vec` midpoint 1.1305 us -> 361.29 ns on `vmi1293453`.
- Pass 47: complete - fj-lax rank-2 F64 3x3 SAME reduce_window now packs literal-backed inputs into a contiguous F64 slab before preserving the existing row-major add order; `eval/reduce_window_64x64_3x3_same_f64` midpoint 182.92 us -> 52.977 us via RCH Criterion.
- Pass 48: complete - fj-lax real F64 QR now bypasses guaranteed-zero complex Householder scratch for nonzero real matrices; same-worker `linalg/qr_128x128_f64` midpoint 1.0700 ms -> 857.05 us on `vmi1149989`.
- Pass 51: complete - fj-lax eig QR iteration now multiplies `R * Q` with an upper-triangular kernel; same-worker `linalg/eig_48x48_f64` midpoint 28.262 ms -> 25.086 ms on `vmi1156319`.
- Pass 52: rejected - fj-lax `matmul_2d` row-block and transposed-RHS candidates failed the keep threshold; same-worker transposed-RHS regressed `linalg/matmul_2d_512x512x512_f64` 25.393 ms -> 67.890 ms, so no production source was kept.
- Pass 57: complete - fj-lax eig QR iteration now keeps internal Q matrices in real `f64` layout and wraps Complex128 zero-imag output only at the public boundary; same-worker `linalg/eig_48x48_f64` reference 12.512 ms -> 11.892 ms on `vmi1227854`.
- Pass 59: complete - fj-dispatch batched-operand gather now uses a segmented row-major copy path for axis-0 size-1 gather slices; RCH `vmap_gather/batched_operand_shared_indices` midpoint 548.59 us -> 23.297 us and `batched_operand_batched_indices` midpoint 609.56 us -> 22.070 us.
- Pass 60: complete - fj-lax dense F64 full `ReduceSum` now folds borrowed dense slices directly; `eval/reduce_sum_64k_f64` midpoint 286.68 us -> 77.698 us.
- Pass 61: rejected - fj-lax dense F64 unary output-storage wrapping preserved behavior but regressed `eval/sin_64k_f64` from 775.02 us to 830.50 us across RCH workers; source diff restored.
- Pass 63: complete - fj-dispatch rank-1 overwrite scatter now returns a lazy repeated-patch `LiteralBuffer` for valid batched index/update output; `vmap_scatter/batched_indices_updates` mean 328.28 us -> 23.507 us.
- Pass 66: complete - fj-dispatch exact I64 functional scan add-emit bodies now use a segmented row-major prefix-scan path; `vmap_scan/functional_sub_jaxpr_add_emit_128x64` mean 400.43 us -> 39.634 us.
- Pass 67: complete - fj-dispatch exact I64 rank-2 paired-vector dot now uses a direct row-major wrapping accumulator; `vmap_dot_i64/paired_vectors` mean 127.83 us -> 77.658 us on `ts2`.
- Pass 67: complete - fj-backend-cpu terminal scalar I64 Add dependency chains now use a direct threaded state-machine evaluator; `backend_scheduler_cutover/dependency_chain/512` mean 56.657 us -> 15.371 us and `backend_execute/dependency_chain_512` mean 56.397 us -> 15.388 us.
- Pass 72: complete - fj-dispatch dense F32 batch-axis-0 Add/Lt while now uses a direct per-lane state-machine path; `vmap_while/batched_scalar_f32_add_128` mean 121.38 us -> 17.501 us on `ts2`.
- Pass 73: complete - fj-dispatch dense F32 shared-init Add scan now uses a direct row-major path; `vmap_scan/shared_init_batched_xs_f32_add_128x64` mean 108.34 us -> 47.511 us on `ts2`.
- Pass 74: complete - fj-dispatch Eigh batching now skips the no-op batch-axis-0 move; `vmap_eigh/batched_matrix_3x3` mean 401.64 us -> 371.56 us on `ts2`.
- Pass 75: complete - fj-dispatch Eigh Jacobi now reuses row scratch buffers across rotations; `vmap_eigh/batched_matrix_3x3` mean 371.56 us -> 308.67 us on `ts2`.
- Pass 76: complete - fj-dispatch batched Eigh now reuses decomposition scratch buffers across batch slices; `vmap_eigh/batched_matrix_3x3` mean 314.34 us -> 263.74 us on `ts2`.
- Pass 77: rejected - fj-dispatch dense F64 Eigh output buffers did not clear the gate; candidate measured 274.76 us on `ts2`, slower than the Pass76 same-worker 263.74 us baseline, so no source was kept.
- Pass 82: rejected - fj-lax 4x4 local-accumulator GEMM preserved bit-order behavior but regressed `linalg/matmul_2d_512x512x512_f64` on `vmi1153651`; profile midpoint 60.229 ms -> candidate 87.437 ms, so no source was kept.
- Pass 83: rejected - fj-lax packed 16-column B-panel GEMM preserved bit-order behavior but regressed `linalg/matmul_2d_512x512x512_f64` on `ts2`; baseline midpoint 33.277 ms -> candidate 59.272 ms, so no source was kept.
- Pass 84: rejected - fj-lax direct 64-column panel GEMM preserved bit-order behavior but regressed `linalg/matmul_2d_512x512x512_f64` on `ts2`; baseline midpoint 33.277 ms -> candidate 43.166 ms, so no source was kept.
- Pass 85: rejected - fj-lax eig QR scratch reuse preserved bit-order behavior but did not clear the keep threshold; same-worker `ts2` baseline `linalg/eig_48x48_f64` was 17.863 ms [17.731, 17.949], candidate confirmations were 17.602 ms [17.507, 17.718] and 17.498 ms [17.324, 17.779], score 1.6, so no source was kept. Next eig primitive: shifted/deflated QR with packed Hessenberg real layout, or first file a behavior-contract bead for FP-reordering eig algorithms.
- Pass 87: rejected - fj-lax full-B transpose GEMM preserved per-cell k-order behavior but regressed `linalg/matmul_2d_512x512x512_f64` on `ts2`; baseline midpoint 33.898 ms -> candidate 152.65 ms, score 0.0, so no source was kept. Next primitive: stop bit-exact GEMM traversal variants; reprofile and attack a different top hotspot, or file a behavior-contract bead for FP-reordering GEMM algorithms before attempting blocked/recursive kernels.
- Pass 89: complete - fj-lax literal-backed F64 max/min reduce_window now packs literals once with `reduce_window_f64_values` and reuses the dense max/min pooling kernel; same-worker `eval/maxpool_256x256_f64_literal_ref` midpoint 12.269 ms -> 2.6980 ms on `ts2`.

## Current Evidence

- Pass 1 artifact: artifacts/performance/evidence/vmap_scatter_rank1_overwrite_2026-06-01.json
- Pass 1 result: `vmap_scatter/batched_indices_updates` midpoint 21.746 ms -> 19.824 ms on `ts2`; score 3.45; golden output sha256 0e77c60d1d9070b0b1ccda582048094f9c04356f2a3cda03a2b4ec5f82fddd6d.
- Pass 2 artifact: artifacts/performance/evidence/egraph_saturation_refresh_noop_2026-06-01.json
- Pass 2 result: `egraph/optimize_safe_config` midpoint 120.65 us and `egraph/optimize_aggressive_config` midpoint 613.23 us on `ts2`; no code lever because the 5 ms scoreboard hotspot did not reproduce.
- Pass 3 artifact: artifacts/performance/evidence/fj_ad_forward_tape_rejected_2026-06-02.json
- Pass 3 result: `value_and_grad_runtime/shared/deep_100_nodes` fresh midpoint 228.46 us on `vmi1152480`; rejected output-move 231.51 us, SmallVec 225.10 us, and indexed-env 225.18 us on `ts2`; no source lever kept.
- Pass 4 artifact: artifacts/performance/evidence/dispatch_borrow_jaxpr_2026-06-02.json
- Pass 4 result: `api_vs_dispatch/api_jit_add` midpoint 5.2806 us -> 5.1010 us and `dispatch_jit_add` midpoint 4.8641 us -> 4.7862 us on `ts2`; score 2.25.
- Pass 5 artifact: artifacts/performance/evidence/api_backend_cow_pass5_2026-06-02.json
- Pass 5 result: `api_overhead/jit/scalar_add` midpoint 5.1753 us -> 5.1017 us, `api_overhead/vmap/vector_add_one` midpoint 5.4013 us -> 5.1022 us, and `api_composition/jit_vmap_grad/compose_helper` midpoint 10.144 us -> 9.6290 us; score 4.5.
- Pass 6 artifact: artifacts/performance/evidence/vmap_gather_flatten_rejected_pass6_2026-06-02.json
- Pass 6 result: fresh vmap multiplier baseline on `vmi1227854` found `vmap_scatter/batched_indices_updates` at 12.875 ms and batched-operand gather at 4.7739/4.4142 ms; flattened batched-operand gather candidate regressed on `ts2`, scoring 0.0, and no source diff was retained.
- Pass 7 artifact: artifacts/performance/evidence/fj_cache_lookup_hex_compare_pass7_2026-06-02.json
- Pass 7 result: `cache_lookup/hit/in_memory` midpoint 448.63 ns -> 365.86 ns on `ts2`; score 6.0; golden output sha256 19dc51e31897469421a211fc1f3f021d757f570f897c8bd48fda928505c7e075.
- Pass 8 artifact: artifacts/performance/evidence/fj_backend_cpu_single_ready_wave_pass8_2026-06-02.json
- Pass 8 result: singleton ready-wave fast path kept; `backend_execute/dependency_chain_512` mean 114.139 us -> 102.162 us, cutover 128 mean 29.090 us -> 25.479 us, cutover 255 mean 55.816 us -> 51.419 us, cutover 256 mean 55.777 us -> 53.809 us, cutover 1000 mean 229.866 us -> 214.914 us; score 15.0; golden output sha256 e831cb440fde35a6bdb9b65340f341f2f7681e84125e31b1207d2bb60255d9f8.
- Pass 16 artifact: artifacts/performance/evidence/fj_ffi_tensor_unmarshal_pass16_2026-06-02.json
- Pass 16 result: `marshal/1k_buffer_to_tensor` mean 5.4424 us on `vmi1149989` -> 0.91168 us on `vmi1227854`; score 15.0; golden output sha256 e6767b8edab90d708df47c0a2b708c6b99ab339136817934ec95fd14e94ef868.
- Pass 17 artifact: artifacts/performance/evidence/fj_ffi_tensor_marshal_pass17_2026-06-02.json
- Pass 17 result: `marshal/1k_tensor_to_buffer` mean 4.4347 us on `vmi1149989` -> 0.97132 us on `vmi1227854`; score 15.0; golden output sha256 b01f5568d30780b08b17898a662906037ce667b7a7bd25099453fabead7c6921.
- Pass 18 artifact: artifacts/performance/evidence/fj_core_equation_clone_pass18_2026-06-02.rejected.json
- Pass 18 result: fj-core `core/jaxpr_clone_large` ranked first at 2.3611 us on `vmi1227854`; manual `Equation` clone candidate rebench was 2.4225 us on the same worker, score 0.0, and the source diff was dropped.
- Pass 19 artifact: artifacts/performance/evidence/fj_lax_reduce_window_3x3_same_pass19_2026-06-02.json
- Pass 19 result: `eval/reduce_window_64x64_3x3_same_f64` mean 138.35 us -> 84.355 us on `vmi1149989`; score 15.0; golden output sha256 91d75d87b714db2d40582e0585da9c92466a886f02b32e58f66d36837d47ea85.
- Pass 21 artifact: artifacts/performance/evidence/fj_egraph_arithmetic_rules_pass21_2026-06-02.json
- Pass 21 result: `egraph/optimize_aggressive_config` mean 203.90 us -> 111.64 us on `vmi1149989`; score 20.0; golden output sha256 5610b99922f1b609b51cf5c8a249ea98229a9ac69d85ee027b3ad88c4aa98779.
- Pass 23 artifact: artifacts/performance/evidence/fj_egraph_single_identity_pass23_2026-06-02.json
- Pass 23 result: `egraph/optimize_add_zero` mean 8.9266 us -> 1.4878 us and `egraph/optimize_mul_one` mean 9.2561 us -> 1.4836 us on `vmi1153651`; score 20.0; golden output sha256 901efcc2f56cabde9e3726f7f15247b25e5dae2a6ea3c028195c1915ee245b09.
- Pass 24 artifact: artifacts/performance/evidence/fj_interpreters_dce_forward_order_pass24_2026-06-02.json
- Pass 24 result: `dce/all_used/1000eq` mean 132.14 us -> 107.47 us on `vmi1153651`; score 9.0; golden output sha256 9c4c856e948a9c072901c3dc7baf124ec91e59bd2dc42d804c94791f848c77d5.
- Pass 25 artifact: artifacts/performance/evidence/fj_ad_indexed_value_store_pass25_2026-06-02.json
- Pass 25 result: `value_and_grad_runtime/shared/deep_100_nodes` same-worker mean 264.24 us -> 226.19 us on `vmi1153651`; score 6.0; golden output sha256 19c2fb7a3c1cec134794978ab7dd49b090f5ea8cbc86a686ddc6cd6193638b37.
- Pass 27 artifact: artifacts/performance/evidence/fj_trace_dense_tracer_store_pass27_2026-06-02.json
- Pass 27 result: checked dense TracerId slots kept; same-worker `trace/make_jaxpr_vector_add` mean 1.4765 us -> 673.79 ns, `make_jaxpr_mul` 710.28 ns -> 480.61 ns, `make_jaxpr_add` 646.66 ns -> 469.84 ns on `vmi1149989`; score 6.0; golden output sha256 c2e73a3134b13238c5a75665c3983a4246151e1ea4bb9e4add776dd715e0086e. `make_jaxpr_chain_5ops` stayed effectively flat and is filed as the next residual hotspot.
- Pass 28 artifact: artifacts/performance/evidence/fj_trace_chain_clone_pass28_2026-06-02.json
- Pass 28 result: move-based TraceFrame finalization kept; clean HEAD remote baseline `trace/make_jaxpr_chain_5ops` mean 3.4242 us on `vmi1156319` -> 1.6004 us on `vmi1293453`; speedup 2.14x; score 6.0; golden output sha256 cb6e29c8a7cb0093603d38407643adf7e73abbc2b063e4d634af23a0165d88e3. Rejected local fallback runs were not counted.
- Pass 29 artifact: artifacts/performance/evidence/fj_dispatch_vmap_axis1_pass29_2026-06-03.json
- Pass 29 result: elementwise nonzero-axis BatchTrace gate kept; `dispatch_latency/vmap/rank2_add_one_in_axes_1` mean 499.94 us on `vmi1153651` -> 277.88 us on `vmi1227854`; speedup 1.80x; score 9.0; golden output sha256 eab4983d401c391b6ea9dfe120c3f5b9e5c09af2f4bbe5879fd8829bbe7534d4.
- Pass 30 result: `frankenjax-mcqr.13` rejected after fresh rch profile of `dispatch_latency/vmap/rank2_add_one_in_axes_1` at 141.07 us [139.68, 142.50] on `vmi1293453`; naive rank2 move rebench 271.88 us, fused output-contiguous I64 scalar add 168.68 us, and fused input-contiguous I64 scalar add 189.45 us on `vmi1149989`; no source diff retained.
- Pass 31 artifact: artifacts/performance/evidence/fj_lax_complex128_mul_pass31_2026-06-03.json
- Pass 31 result: same-shape Complex128 tensor `Mul` direct path kept; `eval/complex_mul_1k_complex128` mean 44.239 us [43.507, 45.024] on `vmi1156319` -> 1.6893 us [1.5939, 1.7863] on `vmi1227854`; speedup 26.19x; score 20.0; golden output sha256 d0f714129d53f5b5edc7b06125b1461b6a780ec118d8f81534fb916eb594748a. Source landed in `489fcd3a` together with `frankenjax-mcqr.14`; this progress entry preserves the separate pass-31 proof trail.
- Pass 32 artifact: artifacts/performance/evidence/fj_lax_i64_add_pass32_2026-06-03.json
- Pass 32 result: same-shape I64 tensor `Add` direct path kept; `eval/add_1k_i64_vec` mean 21.968 us [21.715, 22.264] -> 1.7441 us [1.7067, 1.7800] on `vmi1149989`; speedup 12.60x; score 20.0; golden output sha256 ffb9e599d61ea6db17eb80bb75faf06e30800b06a3829987ad776965e7ac0c3e.
- Pass 33 result: `frankenjax-joer` rejected after fresh rch profile of `dispatch_latency/vmap/rank2_add_one_in_axes_1` at 301.74 us [299.49, 303.90] on `vmi1156319`; direct axis-1 scalar Add candidate rebench was 463.43 us [451.51, 476.37] on `vmi1153651`; no source diff retained.
- Pass 34 artifact: artifacts/performance/evidence/fj_lax_dot_matmul_2d_pass34_2026-06-03.json
- Pass 34 result: rank-2 F64 `Dot`/standard `DotGeneral` matmul now uses `matmul_2d`; `eval/dot_256x256x256_f64` mean 493.61 ms [429.38, 563.62] on `vmi1264463` -> 7.2340 ms [6.9111, 7.5771] on `vmi1149989`; speedup 68.23x; score 25.0; golden output sha256 70016b5338f01d8404fa76a7d5d0c5ad673cc2eed54775283aa1559861e04fb5.
- Pass 35 artifact: artifacts/performance/evidence/fj_lax_matmul_2d_unroll8_pass35_2026-06-03.rejected.json
- Pass 35 result: `frankenjax-mcqr.27` rejected after fresh rch Criterion baseline of `linalg/matmul_2d_256x256x256_f64` at 2.9222 ms [2.8733, 2.9764] and production `eval/dot_256x256x256_f64` at 4.6342 ms [4.5549, 4.7210] on `vmi1293453`; manual 8-wide contiguous `j` unroll rebench was 6.0492 ms [5.8913, 6.2034] on `vmi1149989`, so the source hunk was restored. Golden output sha256 09eb2cddcb075ee7b8140664156c4888ed202a4a14369d26e03fed512a1efae3.
- Pass 36 artifact: artifacts/performance/evidence/fj_dispatch_gather_batched_operand_direct_pass36_2026-06-03.rejected.json
- Pass 36 result: `frankenjax-ggpw` rejected after fresh rch `fj-dispatch` profile found `vmap_gather/batched_operand_shared_indices` 28.519 ms [27.953, 29.065] and `batched_operand_batched_indices` 29.168 ms [27.959, 30.612] on `vmi1153651`; focused same-worker baseline was 3.1843 ms [3.0363, 3.3046] and 3.0936 ms [3.0111, 3.1679] on `vmi1227854`; direct-path candidate regressed to 21.349 ms [21.032, 21.647] and 22.152 ms [21.753, 22.596] on `vmi1227854`, and the borrow-refined rerun failed to show a clear win at 29.900 ms [29.217, 30.562] and 23.993 ms [22.713, 26.001] on `vmi1156319`. Source hunk restored; golden output sha256 e131a02069898c37c19e05b75587cab83a85edda617edd45eaa44ef84a5f99ba.
- Pass 37 artifact: artifacts/performance/evidence/fj_dispatch_scatter_batched_indices_pass37_2026-06-03.rejected.json
- Pass 37 result: `frankenjax-6cdy` rejected after fresh rch baseline of `vmap_scatter/batched_indices_updates` at 9.5462 ms [9.2955, 9.8333] on `vmi1293453`; all-equal operand initialization candidate rebench was 26.234 ms [25.522, 27.001] on `vmi1153651`, which also failed to beat the earlier same-worker full-profile row at 23.703 ms [22.884, 24.647]. Source hunk restored; golden output sha256 29f539ce4ca698a38c578af9464d83f3e842461a9e968ab2ae1c40165cb9d05f.
- Pass 38 artifact: artifacts/performance/evidence/fj_lax_dense_f64_storage_pass38_2026-06-03.rejected.json
- Pass 38 result: `frankenjax-mcqr.30` non-layout F64 Add indexed-loop candidate rejected after fresh rch baseline of `eval/add_64k_f64_vec` at 1.5259 ms [1.4182, 1.6194] and dense reference at 35.638 us [33.802, 38.317] on `vmi1149989`; candidate rebench was 3.6207 ms [3.5077, 3.7282] on `vmi1153651`. Source hunk restored; golden output sha256 5bd0cce1ce702a13dd93acf5757df84f37ea5c86fb369678846330f2e0d8a39d. Real storage-layout work remains the required no-gaps path.
- Pass 39 artifact: artifacts/performance/evidence/fj_lax_nextafter_f64_pass39_2026-06-03.json
- Pass 39 result: `frankenjax-0o2i` kept a same-shape F64 tensor `Nextafter` direct path; fresh rch baseline `eval/nextafter_1k_f64` was 22.912 us [22.328, 23.439] on `vmi1153651`, rebench was 2.3604 us [2.1872, 2.5276] on `vmi1149989`, speedup 9.71x, score 20.0; golden output sha256 55cd2c25745b4baeeed781e211dc1ce74f10ae0cb4451bf9d6876d7dccae3572.
- Pass 42 artifact: artifacts/performance/evidence/fj_core_literal_buffer_pass42_2026-06-03.json
- Pass 42 result: `frankenjax-mcqr.30.1` kept `LiteralBuffer` shared tensor element storage; rch baseline `eval/add_64k_f64_vec` was 905.07 us [853.35, 975.11], rebench was 160.15 us [155.17, 164.86], speedup 5.65x, score 9.0; golden output sha256 b75c7a61cd77b65165546ff418de2280f0915e6f3a96b2c010d46a71903fe9cb.
- Pass 43 artifact: artifacts/performance/evidence/fj_lax_dense_f64_residual_pass43_2026-06-03.rejected.json
- Pass 43 result: `frankenjax-mcqr.30.2` rejected two residual F64 Add micro-specializations; baseline `eval/add_64k_f64_vec` was 72.467 us [67.809, 76.693] on `vmi1149989`, exact-size `Option<Vec<_>>` collection was 120.60 us [113.46, 126.93] on `vmi1149989`, and Add-only specialization was 163.30 us [159.12, 167.97] on `vmi1153651`; source restored; score 0.0; golden output sha256 f4ad337858563d63c3144a11d0522553ad9524e67c0e4cd9f7e2662eafb6615a.
- Pass 44 artifact: artifacts/performance/evidence/fj_core_literal_buffer_dense_f64_pass44_2026-06-03.json
- Pass 44 result: `frankenjax-mcqr.30` kept dense F64 LiteralBuffer storage for `Value::vector_f64`; fresh RCH baseline `eval/add_64k_f64_vec` was 76.890 us [74.833, 79.226] on `vmi1227854`, rebench was 29.070 us [27.069, 31.394] on `vmi1264463`, speedup 2.65x, score 5.33; golden output sha256 5f8ac6d4f527ae05d7081467b801f7229bcac132533fd209e66aecbc33d63c9f.
- Pass 45 artifact: artifacts/performance/evidence/fj_backend_cpu_topological_chain_pass45_2026-06-03.json
- Pass 45 result: `frankenjax-mcqr.39` kept the linear topological pure-chain scheduler fast path; counted RCH baseline on `vmi1149989` measured `/512` 107.76 us and `/1000` 198.36 us, rebench on `vmi1227854` measured `/512` 52.595 us and `/1000` 111.72 us; score 16.0; golden output sha256 00ac5ccc22910cc5fb0fce229dc5d3febe3a53171b482d398ca0fed289ddd681.
- Pass 46 artifact: artifacts/performance/evidence/fj_lax_dense_f64_scalar_broadcast_pass46_2026-06-03.json
- Pass 46 result: `frankenjax-mcqr.30.5` kept the dense F64 scalar broadcast route; same-worker RCH baseline on `vmi1293453` measured `eval/scalar_mul_1k_f64_vec` 1.2194 us and `eval/tensor_sub_scalar_1k_f64_vec` 1.1305 us, rebench measured 299.66 ns and 361.29 ns; score 10.0; golden output sha256 be873886d0b6b1a31e3d440a8c26919804112abc167fb27ecd1c407d40a8ef28.
- Pass 47 artifact: artifacts/performance/evidence/fj_lax_reduce_window_packed_f64_pass47_2026-06-03.json
- Pass 47 result: `frankenjax-mcqr.30.6` kept the packed F64 reduce_window route; RCH baseline on `vmi1149989` measured `eval/reduce_window_64x64_3x3_same_f64` 182.92 us [171.90, 193.87], rebench on `vmi1264463` measured 52.977 us [48.668, 58.488]; speedup 3.45x, score 8.0; golden output sha256 3eadbe8819bff4b60a9a7cd700cd88aaed0ed9b86b2b8a9f72954e65da467e97.
- Pass 48 artifact: artifacts/performance/evidence/fj_lax_qr_real_fast_path_pass48_2026-06-03.json
- Pass 48 result: `frankenjax-p1vbf.1` kept the real-valued QR Householder fast path; same-worker RCH baseline on `vmi1149989` measured `linalg/qr_128x128_f64` 1.0700 ms [1.0592, 1.0851], rebench measured 857.05 us [830.73, 876.42]; speedup 1.248x, score 7.5; golden output sha256 282fa19f1724ae5d2a1a36754e627fb311d11aa029b636a2431b95f1424ecc89.
- Pass 51 artifact: artifacts/performance/evidence/fj_lax_eig_upper_triangular_rq_pass51_2026-06-03.json
- Pass 51 result: `frankenjax-p1vbf.4` kept the upper-triangular `R * Q` eig QR update; same-worker RCH baseline on `vmi1156319` measured `linalg/eig_48x48_f64` 28.262 ms [27.853, 28.698], rebench measured 25.086 ms [24.650, 25.527]; speedup 1.127x; score 10.0; golden output sha256 7bfc0636206bcab5e2a856526f6f176159ea1770f17f5cd882f6c8157a6a339c.
- Pass 52 artifact: artifacts/performance/evidence/fj_lax_matmul_cache_block_pass52_2026-06-03.json
- Pass 52 result: `frankenjax-2wewi` rejected row-block and transposed-RHS GEMM candidates; fresh RCH baseline `linalg/matmul_2d_512x512x512_f64` was 25.393 ms [25.140, 25.650] on `vmi1227854`, best row-block signal was only 24.732 ms [24.454, 25.014] cross-worker, and same-worker transposed-RHS regressed to 67.890 ms [67.501, 68.314]. Source restored; score 0.0; golden output sha256 360e573dfafd46a11b8bdb0029d7e265ecc38098d8018724f04d1096764505d6.
- Pass 53 artifact: artifacts/performance/evidence/fj_lax_dense_f64_transpose_pass53_2026-06-03.rejected.json
- Pass 53 result: `frankenjax-mcqr.30.7` rejected dense F64 transpose output wrapping; fresh RCH baseline `eval/transpose_256x256_f64` was 189.35 us [187.73, 190.91] on `vmi1227854`, candidate rebench was 401.41 us [395.21, 407.99] on `vmi1153651`. Source restored; score 0.0; golden output sha256 074e2db617a00c4f4b6414cabe25723a67e4b7bca06eaf3af9299587057bf1f7. Next primitive is producer-side dense transpose/typed layout that avoids literal materialization before the transpose kernel.
- Pass 54 artifact: artifacts/performance/evidence/fj_lax_packed_panel_gemm_pass54_2026-06-03.rejected.json
- Pass 54 result: `frankenjax-p1vbf.5` rejected a four-row register GEMM microkernel; fresh RCH linalg profile on `vmi1227854` measured `linalg/matmul_2d_512x512x512_f64` at 26.507 ms [26.158, 26.871], and candidate rebench measured 55.889 ms [52.716, 59.423] on `vmi1153651`. Source restored; score 0.0; golden output sha256 0f4fd182d07ce1163d1537d75832003534d39f61b361e8ffc8b7b4e5591c094d. Next primitive is actual packed B panels or cache-size KC blocking; register-only blocking reloads A across column work and regressed.
- Pass 55 artifact: artifacts/performance/evidence/fj_lax_gemm_npanel_pass55_2026-06-03.rejected.json
- Pass 55 result: `frankenjax-p1vbf.6` rejected Rayon row-parallel GEMM; same-worker RCH baseline on `vmi1149989` measured `linalg/matmul_2d_512x512x512_f64` at 24.876 ms [24.337, 25.411], and candidate rebench measured 26.633 ms [24.015, 29.514] with high variance. Source restored; score 0.0; golden output sha256 32a842c6a1b71ca2e7c37e0bc5975d82b5ed5fd8badc3fc1d35c74b060fdd928. Next primitive is cache-size KC blocking or recursive/cache-oblivious GEMM before revisiting parallelism.
- Pass 56 artifact: artifacts/performance/evidence/fj_lax_simd_gemm_pass56_2026-06-03.rejected.json
- Pass 56 result: `frankenjax-p1vbf.7` rejected portable SIMD f64x4 GEMM lanes; fresh RCH linalg profile on `vmi1167313` measured `linalg/matmul_2d_512x512x512_f64` at 50.035 ms [48.412, 51.773], and candidate rebench measured 68.923 ms [65.679, 72.506] on `vmi1153651`. Source restored; score 0.0; golden output sha256 6c9849d7bd9ea60054c3e16998ac0de9119118bc36da13cdd60c4dc9f0838f10. Next primitive is recursive/cache-oblivious GEMM or cache-size KC blocking; lane-level SIMD and coarse row parallelism both regressed.
- Pass 57 artifact: artifacts/performance/evidence/fj_lax_eig_real_q_layout_pass57_2026-06-03.json
- Pass 57 result: `frankenjax-p1vbf.8` kept the real-Q eig QR layout; profile target `linalg/eig_48x48_f64` was 27.616 ms [26.492, 28.760] on `vmi1167313`, same-worker reference was 12.512 ms [12.270, 12.757] on `vmi1227854`, and candidate rebench was 11.892 ms [11.562, 12.229] on `vmi1227854`; speedup 1.052x; score 3.2; golden output sha256 474e89bab282ed5273749161ab1de1956c7eb8231fda51bc158c68f1ec279d43. Next primitive after reprofile is shifted/deflated QR or packed real Hessenberg layout if eig remains hot, otherwise recursive/cache-oblivious GEMM.
- Pass 58 artifact: artifacts/performance/evidence/fj_lax_reduce_sum_64k_bench_pass58_2026-06-03.json
- Pass 58 result: `frankenjax-mcqr.30.8` landed benchmark-only coverage for `eval/reduce_sum_64k_f64`; RCH Criterion on `vmi1149989` measured 259.73 us [259.73, 304.46] with midpoint 286.68 us. No production behavior changed; golden output sha256 6eb8e1b85fd40444b137856e293b108189cedc5bac2e3664e6b6ab7ce1f38b91. Next primitive is profile-backed dense F64 reduction storage, kept only if a single production lever scores >=2.0.
- Pass 59 artifact: artifacts/performance/evidence/fj_dispatch_segmented_gather_pass59_2026-06-03.json
- Pass 59 result: `frankenjax-yty34` kept the segmented batched-operand gather direct path; fresh RCH profile target on `vmi1293453` measured `vmap_gather/batched_operand_shared_indices` at 548.59 us and `vmap_gather/batched_operand_batched_indices` at 609.56 us. Final RCH rebench on `vmi1293453` measured 23.297 us [21.957, 25.011] and 22.070 us [21.425, 22.815]; speedups 23.55x and 27.62x; score 20.0; golden output sha256 f9c21a6dc2a8453292e556dde06b17e057f94951011af990eb333dad74df9dad.
- Pass 60 artifact: artifacts/performance/evidence/fj_lax_dense_f64_reduce_sum_pass60_2026-06-03.json
- Pass 60 result: `frankenjax-mcqr.30.9` kept the dense F64 full `ReduceSum` slice path; RCH baseline from pass58 on `vmi1149989` measured `eval/reduce_sum_64k_f64` at 286.68 us [259.73, 304.46], and final RCH rebench on `vmi1153651` measured 77.698 us [74.171, 82.568]. Speedup 3.69x midpoint, conservative speedup 3.15x, score 8.0; golden output sha256 4203c231009813c279d4f8f18debd546f355555dc0ff7f6d20e8f3d5edb3f0ad. Next primitive is reprofiled dense F64 axis reductions or producer-side dense transpose/typed layout, whichever is the next top ready/profiled target.
- Pass 61 artifact: artifacts/performance/evidence/fj_lax_dense_f64_sin_pass61_2026-06-03.rejected.json
- Pass 61 result: `frankenjax-6fvjw` rejected dense F64 unary output-storage wrapping after profile-backed `eval/sin_64k_f64` baseline 775.02 us [756.67, 783.40] on `vmi1227854`; candidate proof tests passed and dense-vs-literal fixture sha256 was fbefbae01eb564c9cca9aec56e8c03bf8052e42be318ce29f3206af18f86f28d, but final RCH rebench measured 830.50 us [827.84, 833.31] on `ts2`. Score 0.0; source restored. Next primitive is a deeper dense transcendental algorithm rather than output-storage wrapping.
- Pass 62 artifact: artifacts/performance/evidence/fj_lax_dense_f64_broadcast_pass62_2026-06-03.rejected.json
- Pass 62 result: `frankenjax-r3waf` rejected dense-aware mixed F64 broadcast gather after focused RCH baseline of `eval/add_broadcast_bias_1k_f64` at 10.170 us [9.8319, 10.658] on `vmi1227854`; candidate behavior proof passed (`broadcast_binary_f64` tests 2 passed), but final RCH rebench measured 16.953 us [16.753, 17.232] on `ts2`. Score 0.0; source restored; golden output sha256 08568cc7743a9a15f2dc817723e16d063ea9e1cd8f6b260a7539406037a8a1f5. Next primitive is a deeper broadcast kernel rewrite rather than dense output wrapping.
- Pass 63 artifact: artifacts/performance/evidence/fj_dispatch_lazy_scatter_pass63_2026-06-03.json
- Pass 63 result: `frankenjax-y160i` kept lazy repeated-patch scatter output; baseline `vmap_scatter/batched_indices_updates` mean 328.28 us [315.71, 344.53] on `vmi1293453`, rebench mean 23.507 us [23.412, 23.616] on `ts2`; speedup 13.97x; score 12.5; golden output sha256 71718e90b90c6f95379a180c70711cf8fe3c2ed178bf6c398df0cd7b2d129001.
- Policy: one optimization lever per commit; keep only score >= 2.0.
- Pass 64 artifact: artifacts/performance/evidence/fj_lax_dense_f64_reduce_prod_max_min_pass64_2026-06-03.json
- Pass 64 result: `frankenjax-mcqr.30.10` generalized `eval_dense_f64_full_reduce` (formerly `_sum`) from ReduceSum-only to ReduceSum|ReduceProd|ReduceMax|ReduceMin, closing the dense-F64 full-reduction gap for prod/max/min. Same-worker RCH bench on `vmi1153651`: `eval/reduce_max_64k_f64` dense 157.877us [152.435, 165.005] vs same-process `Vec<Literal>` reference 585.271us [571.113, 600.775] = 3.707x midpoint, 3.461x conservative; companion same-worker ReduceSum dense 67.895us vs literal 537.221us = 7.913x. Score 8.0; golden output sha256 208c93b026e48d49bfc08039eb799014bd7b6f7c9d63ca2cb7aed7b75ceaf88b. Bit-exact parity proven by `dense_f64_full_reduce_prod_max_min_bit_identical_to_literal_path` (NaN/±inf/signed-zero); 29 reduction tests pass; check + clippy -D warnings clean. Next primitive: dense F64 *axis* (partial) reductions — eval_reduce_axes still strides Vec<Literal>; target the strided gather-into-dense-accumulator layout next.
- Pass 66 artifact: artifacts/performance/evidence/fj_dispatch_functional_scan_qi3g1_2026-06-03.json
- Pass 66 result: `frankenjax-qi3g1` kept the segmented prefix-scan direct path for exact I64 functional scan add-emit bodies. Focused RCH baseline `vmap_scan/functional_sub_jaxpr_add_emit_128x64` was 400.43 us [398.63, 402.30] on `ts2`; final RCH rebench was 39.634 us [38.748, 40.563] on `ts1`; speedup 10.10x, conservative interval speedup 9.82x, score 12.5; golden output sha256 28b9d02889825fffcb3ae7022bf378ca8e2e1308eb1c39db07f6822ed7c080bb. Forward/reverse/wrapping/multi-carry fallback scan tests pass; full `fj-dispatch` test/check/clippy/fmt/UBS proof is recorded in the artifact. Progress number is unique because Pass64 already landed on the LAX dense-reduction bead and Pass65 is reserved by the active concat bead.
- Pass 67 artifact: artifacts/performance/evidence/fj_dispatch_i64_dot_pass67_2026-06-03.json
- Pass 67 result: `frankenjax-mcqr.42` kept the exact I64 paired-vector dot direct path. Post-pass66 RCH reprofile found `vmap_dot_i64/paired_vectors` at 141.64 us [138.94, 144.19] on `ts2`; focused same-worker baseline was 127.83 us [126.08, 129.31], and final same-worker rebench was 77.658 us [76.458, 79.132]. Speedup 1.65x, conservative interval speedup 1.59x, score 10.0; golden output sha256 5f9c2bd15104b5b7c394253dcc5bd92470ee562679976561682b5eb44887d7ae. The direct path uses `wrapping_mul` + `wrapping_add`, matching fj-lax signed integer dot semantics; full `fj-dispatch` test/check/clippy/fmt/UBS proof is recorded in the artifact.
- Pass 65 artifact: artifacts/performance/evidence/fj_lax_lazy_concat_pass65_2026-06-03.json
- Pass 65 result: `frankenjax-6alq6` kept lazy `LiteralBuffer` concat storage for `eval_concatenate`, replacing eager output block copies with ordered source slices that materialize only for literal observation/mutation. RCH profile baseline on `ts1`: `eval/concat_axis1_3x256x128_f64` 54.183 us [52.831, 56.090] and `eval/concat_axis0_3x128x256_f64` 49.319 us [47.158, 50.871]. Final RCH rebench on `ts2`: axis1 28.971 us [28.899, 29.064] for 1.870x midpoint / 1.817x conservative speedup; axis0 342.77 ns [341.20, 344.50] for 143.89x midpoint / 136.89x conservative speedup. Score 5.33; golden output sha256 c67aa4b2763cbefe2a143b5e09bb26c20f9561494c080d6ddac5ffbd051cbaef. Ordering, tie-breaking, FP bits, RNG absence, and error surfaces are preserved; focused `fj-core`/`fj-lax` tests pass; crate-scoped check/clippy/fmt are clean. UBS remains nonzero only from existing file-wide findings after pass65 panic/expect cleanup.
- Pass 67 artifact: artifacts/performance/evidence/fj_backend_cpu_i64_add_chain_pass67_2026-06-03.json
- Pass 67 result: `frankenjax-enyd2` kept a terminal scalar I64 Add-chain direct threaded evaluator in `fj-backend-cpu`. RCH profile baseline on `ts1`: `backend_scheduler_cutover/dependency_chain/512` 56.657 us [55.846, 57.545], `/1000` 121.57 us [118.46, 126.23], and `backend_execute/dependency_chain_512` 56.397 us [56.105, 56.752]. Final RCH rebench on `ts1`: `/512` 15.371 us [15.179, 15.712], `/1000` 39.549 us [39.017, 40.643], and backend execute 15.388 us [15.157, 15.886]. Speedups are 3.686x, 3.074x, and 3.665x midpoint; score 8.0; golden output sha256 d9bb9639149d132f38106a258e8a6397bb8eeb038d8ac7557053e9349d9a639d. Ordering, tie-breaking, I64 wrapping, FP exclusion, RNG absence, and fallback/error surfaces are preserved; focused and full `fj-backend-cpu` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 65 artifact: artifacts/performance/evidence/fj_lax_dense_f64_axis_reduce_pass65_2026-06-03.json
- Pass 65 result: `frankenjax-mcqr.30.11` added `dense_f64_axis_reduce` — eval_reduce_axes' float branch now folds the contiguous `as_f64_slice()` with a row-major out-index odometer (maintained incrementally to reproduce multi_to_out_flat(flat_to_multi_into(flat)) in ascending flat order), removing BOTH the per-element multi-index decode and the 24-byte Literal materialization. Same-worker RCH on `vmi1153651`: `eval/reduce_sum_256_axis1_f64` dense 254.980us [251.673, 259.168] vs Vec<Literal> 2125.698us [2081.499, 2173.942] = 8.34x mid / 8.03x conservative; `eval/reduce_sum_256_axis0_f64` dense 183.989us vs 1923.922us = 10.46x mid / 10.15x conservative. Score 10.0; golden sha256 d6d1f6bf3821506523d84efc4c467ace5ff047c8146a3b52d53583da27389ac1. Bit-exact parity test (rank-3, 4 reductions, 8 axis subsets, NaN/inf/signed-zero); 30 reduction tests pass; check + clippy -D warnings clean. Next: dense F64 axis reductions for the complex/integral branches use the same odometer; or producer-side typed transpose/gather to keep tensors dense across op chains.
- Pass 68 artifact: artifacts/performance/evidence/fj_backend_cpu_i64_add_dag_pass68_2026-06-03.json
- Pass 68 result: `frankenjax-wu1qx` kept a terminal scalar I64 Add DAG dense-slot evaluator in `fj-backend-cpu`. RCH profile baseline on `ts2`: `backend_scheduler_shapes/branched_fanin/16x8` 39.108 us [38.994, 39.239], `32x8` 75.979 us [75.735, 76.311], `64x4` 84.953 us [84.697, 85.285], and `128x2` 105.48 us [105.38, 105.58]. Final RCH rebench on `vmi1152480`: `16x8` 4.8633 us [4.5064, 5.3106], `32x8` 10.076 us [9.3878, 10.583], `64x4` 12.108 us [10.802, 14.596], and `128x2` 15.297 us [14.486, 15.877]. Speedups are 8.041x, 7.541x, 7.016x, and 6.896x midpoint; conservative interval floor is 5.803x; score 7.0; golden output sha256 ffa44e1fc122fa2c95d20fe782ef579e9a7c46ac81970d52151aaf90480b4c98. Source ordering, ready-wave reconstruction, I64 wrapping, FP exclusion, RNG absence, and fallback/error surfaces are preserved; focused and full `fj-backend-cpu` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 69 artifact: artifacts/performance/evidence/fj_dispatch_scan_i64_shared_pass68_2026-06-03.json
- Pass 69 result: `frankenjax-mcqr.43` kept the exact shared-init row-major I64 Add scan path in `fj-dispatch` (filed as pass68 before a peer Pass68 landed). Same-worker clean-worktree RCH baseline on `ts2`: `vmap_scan/shared_init_batched_xs_128x64` 120.05 us [119.73, 120.43]. Final clean-worktree RCH rebench on `ts2`: 51.733 us [51.473, 52.014]. Speedup is 2.320x midpoint, 2.302x conservative interval; score 12.5; golden output sha256 5b717b7e35d63224f82ec8377a47f07d8c7f40a098f1c45b4e3175723dc81a0c. The direct path only accepts unbatched I64 scalar/scalar-tensor init, I64 rank-2 xs with batch_dim 0, and default/add body_op; all other scan shapes, FP dtypes, nonzero batch axes, and non-add ops fall back. Row order, scan order, reverse iteration, I64 wrapping, FP exclusion, RNG absence, and fallback/error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean. Shared-tree `fj-lax/src/reduction.rs` had unrelated dirty compile errors, so fair baseline/rebench and build gates used clean detached worktrees at `b73e8a24`.
- Pass 66 artifact: artifacts/performance/evidence/fj_lax_axis_reduce_odometer_pass66_2026-06-03.json
- Pass 66 result: `frankenjax-mcqr.30.12` extended the axis-reduction odometer to the integral/complex/float-fallback branches via a shared `OutIndexOdometer`, removing the per-element multi-index decode (flat_to_multi_into + multi_to_out_flat) everywhere in eval_reduce_axes; `dense_f64_axis_reduce` refactored onto it. Same-worker A/B on `ts2` (validated by unchanged calibration bench reduce_sum_64k_f64, 0.8% delta): `eval/reduce_sum_256_axis1_i64` 1271.92us -> 420.09us = 3.03x (2.98x conservative); `axis0_i64` 1275.42us -> 426.95us = 2.99x (2.93x conservative). i64 has no dense storage so this is the decode-removal win alone. Score 8.0; golden sha256 (see .golden.sha256). 30 reduction tests pass; check + clippy -D warnings clean. Next: add dense i64 storage (LiteralBuffer::I64 variant + as_i64_slice) so integral elementwise/reductions get the data-model win too, OR producer-side typed layout to keep tensors dense across op chains.
- Pass 70 artifact: artifacts/performance/evidence/fj_backend_cpu_f64_tensor_add_dag_pass70_2026-06-03.json
- Pass 70 result: `frankenjax-vfogq` kept a terminal F64 tensor Add DAG dense-slot replay path in `fj-backend-cpu`. RCH profile baseline on `ts2`: `backend_scheduler_tensor_shapes/tensor_branched_fanin/32x4x64` 107.631075 us [107.158095, 108.107316]. Final same-worker RCH rebench on `ts2`: 34.835610 us [34.313234, 35.376128]. Speedup is 3.090x midpoint and 3.029x conservative; score 6.0; golden output sha256 7862558dc09366616a169c32348b54ea04c078ab99b5fd2ad995487e7f340289. The direct path only accepts terminal single-output Add-only F64 tensor DAGs with at least 128 equations and at most 128 tensor elements; visible intermediates, nonterminal segments, non-F64 tensors, oversized tensors, shape/length mismatches, params/effects/sub-jaxprs, and sparse VarIds fall back. Source order, tie-breaking metadata, F64 bits, storage class, RNG absence, and error surfaces are preserved; focused and full `fj-backend-cpu` tests pass; crate-scoped check/clippy/fmt/UBS are clean. Next primitive: vectorized/parallel affine-DAG replay for large F64 tensors, since this pass deliberately leaves large tensors on the existing vectorized LAX path.
- Pass 67 artifact: artifacts/performance/evidence/fj_core_dense_i64_storage_pass67_2026-06-03.json
- Pass 67 result: `frankenjax-mcqr.30.13` added a dense i64 storage variant to LiteralBuffer (LiteralBufferStorage::I64 { values: Arc<Vec<i64>>, lazy literals }) mirroring the F64 variant — from_i64_values/as_i64_slice/as_slice/len/make_mut COW/Clone/IntoIterator; TensorValue::new_i64_values; Value::vector_i64 now dense. Wired same-shape i64 Add (eval_same_shape_i64_add) to fold two as_i64_slice() and emit dense i64. Same-worker RCH ts2: `eval/add_64k_i64_vec` dense 24.165us [24.086, 24.254] vs Vec<Literal> 135.404us [134.580, 136.469] = 5.60x (5.55x conservative). Score 10.0; golden sha256 (see .golden.sha256). Bit-exact incl i64::MIN/MAX wrapping; parity tests fj-core (buffer API/COW) + fj-lax (add vs literal); fj-core 129 + fj-lax 1047 lib tests pass; clippy -D warnings clean. Next: wire dense i64 into more consumers — i64 axis/full reductions (as_i64_slice in the odometer + full-reduce), i64 same-shape Sub/Mul, and i64 broadcast — to spread the data-model win across the integral op set.
- Pass 68 artifact: artifacts/performance/evidence/fj_lax_dense_i64_reduce_pass68_2026-06-03.json
- Pass 68 result: `frankenjax-mcqr.30.14` wired pass67's dense i64 storage into the i64 reduction paths — full reduce (eval_reduce integral branch) folds as_i64_slice() directly; axis reduce (eval_reduce_axes integral branch) drives the OutIndexOdometer over the i64 slice. Same-worker RCH ts2: `eval/reduce_sum_64k_i64` (full) dense 12.794us [12.729, 12.872] vs Vec<Literal> 215.093us [214.668, 215.561] = 16.81x (16.68x conservative) — headline. Axis reductions ~2x (reduce_sum_256_axis1_i64 1.98x, axis0 2.00x; modest because pass66's odometer already removed the dominant decode — no-gaps completeness). Score 10.0; golden sha256 (see .golden.sha256). 31 reduction tests pass incl dense_i64_reduce_bit_identical_to_literal_path (Sum/Prod/Max/Min, full + all axis subsets); clippy -D warnings clean. Next: dense i64 same-shape Sub/Mul + i64 scalar broadcast (mirror the F64 broadcast fast path), then revisit whether a dense F32 storage variant is worth it for the float-fallback paths.
- Pass 70 artifact: artifacts/performance/evidence/fj_dispatch_scan_i64_max_shared_pass70_2026-06-03.json
- Pass 70 result: `frankenjax-mcqr.44` kept the exact shared-init row-major I64 Max scan path in `fj-dispatch`. RCH full reprofile on `ts2` found `vmap_scan/shared_init_batched_xs_max_128x64` at 122.20us [121.81,122.59]; refreshed same-worker clean-head baseline after peer dense-i64 storage was 121.98us [121.56,122.42], and final same-worker rebench was 48.040us [47.764,48.349]. Speedup is 2.539x midpoint and 2.513x conservative; score 12.5; golden output sha256 b71bace6079f86322ae7fe0716dd4f972200e4cc3de271626181588bca58f760. The direct path only accepts unbatched I64 scalar/scalar-tensor init, I64 rank-2 xs with batch_dim 0, explicit body_op=max, and valid row-major element count; all non-I64, FP, nonzero-axis, non-max, batched-init, malformed, and functional sub-jaxpr cases fall back. Row order, scan order, reverse iteration, signed max/equal-value behavior, FP exclusion, RNG absence, and error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean. Next dispatch primitive after reprofile: F32 shared-add scan or batched-scalar while, whichever remains top and unclaimed.
- Pass 71 artifact: artifacts/performance/evidence/fj_dispatch_while_i64_add_lt_batch0_pass71_2026-06-03.json
- Pass 71 result: `frankenjax-5c38v` kept the dense-I64 batch-axis-0 Add/Lt while path in `fj-dispatch`. Fresh RCH full profile on `ts2` found `vmap_while/batched_scalar_add_128` at 120.78us [120.43,121.19] after the scan integer passes shifted the bottleneck; same-worker focused baseline was 121.76us [121.36,122.16], and final same-worker rebench was 9.1933us [9.1409,9.2507]. Speedup is 13.246x midpoint and 13.120x conservative; score 12.5; golden output sha256 429d6bdfff70fba0e7c714e2a41cea2a514a2f977e7291240420b8459ad34736. The direct path only accepts absent/add body_op, absent/lt cond_op, dense I64 rank-1 tensors batched on axis 0, and I64 scalar/scalar-tensor unbatched inputs; all non-I64, non-dense, nonzero-axis, mismatched-size, non-add/non-lt, malformed, and functional sub-jaxpr while cases fall back. Lane order, per-lane while iteration order, i64 wrapping_add, `carry < threshold`, max_iter error boundary, FP exclusion, RNG absence, and error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean. Next dispatch primitive after reprofile: likely F32 while, F32 shared-add scan, or eval_jaxpr chain throughput, whichever remains top and unclaimed.
- Pass 72 artifact: artifacts/performance/evidence/fj_dispatch_while_f32_add_lt_batch0_pass72_2026-06-04.json
- Pass 72 result: `frankenjax-mcqr.46` kept the dense-F32 batch-axis-0 Add/Lt while path in `fj-dispatch`. The profile-backed bead baseline on `ts2` measured `vmap_while/batched_scalar_f32_add_128` at 121.38us [120.86,121.95], and final same-worker RCH rebench on `ts2` measured 17.501us [17.131,17.970]. Speedup is 6.936x midpoint and 6.726x conservative; score 12.5; golden output sha256 e5716144c566525c9e66be33218f00a3c41d96e55aaf01f28c7bb0ba0b82a0d4. The direct path only accepts absent/add body_op, absent/lt cond_op, F32 rank-1 tensors batched on axis 0, and F32 scalar/scalar-tensor unbatched inputs; all non-F32, nonzero-axis, mismatched-size, non-add/non-lt, malformed, and functional sub-jaxpr while cases fall back. Lane order, per-lane while iteration order, F32 addition order, NaN comparison exit behavior, max_iter error boundary, RNG absence, and error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 73 artifact: artifacts/performance/evidence/fj_dispatch_scan_f32_shared_add_pass73_2026-06-04.json
- Pass 73 result: `frankenjax-0h5tq` kept the dense-F32 shared-init Add scan path in `fj-dispatch`. Same-worker RCH baseline on `ts2` measured `vmap_scan/shared_init_batched_xs_f32_add_128x64` at 108.34us [108.00,108.65], and final RCH rebench on `ts2` measured 47.511us [47.334,47.702]. Speedup is 2.280x midpoint and 2.264x conservative; score 12.5; golden output sha256 e56c38bad73cc567e327a65327f69486a1afafa89f506f9e81057d3280e1a467. The direct path only accepts unbatched F32 scalar/scalar-tensor init, F32 rank-2 xs with batch_dim 0, default/add body_op, and valid row-major element count; all non-F32, nonzero-axis, batched-init, non-add, malformed, and non-rank2 cases fall back. Row order, scan order, reverse iteration, F32 addition order, NaN propagation, RNG absence, and error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 74 artifact: artifacts/performance/evidence/fj_dispatch_eigh_axis0_move_pass74_2026-06-04.json
- Pass 74 result: `frankenjax-94ig9` kept the Eigh batch-axis-0 move elision in `fj-dispatch`. Full RCH dispatch profile on `vmi1153651` identified `vmap_eigh/batched_matrix_3x3` as the top row at 573.76us [549.81,604.31]. Focused same-worker RCH baseline on `ts2` measured 401.64us [391.37,411.31], and final same-worker rebench on `ts2` measured 371.56us [368.92,373.73]. Speedup is 1.081x midpoint and 1.047x conservative; score 6.0; golden output sha256 7dfe59a6a401b1c2f4c20201c0ae932deacc61832ffadd652ecfad0fba60729f. The path only elides `move_batch_dim_to_front` for leading batch_dim 0; nonleading Eigh batching still moves the batch axis. Row order, Jacobi iteration order, eigenvalue `total_cmp` tie/order behavior, floating-point operations, RNG absence, and fallback/error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 75 artifact: artifacts/performance/evidence/fj_dispatch_eigh_jacobi_row_scratch_pass75_2026-06-04.json
- Pass 75 result: `frankenjax-y5pyk` kept Jacobi Eigh row-scratch reuse in `fj-dispatch`. Same-worker RCH baseline from Pass74 on `ts2` measured `vmap_eigh/batched_matrix_3x3` at 371.56us [368.92,373.73], and final same-worker rebench measured 308.67us [307.93,309.51]. Speedup is 1.204x midpoint and 1.192x conservative; score 15.0; golden output sha256 bcb7a043a581d165097ff63b7dc9caba550b1ed4a724a1f3d208254e31523e21. The change moves only the allocation lifetime of `new_row_p` and `new_row_q`; Jacobi pivot scan order, row update order, eigenvalue `total_cmp` ordering, floating-point expressions, RNG absence, and fallback/error surfaces are preserved. Full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 76 artifact: artifacts/performance/evidence/fj_dispatch_eigh_decomposition_scratch_pass76_2026-06-04.json
- Pass 76 result: `frankenjax-mcqr.49` kept batched Eigh decomposition scratch reuse in `fj-dispatch`; duplicate claim `frankenjax-mcqr.48` was closed as superseded. Same-worker RCH baseline on `ts2` measured `vmap_eigh/batched_matrix_3x3` at 314.34us [312.93,315.86], and final same-worker RCH rebench measured 263.74us [263.02,264.64]. Speedup is 1.192x midpoint and 1.183x conservative; score 15.0; golden output sha256 1f9664d4f4cc680ce95164c2f21b5f9875a28cd5600d985e52a60906d5ffa844. The lever reuses Jacobi/eigen/sort work buffers across batched Eigh slices only; batch slice order, pivot scan order, row update order, eigenvalue `total_cmp` ordering, floating-point expressions, RNG absence, and fallback/error surfaces are preserved. Full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean.
- Pass 77 result: `frankenjax-mcqr.51` rejected dense F64 W/V output buffers for batched Eigh. Fresh RCH profile on `vmi1153651` found `vmap_eigh/batched_matrix_3x3` still top at 342.91us [328.50,356.45], focused baseline on `vmi1153651` was 293.36us [277.92,313.97], and candidate rebench on `ts2` was 274.76us [256.61,290.32], slower than the Pass76 same-worker `ts2` baseline 263.74us [263.02,264.64]. Score 0.0; no source kept. A separate uncommitted 3x3 Eigh specialization appeared in `crates/fj-dispatch/src/batching.rs` during the pass and was left untouched.
- Pass 69 artifact: artifacts/performance/evidence/fj_lax_dense_i64_elementwise_pass69_2026-06-03.json
- Pass 69 result: `frankenjax-mcqr.30.15` extended dense i64 elementwise beyond pass67's Add — generalized the same-shape fast path to any I64xI64 binop (eval_same_shape_i64_binop; int_op carries Sub/Mul/Div/Rem/Max/Min/Pow) and added an i64 scalar-broadcast fast path (eval_i64_scalar_broadcast_binop, both operand orders). Same-worker RCH ts2: `eval/scalar_mul_64k_i64_vec` dense 28.41us vs Vec<Literal> 1258.49us = 44.30x (43.72x conservative) — the generic scalar path runs heavy binary_literal_op per element; `eval/mul_64k_i64_vec` (same-shape) dense 37.23us vs 127.46us = 3.42x (3.31x). Score 10.0; golden sha256 (see .golden.sha256). Parity test dense_i64_sub_mul_and_scalar_broadcast (both orders, wrapping); full fj-lax lib suite 1049 pass; clippy -D warnings clean. Next: dense i64 broadcast for non-same-shape multi-dim (broadcast_binary_tensors) + i64 comparisons (Lt/Eq producing Bool from dense i64), or assess a dense F32 storage variant for the float-fallback paths.
- Pass 70 artifact: artifacts/performance/evidence/fj_lax_dense_i64_compare_pass70_2026-06-03.json
- Pass 70 result: `frankenjax-mcqr.30.16` wired dense i64 storage into eval_comparison — same-shape i64 compare reads two as_i64_slice() directly, and new eval_i64_scalar_compare handles I64 scalar<->tensor in both orders. Same-worker RCH ts2: `eval/lt_scalar_64k_i64_vec` dense 59.71us vs Vec<Literal> 412.12us = 6.90x (6.68x conservative); `eval/lt_64k_i64_vec` same-shape dense 50.06us vs 128.83us = 2.57x (2.52x). Bool output (no dense Bool storage); Score 8.0; golden sha256 (see .golden.sha256). 17 comparison tests pass incl dense_i64_compare_bit_identical_to_literal_path (all 6 ops, both orders, i64::MIN/MAX); clippy -D warnings clean. Next: dense i64 multi-dim broadcast (broadcast_binary_tensors + comparison broadcast), or a dense Bool storage variant (bitset) so comparison OUTPUTS are also dense, or assess dense F32 storage for float-fallback paths.
- Pass 73 artifact: artifacts/performance/evidence/fj_dispatch_scan_f32_shared_pass73_2026-06-04.json
- Pass 73 result: `frankenjax-0h5tq` kept the F32 shared-init Add scan direct path in `fj-dispatch`. Profile-backed RCH baseline on `ts2`: `vmap_scan/shared_init_batched_xs_f32_add_128x64` 108.34us [108.00,108.65]. Final RCH rebench on `ts1`: 47.274us [46.045,48.472]. Speedup is 2.292x midpoint and 2.228x conservative; score 10.0; golden output sha256 b8f601cc772274b45857477c5ff5a5281297046563b13013f4ca603d20566e76. The direct path only accepts unbatched F32 scalar/scalar-tensor init, rank-2 F32 xs batched on axis 0, absent/add body_op, and valid row-major element count; batched init, non-F32, non-axis-0, non-rank-2, non-add, malformed, and mismatched-count cases fall back. Row order, scan order, reverse iteration, F32 addition order, NaN/signed-zero behavior, RNG absence, and error surfaces are preserved; full `fj-dispatch` tests pass; crate-scoped check/clippy/fmt/UBS are clean. Next dispatch primitive after reprofile: F32 scalar-sequence scan variants, F32 while residuals, or eval_jaxpr chain throughput, whichever remains top and unclaimed.
- Pass 71 artifact: artifacts/performance/evidence/fj_lax_dense_broadcast_odometer_pass71_2026-06-03.json
- Pass 71 result: `frankenjax-mcqr.30.17` added a BroadcastOdometer (dual incremental index) and dense gather to multi-dim broadcast — broadcast_binary_f64 gained a dense as_f64_slice tier and new broadcast_binary_i64 folds as_i64_slice (i64 broadcast had NO fast path before). Removes per-element flat_to_multi_into + 2x broadcast_flat_index decode and Literal materialization. Same-worker RCH ts2 (bias [256,256]+[256]): `eval/add_broadcast_256_i64_vec` dense 235.97us vs Vec<Literal> 1863.53us = 7.90x (7.81x conservative); `eval/add_broadcast_256_f64_vec` dense 256.49us vs 1112.61us = 4.34x (4.28x). Score 10.0; golden sha256 (see .golden.sha256). 1052 fj-lax lib tests pass incl dense_broadcast_bit_identical_to_literal_path (5 shapes x Add/Sub/Mul, f64+i64); clippy -D warnings clean. Next: dense i64/f64 multi-dim broadcast COMPARISON (comparison.rs broadcast loop still per-element compare_literals), or a dense Bool storage variant so comparison outputs/bool reductions go dense, or dense F32 storage for float-fallback paths.
- Pass 73 artifact: artifacts/performance/evidence/fj_lax_dense_broadcast_compare_pass73_2026-06-03.json
- Pass 73 result: `frankenjax-mcqr.30.18` reused the pass71 BroadcastOdometer (made pub(crate)) for the comparison.rs multi-dim broadcast branch — dense as_f64_slice/as_i64_slice gather + float_cmp/int_cmp, Bool output (neither f64 nor i64 had a broadcast-compare fast path before). Same-worker RCH ts2 (bias [256,256] cmp [256]): `eval/lt_broadcast_256_f64_vec` dense 317.57us vs Vec<Literal> 2951.18us = 9.29x (9.09x conservative); `eval/lt_broadcast_256_i64_vec` dense 287.10us vs 2528.88us = 8.81x (8.37x). Score 10.0; golden sha256 (see .golden.sha256). 1053 fj-lax lib tests pass incl dense_broadcast_compare_bit_identical_to_literal_path (4 shapes x 6 comparisons, f64 NaN/inf/signed-zero + i64); clippy -D warnings clean. Next: dense Bool storage variant (Vec<u8>/bitset) so comparison OUTPUTS + boolean reductions/masks/select go dense end-to-end, OR dense F32 storage for the float-fallback elementwise/reduce paths.
- Pass 74 artifact: artifacts/performance/evidence/fj_lax_dense_i64_radix_sort_pass74_2026-06-03.json
- Pass 74 result: `frankenjax-mcqr.30.19` — dense i64 ascending sort/argsort via sign-flipped stable LSD radix (sort_along_axis_dense_i64), bypassing the SortKey/Literal machinery. DIAGNOSIS: an in-place radix over the generic (orig_idx, SortKey) pipeline gave NO win (10.83ms vs 10.50ms) — the per-element Literal machinery dominates, not the comparison count (no-ceiling "wrong thing" signal). Pivoted to gather from as_i64_slice -> raw-u64 radix -> dense i64 output. Same-worker-CALIBRATED RCH (baseline ts1 comparison sort vs after vmi1149989 dense radix, full-i64-reduce calibration normalizes 16.3% worker delta): `eval/sort_64k_i64` 1.38ms vs 10.50ms = 8.87x mid / 7.54x conservative. Score 10.0; golden sha256 (see .golden.sha256). 1054 fj-lax lib tests pass incl radix_sort_i64_ascending_matches_comparison_sort (n=1000 negatives/dups/i64::MIN/MAX sort+argsort + [4,300] multi-slice); clippy -D warnings clean. Next: dense f64 sort via radix-on-sortable-bits (flip sign/exponent for IEEE order; careful NaN/-0 parity vs total_cmp), or dense Bool storage variant for select/where/bool-reductions.
- Pass 75 artifact: artifacts/performance/evidence/fj_lax_dense_f64_radix_sort_pass75_2026-06-03.json
- Pass 75 result: `frankenjax-mcqr.30.20` — dense f64 ascending sort/argsort via total_cmp-bit stable LSD radix (sort_along_axis_dense_f64 + f64_total_order_key), bypassing the SortKey/Literal machinery. The bit transform reproduces f64::total_cmp (the generic comparator) incl -NaN<-inf<...<-0<+0<...<+inf<+NaN and NaN payloads. Shared radix_pairs_ascending + for_each_sort_slice (pass74 i64 path refactored onto them). Same-worker-CALIBRATED RCH (baseline comparison sort + full-i64-reduce calib normalizing the cross-worker delta): `eval/sort_64k_f64` 12.94ms -> ~3.09ms normalized = 4.19x mid / 3.92x conservative. Score 8.0; golden sha256 (see .golden.sha256). 13 fj-lax sort tests pass incl radix_sort_f64_ascending_matches_comparison_sort (dense vs Literal by bits, NaN/+-0/+-inf, sort+argsort); clippy -D warnings clean. Next: TopK via partial radix select (O(n) vs full sort), or dense Bool storage for select/where/bool-reductions, or extend radix to u32/u64/i32 dtypes.
- Pass 78 artifact: artifacts/performance/evidence/fj_lax_dense_topk_radix_pass78_2026-06-03.json
- Pass 78 result: `frankenjax-mcqr.30.21` — dense i64/f64 TopK via complement-key stable LSD radix (eval_top_k_dense), reusing the pass74/75 radix machinery to bypass the SortKey/Literal 3-tuple comparison sort. Stable ascending radix on `!total_order_key` = descending value with ascending-index ties (exactly compare_sort_keys(b,a).then(a_idx.cmp(b_idx))). Same-worker RCH vmi1153651 (64k axis, k=128, dense vs Vec<Literal> same process): `eval/topk_64k_k128_f64_vec` dense 2.36ms vs 16.69ms = 7.06x (6.78x conservative); `topk_64k_k128_i64` dense 2.63ms vs 14.74ms = 5.61x (5.40x). Score 10.0; golden sha256 (see .golden.sha256). 1056 fj-lax lib tests pass incl radix_top_k_dense_matches_generic (i64 ties + f64 NaN/+-inf/+-0, multi-slice, by bits); clippy -D warnings clean. Next: true partial radix-SELECT for TopK (narrow to top-k buckets, skip full sort) when k<<n; or extend radix to u32/u64/i32 once those gain dense storage; or dense Bool storage for select/where/bool-reductions.
- Pass 79 artifact: artifacts/performance/evidence/fj_lax_dense_i64_select_pass79_2026-06-03.json
- Pass 79 result: `frankenjax-mcqr.30.22` — dense i64 same-shape Select fast path (select_i64_same_shape_fast_path) reading both branches from as_i64_slice -> dense i64, bypassing the generic per-element select_literal_as_dtype machinery (cond stays per-element bool; no dense Bool storage). Same-worker RCH ts2 (64k, dense vs Vec<Literal> same process): `eval/select_64k_i64_vec` dense 115.18us vs 2666.76us = 23.15x (22.97x conservative). Score 10.0; golden sha256 (see .golden.sha256). 1057 fj-lax lib tests pass incl select_i64_dense_fast_path_matches_generic (i64::MIN/MAX, both cond flags); clippy -D warnings clean. Next: dense Bool storage variant (so cond + comparison outputs + bool reductions/All/Any go dense, compounding select + the pass70/73 compare work), or upgrade the f64 select fast path to as_f64_slice, or partial radix-SELECT for TopK.
- Pass 80 artifact: artifacts/performance/evidence/fj_core_dense_bool_storage_pass80_2026-06-04.json
- Pass 80 result: `frankenjax-mcqr.30.24` — dense Bool tensor storage (LiteralBufferStorage::Bool, 1 byte/elem vs 24-byte Literal) mirroring F64/I64: from_bool_values/as_bool_slice/new_bool_values, vector_bool dense; wired the bool full-reduction (eval_reduce_bitwise_axes DType::Bool reduce_all) to fold as_bool_slice. Same-worker RCH vmi1167313: `eval/reduce_and_64k_bool_vec` dense 1.276us vs Vec<Literal> 45.675us = 35.80x (34.35x conservative). Score 10.0; golden sha256 (see .golden.sha256). fj-lax 1058 + fj-core 130 lib tests pass incl dense_bool_reduce_bit_identical_to_literal_path (ReduceAnd+ReduceOr, all-true/false/mixed); clippy -D warnings clean (ran --release to dodge shared-target proc-macro rmeta drift in debug). Next: wire dense bool into more consumers — comparison fast-path OUTPUTS emit dense bool (pass70/73 produce Vec<Literal::Bool>); select reads cond via as_bool_slice; bool axis-reduction; logical And/Or/Not; this compounds the whole boolean-mask pipeline.
- Pass 81 artifact: artifacts/performance/evidence/fj_lax_dense_bool_axis_reduce_pass81_2026-06-04.json
- Pass 81 result: `frankenjax-mcqr.30.25` — dense bool AXIS reduction via the shared OutIndexOdometer (the pass66 decode-removal was never applied to the bool axis branch) + as_bool_slice dense fold (pass80 storage). Same-worker-CALIBRATED RCH (committed per-element-decode baseline vs after; full-bool-reduce calib normalizes): `eval/reduce_and_256_axis1_bool_vec` 1062.73us -> 224.99us (calib-normalized) = 2.75x mid / 2.69x conservative. Score 8.0; golden sha256 (see .golden.sha256). 33 reduction tests pass incl dense_bool_axis_reduce parity (And/Or x all axis subsets); clippy -D warnings clean (release). REJECTED this session (not shipped): dense-bool comparison OUTPUT (~0.92x neutral — comparison bottleneck is per-element int_cmp/i128, not output write); the data-model-only delta on bool axis was 1.23x (the real win is the odometer decode-removal). Next: the comparison per-element int_cmp/i128 widening is the next bottleneck (specialize i64 compare to avoid i128); or dense bool select-cond via as_bool_slice; or help land the in-flight GEMM col8 register-blocking (another agent's tensor_contraction WIP).
- Pass 82 artifact: artifacts/performance/evidence/fj_lax_matmul_2d_4x4_pass82_2026-06-04.rejected.json
- Pass 82 result: `frankenjax-p1vbf.10` rejected the 4x4 local-accumulator GEMM tile. RCH proof tests passed in candidate state on `ts2`, including adversarial signed-zero/infinity/NaN bit-pattern coverage, and the source was restored to an empty diff. Same-worker RCH `vmi1153651` profile row was `linalg/matmul_2d_512x512x512_f64` 60.229 ms [54.278, 66.963]; candidate rebench was 87.437 ms [80.274, 95.254] = 0.68883x, score 0.0. Golden sha256 6608eab535b1f024b438639b8984bd7c3d35e516f773c41f454a5c60a362f24c. Next primitive: packed-B-panel or recursive/cache-oblivious GEMM with an explicit per-cell ascending-k proof, not another local accumulator micro-lever.
- Pass 83 artifact: artifacts/performance/evidence/fj_lax_matmul_2d_packed_b_panel_pass83_2026-06-04.rejected.json
- Pass 83 result: `frankenjax-p1vbf.11` rejected the packed 16-column B-panel GEMM traversal. RCH proof tests passed in candidate state on `ts2`, including adversarial signed-zero/infinity/NaN bit-pattern coverage over full panel plus tail columns, and the source was restored to an empty diff. Same-worker RCH `ts2` baseline was `linalg/matmul_2d_512x512x512_f64` 33.277 ms [33.178, 33.432]; candidate rebench was 59.272 ms [58.944, 59.495] = 0.56143x, score 0.0. Golden sha256 8024918c0fc067e75296a70100d7b830ed274b740eac97216aa978d4dbd41657. Next primitive: recursive/cache-oblivious GEMM traversal preserving per-cell ascending-k accumulation; defer FP-reordering algorithms until a separate behavior-contract bead proves they are allowed.
- Pass 84 artifact: artifacts/performance/evidence/fj_lax_matmul_2d_column_panel_pass84_2026-06-04.rejected.json
- Pass 84 result: `frankenjax-p1vbf.12` rejected the direct 64-column panel GEMM traversal. RCH proof tests passed in candidate state on `ts2`, including adversarial signed-zero/infinity/NaN bit-pattern coverage over full panel plus tail columns, and the source was restored to an empty diff. Same-worker RCH `ts2` baseline was `linalg/matmul_2d_512x512x512_f64` 33.277 ms [33.178, 33.432]; candidate rebench was 43.166 ms [42.717, 44.027] = 0.77091x, score 0.0. Golden sha256 270738612be03828bd21f2d048d99e7d5ebe1e6b9973dce03d341c7886cfe4e1. Next target: reprofile and pivot to `linalg/eig_48x48_f64` or another non-GEMM top hotspot with a different algorithmic primitive.
- Pass 82 artifact: artifacts/performance/evidence/fj_lax_dense_cumulative_pass82_2026-06-04.json
- Pass 82 result: `frankenjax-mcqr.30.26` — dense F64/I64 cumulative scan (eval_cumulative_dense) scanning the contiguous as_f64_slice/as_i64_slice per line, bypassing the Vec<Literal> materialize (to_vec) + per-element Literal scan. A genuinely different primitive (prefix scan) from the prior dense reductions/elementwise. Same-worker RCH ts2: `eval/cumsum_64k_f64_vec` dense 91.56us vs Vec<Literal> 1061.25us = 11.59x (11.45x conservative). Score 10.0; golden sha256 (see .golden.sha256). 34 reduction tests pass incl dense_cumulative_bit_identical_to_literal_path (Cumsum/Cumprod/Cummax/Cummin x fwd/rev x axis 0/1, multi-line, f64 by bits + i64); clippy -D warnings clean (release). Next: i64 comparison int_cmp/i128 specialization (diagnosed pass81 bottleneck); dense gather/scatter index paths; or revisit linalg (GEMM register blocking — col8 was rejected, needs a different blocking that stays bit-exact).
- Pass 83 artifact: artifacts/performance/evidence/fj_lax_dense_gather_pass83_2026-06-04.json
- Pass 83 result: `frankenjax-mcqr.30.27` — dense F64/I64 contiguous gather fast path (copy contiguous typed slices from as_f64_slice/as_i64_slice into a dense output, bypassing the Vec<Literal> materialize + copy). Same-worker RCH ts2: `eval/gather_256x256_f64_vec` dense 20.86us vs Vec<Literal> 56.02us = 2.69x (2.65x conservative); memory-bound (512KB vs 1.5MB + materialization). Score 8.0; golden sha256 (see .golden.sha256). dense_gather_contiguous parity (clip + fill_or_drop OOB, f64 by bits + i64); clippy -D warnings clean (release). Next: dense scatter (write-side index op); strided (non-contiguous) gather dense path; or the diagnosed i64 comparison int_cmp/i128 specialization.
- Pass 84 artifact: artifacts/performance/evidence/fj_lax_dense_scatter_pass84_2026-06-04.json
- Pass 84 result: `frankenjax-mcqr.30.28` — dense F64/I64 scatter fast path (eval_scatter_dense): scatter over a clone of the contiguous as_f64_slice/as_i64_slice operand, reading typed update slices (overwrite via copy_from_slice, add via a+b f64 / wrapping_add i64), bypassing the to_vec materialize + Literal copy/add. Same-worker RCH ts2: `eval/scatter_256x256_f64_vec` dense 34.38us vs Vec<Literal> 105.42us = 3.07x (3.02x conservative). Score 8.0; golden sha256 (see .golden.sha256). dense_scatter_matches_literal_path parity (overwrite+add x fill_or_drop+clip, OOB + repeated indices, f64 bits + i64); clippy -D warnings clean (release). Next: strided (non-contiguous) gather dense path; or the diagnosed i64 comparison int_cmp/i128 specialization. GEMM blocking (col8/4x4/packed-B) all rejected bit-exact — that lever is closed.
- Pass 85 artifact: artifacts/performance/evidence/fj_lax_dense_maxpool_pass85_2026-06-04.json
- Pass 85 result: `frankenjax-mcqr.30.30` — dense rank-2 f64 max/min reduce_window (maxpool/minpool) via eval_reduce_window_rank2_f64_max_min (as_f64_slice + jax_max_f64/jax_min_f64, new_f64_values), routed before the generic per-window Literal path. A different primitive (CNN pooling stencil). Same-worker RCH ts2: `eval/maxpool_256x256_f64_vec` (3x3 SAME) dense 2.60ms vs generic 12.59ms = 4.84x (4.81x conservative). Score 10.0; golden sha256 (see .golden.sha256). dense_f64_reduce_window_max_min parity (max/min x SAME/VALID, NaN/+-inf/-0, dense vs generic by bits); reduce_window 28 tests pass; clippy -D warnings clean (release). Next: dense rank-2 f64 SUM reduce_window still reads Literal (.as_f64() per element) — make it dense too; or dense reduce_window for higher ranks / i64; or re-profile for a new hot family. GEMM blocking (col8/4x4/packed-B/column-panel) all rejected bit-exact — closed.
- Pass 86 artifact: artifacts/performance/evidence/fj_lax_i64_compare_direct_pass86_2026-06-04.rejected.json
- Pass 86 result: `frankenjax-mcqr.30.29` rejected direct Primitive-specific i64 comparisons without i128 widening. Same-worker RCH ts2 baseline `eval/lt_64k_i64_vec` was 48.915us [48.573, 49.314] vs literal ref 128.99us [127.02, 131.60]; candidate rebench on ts2 was 93.286us [92.111, 94.761] vs literal ref 156.65us [155.75, 157.52] = 0.524x, score 0.0. Candidate proof passed `rch exec -- cargo test -p fj-lax --lib comparison` (20 tests, including dense i64/broadcast/scalar parity), but source was restored and `crates/fj-lax/src/comparison.rs` has an empty diff. Golden sha256 0f09da3362040cd05f2925a7deb3124884e8aab25da6dd6b7dce31835e3af2a0. Next: stop repeating comparison micro-levers; reprofile and attack the strided non-contiguous gather dense path or another higher profile row with a structural primitive.
- Pass 86 artifact: artifacts/performance/evidence/fj_lax_dense_sumpool_pass86_2026-06-04.json
- Pass 86 result: `frankenjax-mcqr.30.31` — dense rank-2 f64 SUM reduce_window general path (as_f64_slice + f64 accumulate + new_f64_values), before the Literal loop; 3x3-SAME sub-path untouched. Completes dense pooling (maxpool pass85 + now sumpool/avgpool). Same-worker RCH ts2: `eval/sumpool_256x256_f64_vec` (2x2 stride2 VALID) dense 204.26us vs Literal 559.52us = 2.74x (2.69x conservative). Score 8.0; golden sha256 (see .golden.sha256). dense_f64_reduce_window_sum parity (2x2/3x2 VALID, 2x3 SAME, NaN/+-inf/-0); 30 reduce_window tests pass; clippy -D warnings clean (release). Next: dense reduce_window for i64 / higher ranks, or RE-PROFILE the full bench suite for a genuinely new hot family — the dense vein across fj-lax elementwise/broadcast/compare/reduce/scan/sort/topk/select/gather/scatter/pooling is now broad. GEMM blocking closed bit-exact.
- Pass 87 artifact: artifacts/performance/evidence/fj_lax_dense_conv2d_pass87_2026-06-04.json
- Pass 87 result: `frankenjax-mcqr.30.32` — dense f64 2D convolution fast path (eval_conv_2d reads both operands from as_f64_slice + f64 multiply-accumulate + new_f64_values), bypassing the per-multiply Literal materialize+match in the conv hot loop. Conv is the CNN core op — a genuinely different primitive. Same-worker RCH vmi1156319: `eval/conv2d_32x32x8_3x3x16_f64_vec` (SAME) dense 4.39ms vs Literal 18.01ms = 4.10x (3.93x conservative). Score 10.0; golden sha256 (see .golden.sha256). dense_f64_conv_2d_matches_generic parity (valid/same x stride 1/2, multi-channel, by bits); 22 conv tests pass; clippy -D warnings clean (release). Next: dense f64 conv_1d (same Literal pattern); dense i64 conv; or re-profile bench suite for a new family. GEMM blocking closed bit-exact.
- Pass 87 artifact: artifacts/performance/evidence/fj_lax_matmul_2d_full_b_transpose_pass87_2026-06-04.rejected.json
- Pass 87 result: `frankenjax-p1vbf.14` rejected the full-B transpose GEMM layout. Candidate proof passed 3 focused matmul tests on `ts2`, including adversarial NaN/infinity/signed-zero bit coverage, and `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs` passed. Same-worker RCH `ts2`: baseline `linalg/matmul_2d_512x512x512_f64` 33.898ms [33.580,34.228] -> candidate 152.65ms [152.01,153.38], speedup 0.222x, Score 0.0. Source diff is empty after restore; golden sha256 bf2085e8fa2300e495c6e24c8678941843cc40204f30ce6a8f0c714239a63801. Next: stop repeating bit-exact GEMM traversal variants; reprofile and choose a non-GEMM hotspot, or first file/prove a behavior-contract bead for FP-reordering GEMM algorithms.
- Pass 88 artifact: artifacts/performance/evidence/fj_lax_dense_conv1d_pass88_2026-06-04.json
- Pass 88 result: `frankenjax-mcqr.30.33` — dense f64 1D convolution fast path (eval_conv_1d reads both operands from as_f64_slice + f64 multiply-accumulate + new_f64_values), bypassing the per-multiply Literal materialize+match (same pattern as conv_2d pass87). Same-worker RCH ts2: `eval/conv1d_1024x16_5x32_f64_vec` (SAME) dense 10.71ms vs Literal 31.18ms = 2.91x (2.90x conservative). Score 8.0; golden sha256 (see .golden.sha256). dense_f64_conv_1d_matches_generic parity (valid/same x stride 1/2, multi-channel, by bits); 23 conv tests pass; clippy -D warnings clean (release). NEXT: dense data-model now spans the full fj-lax op surface (elementwise/broadcast/compare/reduce(full+axis,all dtypes+bool)/sort/topk/select/scan/gather/scatter/pooling/conv1d+2d). Time to RE-PROFILE: run the full lax_baseline sweep to rank slowest absolute ops and find a new hot family (likely fj-dispatch transform/vmap or fj-core IR build), OR dense i64/complex conv. GEMM blocking closed bit-exact.
- Pass 89 artifact: artifacts/performance/evidence/fj_lax_literal_maxpool_pack_pass89_2026-06-04.json
- Pass 89 result: `frankenjax-mcqr.30.34` — literal-backed rank-2 F64 max/min reduce_window now packs declared-F64 Literal tensors once via `reduce_window_f64_values` and reuses the typed max/min pooling kernel. Same-worker RCH ts2 baseline: `eval/maxpool_256x256_f64_literal_ref` 12.269ms [12.236,12.313] while dense vec was 2.5781ms [2.5704,2.5923]. Final same-worker RCH rebench: literal_ref 2.6980ms [2.6865,2.7114], dense vec 2.5497ms [2.5418,2.5590]. Speedup is 4.547x midpoint and 4.513x conservative; score 10.0; golden sha256 39f7645f0b7f15d0baa00c72ab3189420463213cb3c6dec26cef825593d9d37e. Dense tensors borrow and literal tensors pack once; malformed declared-F64, non-F64, non-rank-2, and non-max/min cases fall back. Row-major output order, in-window iteration order, SAME/VALID padding skips, `jax_max_f64`/`jax_min_f64` NaN/signed-zero behavior, tie/RNG absence, and fallback/error surfaces are preserved. Focused reduce_window parity test and release clippy passed through RCH on ts2; isolated rustfmt and golden sha256 checks passed; UBS file scan build/fmt/clippy/audit/deny subchecks were clean but existing file-wide panic/warning inventory remains. Next: re-profile full `fj-lax` / dispatch benches for the next shifted hotspot; candidate structural primitives are dense i64/complex conv, strided non-contiguous gather, or a behavior-contract bead for FP-reordering linalg algorithms.
- Pass 89 result (REJECTED, not shipped): dense f64/i64 cache-blocked 2D transpose. RCH ts2 same-process: transpose_256x256_f64 dense 247.09us vs Vec<Literal> 316.36us = 1.28x — below Score>=2.0 (transpose is already cache-blocked, so memory-bandwidth bound; data-model delta alone is only ~1.28x). Reverted. Findings: (1) fj-core IR ops all sub-us (clone_large 3.4us) — not worth densifying; (2) i64 conv emits F64Bits into an I64-declared tensor via conv_float_literal_from_f64's `_ => from_f64` arm — filed bug bead frankenjax-twuhs; dense i64 conv is not a clean drop-in until fixed. The fj-lax dense data-model is complete for high-value f64/i64 ops; the next real swing is a fj-dispatch (transform/vmap/jit) profile, a different crate — NOT more fj-lax dtype variants. Evidence: artifacts/performance/evidence/fj_lax_dense_transpose_pass89_2026-06-04.rejected.json
- Pass 90 (NO-CEILING pivot, not a micro-lever): profiled fj-dispatch (dispatch_baseline). Top costs: vmap_eigh 241us / svd 169us / qr 137us (batched linalg, 512 small matrices); eval_jaxpr_throughput/chain_add/1000 117us (~117ns/eqn); dispatch_latency/jit/scalar_add 4.2us fixed/call. Findings: (a) batched-linalg dispatch is ALREADY scratch-optimized (batch_eigh/qr/svd_multi reuse buffers) — residual is the iterative per-matrix kernel; closed-form/blocked small-matrix solvers are a different complexity class but CHANGE OUTPUT BITS, so they're a JAX-tolerance-parity-revalidation track, NOT isomorphic optimization. (b) eval_jaxpr loop: prototyped a scratch-buffer + direct-eval_primitive rewrite killing the 2 per-equation Vec allocs -> measured ~1.23x (95us vs 117us @1000, consistent 1.24x @100 = real, not noise), BELOW the >=2x bar, REVERTED; remaining cost is eval_primitive dispatch + FxHashMap env, so the real swing is a COMPILED interpreter (flat opcode stream + register-allocated slots). (c) dispatch() pays ~4.2us fixed pipeline overhead per call (BackendRegistry+Box rebuilt per call, verify_transform_composition + simulate_nested_trace_contexts + build_cache_key_ref + unconditional evidence-ledger/effect-trace strings) — much is behavior-contracted. NEXT TARGET (filed bead frankenjax-y0hsx, target >=2x): per-signature compilation cache (JAX jit model) memoizing the traced+transformed executable plan by the already-computed cache_key, bit-exact incl the per-call audit ledger. Also closed bead frankenjax-twuhs as a non-bug (eval_conv's is_numeric guard rejects integer conv up front, so conv_float_literal_from_f64's `_` arm only ever sees F64 — no F64Bits-in-I64-tensor inconsistency is reachable). Evidence: artifacts/performance/evidence/fj_dispatch_profile_pass90_2026-06-04.json
- Pass 91: complete (bead frankenjax-y0hsx, structural/memory-layout swing). Replaced the fj-interpreters FxHashMap<VarId,Value> interpreter environment with a flat Vec<Option<Value>> slot array indexed by VarId.0 (O(1) array lookup, single allocation), plus an allocation-free reused input-scratch buffer and direct single-output eval_primitive dispatch; sparse/huge var-id jaxprs fall back to the original FxHashMap path (identical semantics). eval_jaxpr_throughput/chain_add/1000 117us -> 67.3us (1.74x), /100 11.93us -> 6.80us (1.75x), same-worker (vmap_eigh calibration ~2% delta). Bit-exact: dense_and_hashed_envs_agree parity test + 131 fj-interpreters tests pass; clippy clean. ICE Score 10.0. The residual ~67ns/eqn is eval_primitive dispatch itself; the >=2x follow-on is a COMPILED interpreter (flat opcode/fn-pointer stream + register-allocated slots), filed as frankenjax-7d3dd. Closed y0hsx (its dispatch-compilation-cache premise doesn't hold: dispatch() cost is required-output generation — evidence ledger + SHA256 key — not cacheable recomputation). Evidence: artifacts/performance/evidence/fj_interpreters_dense_env_pass91_2026-06-04.json (+.golden/.sha256)
- Pass 92: complete (structural arena-reuse swing). fj-dispatch batched Householder QR (batch_qr_multi/qr_decompose_matrix) was allocating ~6 buffers per matrix (r working copy, Vec<Vec<f64>> reflector store, tau, Q+R outputs) x batch_size. Introduced QrScratch reused across the whole vmapped batch -> zero per-matrix allocation, identical Householder arithmetic. vmap_qr/batched_matrix_3x2: raw 130.11us -> 57.51us; eigh-calibration-normalized (eigh untouched, used as worker-speed proxy) = 1.46x (in-process qr/eigh 0.556 -> 0.380). Bit-exact: 256+55 fj-dispatch tests pass incl all test_batch_trace_qr_multi_* (assert exact Q/R); clippy clean. ICE Score 7.5. batch_svd_multi has the identical pattern (vmap_svd 169us, #2 cost) -> filed ready follow-on. Evidence: artifacts/performance/evidence/fj_dispatch_qr_scratch_pass92_2026-06-04.json (+.golden/.sha256)
- Pass 93: complete (bead frankenjax-5rvc7; structural arena reuse, completes batched-linalg arena trilogy). Added SvdScratch reused across the vmapped batch in batch_svd_multi/svd_decompose_matrix (ata, Jacobi buffers via jacobi_eigendecomposition_matrix_into, indices, sigma, v_sorted, u, u_out, vt); removed the now-dead jacobi_eigendecomposition_matrix allocating wrapper; derived Default on JacobiScratch. Bit-exact: 256+55 fj-dispatch tests pass incl all test_batch_trace_svd_multi_* (assert exact U/sigma/Vt); clippy clean. vmap_svd/batched_matrix_3x2 worker-corrected 1.29x (raw 170.60->154.26us; svd/eigh 0.711->0.549, eigh as worker calibration). Weaker than QR pass92 (1.46x) — for 3x2 the Jacobi eigensolve + output Literal conversion dominate, so allocation was a smaller fraction. DIMINISHING RETURNS on the arena lever => batched-linalg arena vein DONE (eigh/QR/SVD); next swing is ALGORITHMIC (closed-form 3x3 eigh, register-blocked tiny QR/SVD — parity-revalidation track, not isomorphism), filed. Evidence: artifacts/performance/evidence/fj_dispatch_svd_scratch_pass93_2026-06-04.json (+.golden/.sha256)
- Pass 94: reconnaissance + lr0pf de-risking (no perf lever shipped — honest: no clean bit-exact >=2x remained without gambling parity or editing foreign-WIP linalg.rs). Profiled fj-backend-cpu (top costs = large-tensor compute routing to densified fj-lax kernels + pass45-optimized scheduler; no fresh lever). Verified special-function parity gaps are CLOSED: lgamma already Lanczos, bessel uses exact Cephes chbevl (bessel_oracle 29/29 pass), erf ~1e-12 — MEMORY notes were stale. DE-RISKED lr0pf (closed-form eigh, #1 cost vmap_eigh 241us): e2e ripple CONTAINED (eigh goldens are {pass:true} booleans, not numerical); but batch tests use assert_eigh_matches_slice_oracle so the swap must change BOTH batching.rs append_eigh_decomposition AND fj-lax linalg.rs eval_eigh/jacobi_eigendecomposition, pass the JAX oracle at 1e-10 (ascending order + eigenvector signs), and linalg.rs currently has 122 lines of foreign WIP (blocker). Filed full implementation plan on the bead. Evidence: artifacts/performance/evidence/fj_recon_pass94_2026-06-04.json
- Pass 95: complete (bead frankenjax-fvfc4; ALGORITHMIC complexity-class swing per NO-CEILING addendum). Replaced the O(n^2) direct-DFT fallback for non-power-of-two FFT lengths with Bluestein's chirp-z transform (length-n DFT -> length-m=next_pow2(2n-1) convolution via the existing radix-2 FFT), O(n^2)->O(n log n). bluestein_dft_into in fj-lax/src/fft.rs; fft_1d_into/ifft_1d_into route non-pow2 to it; power-of-two keeps Cooley-Tukey. RCH Criterion same-bench A/B (baseline = stashed O(n^2) dft_1d): eval/fft_1000_complex128 2058.4us->160.45us = 12.8x; eval/fft_1009_prime_complex128 1606.9us->160.79us = 10.0x. Parity (tolerance, not bit-exact — algorithm swap): Bluestein vs direct DFT at 1e-9 across n in {3,5,6,7,9,10,11,12,15,17,100,257,1000} fwd/inv/roundtrip (new bluestein_matches_direct_dft_across_sizes test) + fj-conformance linalg_fft_oracle_parity (real jnp.fft) passes. 30 fft tests + clippy clean. dft_1d/idft_1d/precompute_twiddles -> #[cfg(test)] references; removed dead radix2_fft_1d. 100% safe Rust, no C/FFTW/MKL. Evidence: artifacts/performance/evidence/fj_lax_fft_bluestein_pass95_2026-06-04.json (+.golden/.sha256)
- Pass 96: complete (bead frankenjax-2alxl; structural FFTW-style plan reuse, bit-exact). For batched non-power-of-two FFT along the last axis of [B,n], the Bluestein chirp table + convolution-kernel FFT are identical across all B rows but were rebuilt per row (+~3n sin_cos +6 allocs/row). Introduced BluesteinPlan (chirp table + fb, built once) + reusable BluesteinScratch; eval_fft/eval_ifft build the plan once and call plan.apply_into per row. bluestein_dft_into builds a one-shot plan (single calls now compute the chirp table once vs 3 sin_cos loops). BIT-IDENTICAL to pass95 per-row Bluestein (same arithmetic, hoisted) — 30 fft tests + linalg_fft_oracle_parity (real jnp.fft) pass; clippy clean. Same-bench A/B normalized by eval/fft_256 (pow2, unaffected; workers within 4.6%): eval/fft_batch_128x1000_complex128 25.358ms->11.180ms = 2.17x worker-corrected; single eval/fft_1000 221.28us->140.12us = 1.51x. Score 10.0. Next FFT candidate: rfft does a full-complex FFT instead of the half-size real-packing trick (~2x). Evidence: artifacts/performance/evidence/fj_lax_fft_bluestein_plan_pass96_2026-06-04.json (+.golden/.sha256)
- Pass 97: complete (bead frankenjax-i516k; bit-exact FFT-family plan-reuse completion). Extended pass96's shared BluesteinPlan to eval_rfft + eval_irfft (build plan once per non-pow2 fft_length, reuse BluesteinScratch across rows). Bit-identical to committed per-row Bluestein; 30 fft tests + linalg_fft_oracle_parity (real jnp.fft, incl rfft/irfft) pass; clippy clean. Same-bench A/B normalized by eval/fft_1000_complex128 (eval_fft path, unaffected; workers within 6%): eval/rfft_batch_64x1000_f64 8.710ms->6.288ms = 1.31x worker-corrected. Score 7.5 (lower than pass96 fft 2.17x: rfft per-row is dominated by the 2 inner FFTs + Literal I/O, so the eliminated plan-rebuild is a smaller fraction). REJECTED sub-lever this session: rfft half-size real-packing (pack N reals into N/2 complex + unpack) — measured REGRESSION (~1.18x slower @256, ~1.05x @4096; unpack+buffers offset the half-size FFT saving); reverted, not shipped. The FFT vein's big algorithmic wins are realized (pass95 12.8x non-pow2, pass96 2.17x batched fft, pass97 1.31x rfft); next big swing should be a DIFFERENT primitive. Evidence: artifacts/performance/evidence/fj_lax_rfft_plan_pass97_2026-06-04.json (+.golden/.sha256)
- Pass 98: complete (bead frankenjax-0g38r; ALGORITHMIC complexity-class win, bit-exact). F32 sort/argsort had no dense storage so fell to the generic O(n log n) comparison sort. Added sort_along_axis_f32_radix: keys each element by f64_total_order_key(literal.as_f64()) and LSD-radix sorts (O(n)); routed for F32 ascending axis>=256, descending/short fall through. BIT-IDENTICAL permutation to the generic path (both stable slice::sort_by/radix; f64_total_order_key reproduces f64::total_cmp on the as_f64 promotion that SortKey::Float uses). Validation: radix_sort_f32_ascending_matches_total_cmp_reference (NaN/inf/-0/payload/dups by bits vs independent total_cmp reference) + 14 sort tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64 (ReduceSum, unaffected; 7.6% worker gap): eval/sort_64k_f32 7.5544ms->2.3540ms = 2.98x worker-corrected (3.21x raw). Score 10.0. Filed follow-on (frankenjax): BF16/F16/U32/U64/I32 radix (same pattern). Evidence: artifacts/performance/evidence/fj_lax_sort_f32_radix_pass98_2026-06-04.json (+.golden/.sha256)
- Pass 99: complete (bead frankenjax-h7joc; generalize pass98 radix, bit-exact). Replaced sort_along_axis_f32_radix with sort_along_axis_literal_radix: per-dtype-family ascending-u64 key — Float(F32/F16/BF16)=f64_total_order_key(as_f64), Unsigned(U32/U64)=as_u64, Signed(I32)=(as_i64 as u64)^(1<<63) — reproducing the generic compare_sort_keys order in O(n) LSD radix. F64/I64 keep their dense paths (excluded); Complex/Bool stay generic. BIT-IDENTICAL to generic (both stable): radix_sort_f32_* + new radix_sort_u32_ascending_matches_unsigned_reference + 15 sort tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64: eval/sort_64k_u32 17.749ms->10.792ms = 1.72x worker-corrected (noisy worker, wide but non-overlapping CIs); f32 path preserved at pass98's 2.98x. Score 10.0. Completes radix dtype coverage (F64/I64 dense + F32/F16/BF16/U32/U64/I32 Literal). Evidence: artifacts/performance/evidence/fj_lax_sort_radix_dtypes_pass99_2026-06-04.json (+.golden/.sha256)
- Pass 100: complete (bead frankenjax-iw8x2; ALGORITHMIC complexity-class win, bit-exact). eval_top_k_dense covered only i64/f64 dense storage; F32/F16/BF16/U32/U64/I32 (Literal-backed) fell to the generic O(n log n) full comparison-sort top-k. Added a Literal-backed complement-key radix branch mirroring pass99: float=!f64_total_order_key(as_f64), unsigned=!as_u64, signed=!((as_i64 as u64)^(1<<63)); order_top_k_pairs uses O(n) partial selection (select_nth_unstable) for small k and stable LSD radix for large k. BIT-IDENTICAL to generic top_k (complement key -> descending value, in-slice index -> ascending tie order == compare_sort_keys(b,a).then(idx)): top_k_f32_radix_matches_reference (k=1/32/257, NaN/inf/-0/payload/dups by bits) + 6 topk tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64 (ReduceSum, unaffected): eval/topk_64k_k128_f32 9.7631ms->702.43us = 16.4x worker-corrected (13.9x raw) — bigger than f32 sort because top_k uses partial selection (O(n) for k<<n) vs the generic full sort. Score 12.5. Completes radix coverage of the sort family (sort + topk) for all single-key numeric dtypes; Complex stays generic (2-key). NOTE: accidentally closed lr0pf in error mid-pass; reopened it (still blocked by foreign linalg.rs WIP). Evidence: artifacts/performance/evidence/fj_lax_topk_literal_radix_pass100_2026-06-04.json (+.golden/.sha256)
- Pass 101: complete (bead frankenjax-mxskp; dense data-model fast path, different primitive from sort). argmax/argmin (extremum_along_axis) scanned each element via sort_key(literal)+compare_sort_keys (SortKey enum build + match per element). Added dense f64 (as_f64_slice, v.total_cmp(&best)) and i64 (as_i64_slice, v cmp best) scans with the SAME strict-greater(max)/strict-less(min) update (first occurrence wins ties), best init at in-slice index 0. BIT-IDENTICAL indices to generic (same total_cmp/cmp, NaN/+-0/+-inf ordering, tie behavior): argmax_argmin_dense_matches_generic (f64+i64, NaN/inf/-0/dup-extremum, axis 1) + 6 argm tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64 (ReduceSum, unaffected): eval/argmax_64k_f64 1025.8us->156.79us = 4.14x worker-corrected (6.54x raw). Score 10.0. Dense vein now covers reductions + cumulative + argmax/argmin. Evidence: artifacts/performance/evidence/fj_lax_argmax_dense_pass101_2026-06-04.json (+.golden/.sha256)
- Pass 102: complete (bead frankenjax-ucx3d; ALGORITHMIC complexity-class win, bit-exact). Descending sort/argsort was gated out of all radix paths (always generic O(n log n) comparison sort, even f64/i64). Added a complement-key trick: descending == ascending radix of (key ^ u64::MAX). A key_mask (u64::MAX when descending, else 0) XORed into the radix key in sort_along_axis_dense_i64/_f64/_literal_radix; routing now sends descending through them. Stable radix of !key keeps equal keys in ascending index order == the generic STABLE descending comparator. Covers i64/f64 dense + f32/f16/bf16/u32/u64/i32 literal. BIT-IDENTICAL to generic stable descending: radix_sort_descending_matches_generic (f64/i64 dense-vs-Literal by bits + f32 vs independent descending-total_cmp reference) + pre-existing sort_1d_descending now route through radix and pass; 16 sort tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64 (workers measured identical, 0.2%): eval/sort_64k_f64_descending 10.009ms->2.810ms = 3.56x. Score 10.0. COMPLETES radix coverage of sort/argsort (ascending pass98/99 + descending pass102, all single-key numeric dtypes; Complex stays generic). Evidence: artifacts/performance/evidence/fj_lax_sort_descending_radix_pass102_2026-06-04.json (+.golden/.sha256)
- Pass 103: complete (bead frankenjax-9d33s; dense data-model w/ DENSE OUTPUT). ConvertElementType built a full-size Vec<Literal> via per-element convert_literal (closures + double match). First tried an input-only dense read (reconstruct + convert_literal) -> ~neutral (1.09x): the output Vec<Literal> build dominates, not input materialization. Fixed: dense source (as_f64_slice/as_i64_slice) x dense-output target (F64/I64/Bool exact dtype) -> tight typed loop into new_f64_values/new_i64_values/new_bool_values (F64->{F64,I64,Bool}, I64->{F64,I64,Bool}); other targets / non-dense fall through to generic. BIT-EXACT vs convert_literal (F64->I64 = v as i64, F64->Bool = v!=0.0, I64->F64 = v as f64, etc.): convert_element_type_dense_matches_generic_all_targets (11 targets x f64/i64, NaN/inf/-0/extremes by bits) + 6 convert tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64: eval/convert_64k_f64_to_i64 996.07us->70.991us = 14.8x worker-corrected (14.0x raw). Score 10.0. Dense vein now covers reductions/cumulative/argmax/elementwise/convert. Evidence: artifacts/performance/evidence/fj_lax_convert_dense_pass103_2026-06-04.json (+.golden/.sha256)
- Pass 104: complete (bead frankenjax-4t5ou; dense data-model w/ DENSE OUTPUT, ubiquitous primitive). BroadcastInDim replicated input into a full-size Vec<Literal> (per output elem: odometer in_flat + push Literal). Factored the row-major replication into broadcast_replicate<T: Copy> (shared by dense + generic), and route dense source (as_f64_slice/as_i64_slice/as_bool_slice) -> new_*_values dense output (F64->F64, I64->I64, Bool->Bool; other dtypes generic). BIT-EXACT: identical odometer/in_flat mapping for dense and Literal element types; broadcast_in_dim_dense_matches_literal_path (f64/i64, [3]->[4,3,5] by bits) + 10 broadcast_in_dim tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64: eval/broadcast_256_to_256x256_f64 596.10us->313.17us = 2.79x worker-corrected (1.90x raw). Score 10.0 (lower than Convert 14.8x: the per-element odometer remains the dominant cost; the win is removing input materialize + Vec<Literal> output). Dense vein now covers reductions/cumulative/argmax/elementwise/convert/broadcast. Evidence: artifacts/performance/evidence/fj_lax_broadcast_dense_pass104_2026-06-04.json (+.golden/.sha256)
- Pass 105: complete (bead frankenjax-0y5yp; dense + bulk row-copy, huge win). Pad built vec![pad_literal; out_total] (per-element Literal clone) + per-element odometer placement + Vec<Literal> output. Added dense F64/I64 path: typed dense fill, then for no-interior/no-crop (all low>=0, low+in<=out) bulk copy_from_slice of contiguous input rows (pad_copy_rows, O(rows) memcpy vs O(in_total) per-element), else per-element placement (pad_fill_place); dense output new_*_values. First tried a simple input-only dense variant (kept per-element placement) -> ~1.40x (placement dominates); the win is the ROW COPY. BIT-EXACT (pad_copy_rows + pad_fill_place mirror the generic offsets): pad_dense_matches_literal_path (f64/i64, crop/interior -> fill_place AND no-crop -> row-copy) + 23 pad tests pass; clippy clean. Same-bench A/B normalized by sort_calib_reduce_64k_i64 (candidate worker 1.6x slower): eval/pad_256x256_to_258x258_f64 635.34us->26.425us = 24.0x raw / ~38.5x worker-corrected. Score 10.0. Dense vein: reductions/cumulative/argmax/elementwise/convert/broadcast/pad. Evidence: artifacts/performance/evidence/fj_lax_pad_dense_pass105_2026-06-04.json (+.golden/.sha256)
- Pass 106: complete (bead frankenjax-y28by; dense + per-element-alloc removal). Rev allocated a fresh vec![0;rank] coords buffer PER OUTPUT ELEMENT + division decode + Vec<Literal> output. Added shared rev_gather<T>: one reused coordinate odometer (no per-element alloc, incremental row-major advance), reflect reversed-axis coords, copy src[src_flat]; dense F64/I64/Bool read the typed slice -> new_*_values, generic path also uses rev_gather over the materialized Literal slice. BIT-EXACT (rev_dense_matches_generic f64/i64 axes 0,2 + 13 rev tests pass; clippy clean). Same-bench A/B normalized by sort_calib_reduce_64k_i64 (workers within ~5%): eval/rev_256x256_f64 1587.1us->331.80us = 5.0x worker-corrected (4.78x raw). Score 10.0. Dense vein: reductions/cumulative/argmax/elementwise/convert/broadcast/pad/rev. Evidence: artifacts/performance/evidence/fj_lax_rev_dense_pass106_2026-06-04.json (+.golden/.sha256)

## pass107 — fj-lax OneHot fill+scatter (SHIPPED)
- target eval/one_hot_2048x512_f64; lever: replace per-output-element O(rank) flat-index decode (+ Vec<Literal>) with fill-off + sparse scatter-on. O(total)+O(indices) vs O(total*rank). Dense F64/I64/Bool -> new_*_values; one_hot_scatter<T> generic helper; Literal fallback for I32/U32/U64/F32/F16/BF16/Complex.
- baseline 11.581ms -> candidate 164.09us = 70.6x raw; calib 8.10/13.02us (cand 1.61x slower worker) -> ~113x worker-corrected. Score 12.5.
- bit-exact: one_hot_fill_scatter_matches_reference (axis=1, OOB indices -1 & 5, f64 + i32 dtype) + 7 one_hot tests pass; clippy -D warnings clean.
- bead frankenjax-rl94y closed. golden sha256 83ef1bd5...

## pass108 — fj-lax BroadcastedIota structural fill+memcpy (SHIPPED)
- target eval/broadcasted_iota_512x512_i64. lever: replace per-element `(flat/stride)%axis_extent` division + Vec<Literal> push with STRUCTURAL construction: run-length fill of `stride` copies per axis value (axis_extent of them), then extend_from_within memcpy block-repeat `outer` times. No per-element division. Dense I64/F64 -> new_*_values; Literal path builds small axis-value table once.
- baseline 2.1768ms -> candidate 45.365us = 48.0x raw; calib 9.496/8.376us -> ~42.3x worker-corrected. Score 10.
- bit-exact: broadcasted_iota_dense_matches_reference (rank-3, dim=1, i64+f64) + 12 iota tests pass; clippy -D warnings clean. bead frankenjax-shyls. golden sha256 f78e84d4...
- NEGATIVE RESULTS recorded this pass (key learning): (1) dense-output-only iota KEEPING the division = only 1.60x (below bar) — dense output alone is useless when per-element arithmetic dominates; the algorithmic removal of the division is what wins. (2) dense-output eval_transpose REGRESSED ~13% (generic already cache-blocked+division-free; criterion amortizes input materialization) -> reverted. DO NOT retry dense transpose.

## pass109 — fj-interpreters scalar I64 Add opcode fast path (SHIPPED)
- target eval_jaxpr_throughput/chain_add; bead frankenjax-7d3dd. lever: scalar I64 literal-right Add-chain opcode fast path before the dense/hash interpreter environments. The guard accepts exactly one scalar I64 input, one output, no constvars/const values/effects/params/sub-jaxprs, Add equations only, Var(previous)+Lit(I64) inputs, and a final outvar equal to the last equation output; all guard misses fall back to the existing interpreter.
- same-worker RCH ts2 baseline -> candidate: chain_add/10 820.74ns -> 44.449ns (18.47x), chain_add/100 7.0512us -> 296.83ns (23.75x), chain_add/1000 69.034us -> 2.9793us (23.17x; 22.67x conservative). Score 10.0.
- isomorphism: arity error ordering unchanged because the fast path runs after existing arity checks; accepted programs have no floating-point, RNG, tie-breaking, or effects; I64 addition uses wrapping_add matching Primitive::Add scalar semantics; output order is a single original outvar. Bit-exact tests compare the fast path to eval_jaxpr_hashed_env at lengths 1/10/1000 plus overflow and guard-miss cases; proptest covers random I64 starts/addends.
- validation: RCH `cargo check -p fj-interpreters --all-targets`, RCH `cargo test -p fj-interpreters`, RCH `cargo clippy -p fj-interpreters --all-targets -- -D warnings`, rustfmt check, UBS exit 0, golden sha256 e396a5e0366b0f7ca8e02036eae1704ec08315d0d41680ead72e1cbe2eb987fc. Evidence: artifacts/performance/evidence/fj_interpreters_scalar_i64_add_opcode_pass109_2026-06-04.json (+.golden/.sha256)

## pass110 — fj-interpreters DCE linear-chain all-used shortcut (REJECTED)
- profile target: fresh RCH `fj-interpreters` profile after pass109 ranked `dce/all_used/1000eq` as the largest disjoint interpreter row at 85.576us on ts2. Focused baseline on ts2: /10 1.0567us, /100 9.5103us, /1000 86.286us.
- rejected lever: guarded linear-chain all-used DCE fast path. It passed 10 focused DCE tests on RCH ts1 and preserved the existing large-chain golden hash, but same-worker ts2 rebench for `/1000eq` was 87.297us midpoint versus the recorded 86.286us baseline. No keepable win; Score 0.0; source removed, `partial_eval.rs` diff empty.
- next pivot: do not retry DCE all-used shortcuts. Attack a deeper structural primitive around partial_eval/staging clone pressure or scan execution. Evidence: artifacts/performance/evidence/fj_interpreters_dce_linear_all_used_pass110_2026-06-04.rejected.json

## pass111 — fj-interpreters scalar-I64 scan add-emit direct evaluator (SHIPPED)
- target eval/scan_sub_jaxpr_add_emit_128; bead frankenjax-17h7c. Profile-backed after pass110 pivot: focused RCH baseline on ts2 was 51.258us midpoint [51.122, 51.418]. Lever: before generic functional Scan iteration, guard the exact scalar-I64 carry + scalar/rank-1 I64 xs body `Add(carry, x) -> carry; Add(carry, 0) -> y` and compute final carry plus dense I64 ys directly.
- RCH rebench: candidate on ts1 381.73ns [376.80, 388.17] = 134.28x midpoint; independent confirmation on vmi1227854 395.89ns [391.38, 400.30]; conservative lower-baseline/upper-candidate speedup 127.71x. Score 10.0.
- isomorphism: accepted path has no floating point, RNG, effects, unrelated params, sub-jaxprs, or tie-breaking; I64 arithmetic uses wrapping_add; output order remains `[final_carry, stacked_ys]`; reverse scan writes ys in input order matching generic push-then-reverse; all nonmatching scan/body/value shapes fall back after existing scan validation. Focused tests compare fast path against a forced-generic body for forward/reverse overflow, scalar xs, explicit guard miss, and literal-backed xs.
- validation: RCH focused scan tests pass (7 tests), RCH `cargo check -p fj-interpreters --all-targets` pass, RCH `cargo test -p fj-interpreters --lib` pass (137 tests before the proof-test additions; focused rerun covers the added tests), RCH `cargo clippy -p fj-interpreters --all-targets -- -D warnings` pass on vmi1227854 and again on ts2 after proof-test additions; rustfmt pass; UBS has no critical issues and reports the existing file-wide warning inventory; golden sha256 8fe4b8e75705b40ff5a3c4329f1a5ceed14a5a70c4e341085c548d11bdea2ef1. Evidence: artifacts/performance/evidence/fj_interpreters_scan_i64_add_emit_pass111_2026-06-04.json (+.golden/.sha256)

## pass109 — closed-form analytic 3x3 symmetric eigensolver (fj-lax + fj-dispatch) [bead lr0pf, stays OPEN]
- lever: replace iterative Jacobi 3x3 eigh kernel with closed-form analytic solver `fj_lax::linalg::analytic_eigh_3x3` — eigenvalues via char-cubic trig (Smith/Kopp), eigenvectors via isolate-best-separated-eigenvalue + 2x2 in-plane solve (robust for degenerate spectra), reconstruction-residual check + Jacobi fallback. SHARED by eval_eigh(m==3) and fj-dispatch append_eigh_decomposition -> batched==single bit-identical.
- parity (NOT sha256-isomorphism; kernel changes bits): conformance linalg_oracle eigh 12/12 (reconstruction + orthonormality + ascending @1e-10..1e-12, incl repeated [2,2,5]); batched slice-oracle ok; rank3 determinism golden updated 1dc99b9->9c8554df. fj-lax eigh 10/10, fj-dispatch eigh 6/6, clippy -D clean.
- perf: vmap_eigh 239.58us -> 102.72us; calib vmap_qr 88.9->67.8us -> 1.78x worker-corrected (2.33x raw). REAL parity-perfect algorithmic win but BELOW 2x bar.
- foreign eval_lu WIP in linalg.rs isolated via git stash for the whole pass, restored after commit (NOT committed).
- lr0pf STAYS OPEN: remaining to 2x = fixed batched-dispatch + per-matrix Literal value-build overhead; register-blocked QR/SVD (vmap_qr 137us, vmap_svd 169us) pending.

## pass112 — fj-dispatch dense I64 scan add-emit outputs (SHIPPED)
- target vmap_scan/functional_sub_jaxpr_add_emit_128x64; bead frankenjax-iv4mg. Lever: inside the existing exact add-emit scan batching specialization, emit final carry and ys via dense `TensorValue::new_i64_values` buffers and read dense xs directly when available; literal-backed xs remains the fallback read path.
- same-worker RCH ts2 baseline -> candidate: 60.798us [60.569, 61.054] -> 45.167us [45.135, 45.199] = 1.346x midpoint, 1.340x conservative. Score 8.0.
- isomorphism: guard and validation surface unchanged; row-major indexing and reverse writeback order unchanged; I64 arithmetic remains `wrapping_add`; accepted path has no floating-point, RNG, ties, or effects; only output representation changes from `Vec<Literal>` to dense I64 storage while dtype, shape, batch_dim, and values stay identical.
- validation: RCH focused scan_sub_jaxprs tests pass (4 tests), RCH `cargo check -p fj-dispatch --all-targets` pass, RCH `cargo test -p fj-dispatch --lib` pass (256 tests), RCH `cargo clippy -p fj-dispatch --all-targets -- -D warnings` pass, rustfmt check pass, `git diff --check` pass, UBS has no critical issues and reports the existing file-wide warning inventory; golden sha256 1cbac033921fbe66cc9153ec9fd30080b50710177c1e815d07d1d271682254c0. Evidence: artifacts/performance/evidence/fj_dispatch_scan_i64_dense_outputs_pass112_2026-06-04.json (+.golden/.sha256)

## pass113 — fj-dispatch unrolled 3x2 thin QR batching (SHIPPED)
- target `vmap_qr/batched_matrix_3x2`; bead frankenjax-lr0pf. Lever: guarded register-blocked 3x2 thin Householder QR helper inside `batch_qr_multi`, replacing per-slice matrix scratch materialization and generic loops for exactly `m=3, n=2, full_matrices=false`.
- same-worker RCH ts2 baseline -> candidate: 87.589us [87.429, 87.807] -> 64.963us [64.928, 64.993] = 1.348x midpoint, 1.345x conservative. Score 8.0.
- isomorphism: accepted path reads the same six row-major elements, uses the same Householder reflector construction/order, threshold checks, dot/update expression order, reverse Q reflector application, diagonal sign normalization, and Q/R extension order as `qr_decompose_matrix(3, 2, ..., false)`. The focused proof test compares Q and R `f64::to_bits()` against the generic helper across positive, mixed-sign, zero-leading, and all-zero matrices. Full matrices, non-3x2 shapes, non-rank-3 inputs, nonnumeric elements, and all unrelated cases fall back unchanged. No RNG or tie-breaking is present.
- validation: RCH focused `qr_` tests pass (7 tests), RCH `cargo check -p fj-dispatch --all-targets` pass, RCH `cargo test -p fj-dispatch --lib` pass (257 tests), RCH `cargo clippy -p fj-dispatch --all-targets -- -D warnings` pass, rustfmt check pass, `git diff --check` pass, golden sha256 check pass, UBS has no critical issues and reports the existing file-wide warning inventory; golden sha256 5c74d824c6e5708478669206fec02a5bf7c12386652638667c9e3cbe78861806. Evidence: artifacts/performance/evidence/fj_dispatch_qr_3x2_unrolled_pass113_2026-06-04.json (+.golden/.sha256)

## pass114 — fj-dispatch unrolled 3x2 thin SVD batching (SHIPPED)
- target `vmap_svd/batched_matrix_3x2`; bead frankenjax-ng9ky. Lever: guarded register-blocked 3x2 thin SVD helper inside `batch_svd_multi`, unrolling the current `A^T A -> 2x2 Jacobi -> descending total_cmp sort -> U/S/Vt` computation for exactly `m=3, n=2, full_matrices=false`.
- same-worker RCH ts2 baseline -> candidate: 103.93us [103.06, 105.66] -> 83.398us [83.302, 83.502] = 1.246x midpoint, 1.234x conservative. Score 6.0.
- isomorphism: accepted path reads the same six row-major elements and preserves the generic A^T A accumulation order, 2x2 Jacobi threshold/angle formulas, eigenvector update order, descending `f64::total_cmp` ordering with equal-value order preserved, sigma max/sqrt logic, U dot-product term order, Vt transpose layout, and U/S/Vt extension order. The focused proof test compares U, S, and Vt `f64::to_bits()` against `svd_decompose_matrix(3, 2, ..., false)` across positive, benchmark-pattern, mixed-sign, and all-zero matrices. Full matrices, non-3x2 shapes, non-rank-3 inputs, nonnumeric elements, and unrelated cases fall back unchanged. No RNG or side effects are present.
- validation: RCH focused `svd_` tests pass (5 tests), RCH `cargo check -p fj-dispatch --all-targets` pass, RCH `cargo test -p fj-dispatch --lib` pass (258 tests), RCH `cargo clippy -p fj-dispatch --all-targets -- -D warnings` pass, rustfmt check pass, `git diff --check` pass, golden sha256 check pass, UBS has no critical issues and reports the existing file-wide warning inventory; golden sha256 f24b65c62d7dbe87112120e2b01c0d097d1637bd8130a57c363e2b6ca4af4690. Evidence: artifacts/performance/evidence/fj_dispatch_svd_3x2_unrolled_pass114_2026-06-04.json (+.golden/.sha256)

## pass110 — fj-lax GEMM micro-arch levers REJECTED + next-axis bead filed
- target eval/dot_256x256x256_f64 (tensor_contraction::matmul_2d, rank-2 f64 dot kernel). Attempted two bit-exact (ascending-l preserved) GEMM levers:
  - NB×KB cache-blocking (128/128): REGRESSED ~0.60x worker-corrected (7.466ms vs 4.785ms baseline; calib 8.40/8.97us). Full-width contiguous inner loop already prefetcher-friendly; B(512KB) served from L2; narrow panels + C re-streaming added cost.
  - 4-row M-axis register-tiling (4x B reuse): ~1.09x (6.176ms; calib swung to 12.6us — within noise). Kernel near compute/L2 bound at 256^3, not B-bandwidth bound.
- BELOW 2x bar -> both reverted; kept scalar i-k-j. Diagnosis: at this size on RCH workers the serial kernel is compute/L2-bound; micro-arch reuse can't reach 2x.
- SHIPPED: revert + new large-size bit-identity regression test matmul_2d_large_bit_identical_to_ijk (m=130,k=300,n=290, crosses cache levels) + rejected evidence fj_lax_gemm_blocking_rejected_pass110.
- NEXT AXIS (bead frankenjax-5vqfx): threaded per-row GEMM (rayon, bit-exact per-row ascending-l, target ~Ncore x) OR explicit std::simd MR×NR microkernel. Threading-in-eval needs dep/owner decision.
- foreign WIP (linalg.rs eval_lu, batching.rs, interpreters scan) untouched; lr0pf IN_PROGRESS under CodexOptimization — not touched.

## pass115 — fj-lax rank-2 F64 Dot dense output (SHIPPED)
- target `eval/dot_256x256x256_f64`; bead frankenjax-5vqfx. Threaded per-row GEMM probe was rejected before commit (ts2 original baseline eval/dot 7.3707ms, matmul_2d 4.5660ms; candidate 7.3507ms / 4.5113ms, below keep threshold). Kept lever: rank-2 F64 Dot wraps the existing `matmul_2d` `Vec<f64>` directly with dense `TensorValue::new_f64_values` and borrows already-dense F64 inputs via `Cow`, instead of converting the output through `Vec<Literal>`.
- same-worker RCH ts1 control baseline -> candidate: `eval/dot_256x256x256_f64` 4.5976ms [4.5330, 4.6547] -> 3.1906ms [3.1770, 3.2060] = 1.441x midpoint, 1.414x conservative. Raw `matmul_2d` control got slightly slower in the candidate run (2.9006ms -> 3.0769ms), so the eval/dot win is not worker-speed noise. Score 8.0.
- isomorphism: accepted path calls the same `matmul_2d` with unchanged row-major inputs and per-cell ascending-k accumulation; malformed literal-backed F64 inputs still fall back to generic dot; dtype/shape/order are unchanged; `LiteralBuffer` equality/indexing/iteration/serde all use the same ordered `as_slice()` Literal view, so dense output is serialization-equivalent. No RNG, sorting, tie-breaking, or transform ordering touched.
- validation: RCH focused dot_rank2 tests pass (3 tests), RCH `cargo check -p fj-lax --all-targets` pass, RCH `cargo test -p fj-lax --lib` pass (1082 tests), RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass, `cargo fmt -p fj-lax --check` pass, `git diff --check` pass, golden sha256 check pass. UBS on `arithmetic.rs` exits 1 on the existing file-wide test panic/unwrap/direct-index inventory; no unsafe blocks and cargo fmt/clippy/check/test subchecks were clean. Evidence: artifacts/performance/evidence/fj_lax_dot_dense_output_pass115_2026-06-04.json (+.golden/.sha256)

## pass111 — fj-lax einsum2 matmul fast path (SHIPPED, bead frankenjax-mkhb7)
- target einsum2 "XY,YZ->XZ" (jnp.einsum 2-D matmul). Generic path allocated a HashMap + decoded coords PER (output,contracted) pair -> O(out*sum) heap-bound. Added try_einsum2_matmul detecting the standard distinct-XYZ 2-D matrix product and routing to the contiguous matmul_2d kernel; ONE lever, falls through to generic for all other patterns.
- BIT-EXACT (both sum contracted index ascending into result[X*dimZ+Z], same layout): committed test einsum2_matmul_fast_path_bit_identical (3 labellings, to_bits) + fj-conformance einsum_oracle 15/15 (JAX parity).
- perf: generic 2426.8ms/call (criterion warmup est, 256^3, HashMap-per-iter) -> 6.236ms fast path = ~389x raw. clips 2x bar by >100x. fj-lax einsum 10/10, clippy -D clean.
- clean-file work: only einsum.rs committed (calls stable pub matmul_2d). Bench measured via a temporary addition to the foreign-dirty lax_baseline.rs, NOT committed (restored). foreign WIP (arithmetic/tensor_ops/linalg/lax_baseline) untouched.

## pass120 — fj-lax MR4xNR4 register-tile GEMM candidate (REJECTED, bead frankenjax-p1vbf.15)
- target `linalg/matmul_2d_256x256x256_f64`; profile-backed residual after pass119 showed blocked threading could not rescue the 256^3 single-thread window. Candidate was a safe-Rust MR4xNR4 scalar-register tile inside `matmul_2d_row_block`, preserving per-cell ascending-k accumulation with scalar row/column tails.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` passed 5 focused bit-identity tests on `ts2` (`matmul_2d_ikj_bit_identical_to_ijk`, threaded parity, large non-power-of-two parity, etc.).
- perf gate failed on same-worker RCH `ts2`: baseline `matmul_2d_256` 4.2498 ms -> candidate 4.7055 ms (0.904x), reachable `eval/dot_256` 4.5551 ms -> 5.1421 ms (0.886x), and `matmul_2d_512` control 6.0199 ms -> 6.2243 ms. Score 0.0.
- decision: rejected and source hunk removed; `git diff -- crates/fj-lax/src/tensor_contraction.rs` empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_register_microkernel_pass120_2026-06-05.rejected.json` (+ `.golden`, `.golden.sha256`). Do not retry this MR4xNR4 scalar-tile shape; next pivot must be a different primitive (packed panels plus explicit safe SIMD if available, or a behavior-contract bead for FP-reordering blocked/Strassen-style GEMM under JAX tolerance).

## pass120 — fj-lax threaded transcendental elementwise (SHIPPED, bead frankenjax-49htz)
- pivot off matmul (single-core at LLVM ceiling per pass110/119) to a DIFFERENT reachable op class: compute-bound transcendental elementwise. Added arithmetic::eval_unary_elementwise_parallel (std::thread::scope, no dep); routed eval_lgamma/eval_digamma/eval_erf_inv to it. Cheap memory-bound unary ops (neg/abs) deliberately NOT routed (threading regresses bandwidth-bound work).
- BIT-EXACT (each element independent): lgamma_parallel_bit_identical (70k, parallel vs serial to_bits) + lgamma value/property tests; clippy -D clean. Gated >=65536 elements (small stays serial).
- perf: lgamma 1M f64 serial 23.331ms -> threaded 6.4515ms = ~3.66x worker-corrected. Same applies to digamma + erf_inv (same routing).
- reachable (Primitive::Lgamma/Digamma/ErfInv); high value in probabilistic ML (Gamma/Beta/Dirichlet log-densities + gradients).
- clean file: only arithmetic.rs committed; temp bench in foreign-dirty lax_baseline.rs restored; serial baseline via in-place threshold=MAX toggle.
- NOTE clippy transient: an rch stale-incremental run falsely flagged "too many arguments (8/7)"; two fresh runs Finished clean.

## pass124 — fj-conformance general Eig behavior contract (SHIPPED, bead frankenjax-p1vbf.18)
- target `linalg/eig_48x48_f64`; contract gate before the shifted/deflated packed-Hessenberg QR primitive. RCH baseline via `cargo bench -p fj-lax --bench lax_baseline -- linalg/eig_48x48_f64` on ts2: 17.600 ms [17.600, 17.781], midpoint 17.692 ms.
- lever: conformance-only Primitive::Eig oracle contract for real diagonal-dominant, nonsymmetric real, repeated, near-repeated, complex-conjugate, and 3x3 diagonal-deflation cases. No production/runtime/linalg implementation changed in this commit.
- isomorphism: output order remains public-unspecified; proof summary canonicalizes only for sha reporting with real-desc, imag-desc, original-index tie. Eigenvalue multisets checked unordered with fixed tolerances; real cases check `Av=lambda*v`; Complex128 dtype/shape fixed; no RNG or production FP arithmetic changed. Golden sha256 `9b84bf2da55576f8de5832828ae6cb271d82a59dc11212513acd95ec29603791`.
- validation: RCH `cargo test -p fj-conformance --test linalg_oracle eig -- --nocapture` on ts2 passed 18/18; RCH `cargo clippy -q -p fj-conformance --test linalg_oracle -- -D warnings` on ts1 passed; focused `rustfmt --check crates/fj-conformance/tests/linalg_oracle.rs` passed. Package-level `cargo fmt -p fj-conformance --check` is blocked by unrelated `gather_scatter_oracle.rs` formatting; UBS exits nonzero on existing test-file panic/assert/direct-index inventory, with embedded cargo subchecks clean.
- performance result: contract-only, Score N/A. Next profile-backed primitive is shifted/deflated QR over packed real Hessenberg layout from communication-avoiding / blocked eigensolver techniques, target >=2x against the 17.692 ms eig_48 baseline.

## pass125 — fj-dispatch stale axis-1 vmap bead closeout (REJECTED, bead frankenjax-z92vn)
- current in-flight cleanup: `frankenjax-z92vn` was still assigned to `CodexOptimization`, so it was checked before switching to the ready GEMM bead.
- fresh RCH baseline: `dispatch_latency/vmap/rank2_add_one_in_axes_1` on `ts2` measured 39.480us [39.039, 39.930].
- decision: closed as a stale duplicate/rejected lane. The exact I64 rank-2 axis-1 add path already has `fuse_rank2_axis1_i64_scalar_add_move_to_front`, and prior pass121 rejected the remaining direct axis-1 add-literal kernel at 40.077us -> 46.116us. No source lever was kept.
- next target: claimed `frankenjax-mcqr.56` for a profile-backed `fj-lax` GEMM primitive. Peer-owned dirty `crates/fj-lax/src/tensor_ops.rs` conv1d work remains untouched.

## pass126 — fj-dispatch axis-1 I64 dense output keep (SHIPPED, bead frankenjax-z92vn)
- target `dispatch_latency/vmap/rank2_add_one_in_axes_1`. Lever: inside the existing exact rank-2 axis-1 I64 scalar-Add fusion, borrow dense I64 input when available and emit dense I64 output via `TensorValue::new_i64_values`, preserving the prior transposed output order and wrapping-add arithmetic.
- same-worker RCH ts2 baseline -> candidate: 42.795us [42.236, 43.410] -> 21.841us [21.657, 22.062] = 1.959x midpoint, 1.914x conservative. Score 16.0.
- isomorphism: output order remains `output[col,row] = source[row,col].wrapping_add(scalar)`; shape `[cols, rows]`, dtype `I64`, fallback/error behavior, tie/RNG/FP exclusions, and scalar guard are unchanged. Golden sha256 29d48dbffb8ba3bc95e29416bbe470cd18432ffd471b8ec7ef068c3b7a61b3ef.
- validation: RCH focused `test_in_axes_1` pass (3 tests), RCH `cargo check -p fj-dispatch --all-targets` pass, RCH clippy `-p fj-dispatch --all-targets --no-deps -- -D warnings` pass, rustfmt check pass, golden sha256 check pass, JSON parse pass, `git diff --check` pass, UBS exit 0. Full `cargo test -p fj-dispatch --lib` is currently blocked by unrelated Eigh eigenvector sign-orientation assertions at `batching.rs:7325`.
- evidence: artifacts/performance/evidence/fj_dispatch_axis1_i64_dense_output_pass126_2026-06-05.json (+ `.golden`, `.golden.sha256`).

## pass127 — fj-lax column-tiled real QR Householder updates (SHIPPED, bead frankenjax-p1vbf.24)
- target `linalg/qr_1024x1024_f64`. Lever: replace the real QR path's per-column stride-n Householder dot/update loops with an 8-column microkernel that computes a tile of independent columns while preserving each column's ascending-row reduction order and the same reflector sequence.
- same-worker RCH ts2 baseline -> candidate: 246.75ms [244.60, 248.86] -> 51.927ms [49.466, 54.434] = 4.752x midpoint, 4.494x conservative. Score 4.51.
- isomorphism: reflector construction, tau threshold, reverse Q application, diagonal sign normalization, shapes, fallback behavior, and every per-column floating-point reduction order are unchanged; only independent columns are interleaved for locality. No RNG, sorting, pivoting, or tie-breaking surface exists. Golden sha256 6119fc5cf4759d8cdcd9c34d89a79de89d205203730814fc06aa52bf57ff262b.
- validation: RCH focused fj-lax `qr_` tests 10/10, fj-conformance `linalg_oracle qr` 8/8, fj-conformance `linalg_higher_rank qr` 2/2, RCH `cargo check -p fj-lax --all-targets` pass, RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass, `git diff --check` pass. `cargo fmt -p fj-lax --check` remains blocked by pre-existing formatting drift outside the QR hunk; UBS exits nonzero on existing file-wide panic/indexing inventories while embedded fmt/clippy/check/test subchecks are clean.
- evidence: artifacts/performance/evidence/fj_lax_qr_tiled_reflector_pass127_2026-06-05.json (+ `.golden`, `.golden.sha256`).

## pass128 — fj-lax Hessenberg/Givens real Eig QR iteration (SHIPPED, bead frankenjax-65m9n)
- target `linalg/eig_48x48_f64`. Lever: replace dense n>=3 Eig QR iteration with safe-Rust Householder Hessenberg reduction plus explicit Givens QR steps on the Hessenberg form, accumulating the orthogonal basis in-place.
- same-worker RCH ts1 baseline -> candidate: 11.765ms [11.581, 11.882] -> 0.67409ms [0.66686, 0.68439] = 17.45x midpoint, 16.92x conservative. Score 12.5.
- behavior proof: 1x1/2x2 paths unchanged; n>=3 uses the already pinned public Eig contract (output order public-unspecified, canonical golden contract sha256 9b84bf2da55576f8de5832828ae6cb271d82a59dc11212513acd95ec29603791). No RNG. Golden output sha256 4f85accc348e891face6a47bb9aba2e9d715c1b32877d03b1d6101581efbd459.
- validation: RCH `hessenberg` test 1/1, RCH `eig_qr_iteration` test 1/1, RCH fj-conformance `linalg_oracle eig` 18/18, RCH `cargo check -p fj-lax --all-targets` pass, RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass, `git diff --check` pass. Direct rustfmt remains blocked by pre-existing linalg.rs drift; UBS reports existing file-wide inventory with embedded subchecks clean.
- evidence: artifacts/performance/evidence/fj_lax_eig_hessenberg_givens_pass128_2026-06-05.json (+ `.golden`, `.golden.sha256`).

## pass129 — fj-lax QR reflector while-loop microkernel (SHIPPED, bead frankenjax-p1vbf.26)
- target `linalg/qr_1024x1024_f64`. Lever: replace iterator-based lane loops inside the existing 8-column `apply_real_householder_columns` tile with explicit `while` loops over the same rows/lanes.
- same-worker RCH ts1 baseline -> candidate: 95.653ms [94.480, 96.780] -> 76.259ms [75.393, 77.219] = 1.254x midpoint, 1.224x conservative; absolute midpoint saved 19.394ms. Score 8.0.
- isomorphism: reflector generation/order, Q reverse-pass order, R extraction, diagonal sign normalization, dtype/shape/fallback behavior, and per-element FP expression order are unchanged (`dot += vi*x`, `dot *= tau`, `x -= vi*dot`). QR has no tie-breaking/RNG surface. Golden QR digest remains 6119fc5cf4759d8cdcd9c34d89a79de89d205203730814fc06aa52bf57ff262b.
- validation: RCH fj-lax `qr_` tests 10/10, RCH fj-conformance `linalg_oracle qr` 8/8, RCH `cargo check -p fj-lax --all-targets` pass, RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass, `git diff --check` pass, golden sha256 OK. Direct rustfmt remains blocked by pre-existing SVD/LU formatting drift; UBS reports existing file-wide inventory with embedded subchecks clean.
- evidence: artifacts/performance/evidence/fj_lax_qr_reflector_while_loops_pass129_2026-06-05.json (+ `.golden`, `.golden.sha256`).

## pass130 — fj-lax QR flat reflector arena (REJECTED, bead frankenjax-p1vbf.27)
- target `linalg/qr_1024x1024_f64`. Lever tested: replace the real QR path's `Vec<Vec<f64>>` Householder reflector storage with one preallocated flat `Vec<f64>` arena plus per-reflector offsets, preserving reflector order, tau threshold behavior, R application order, reverse Q application order, diagonal sign normalization, dtype/shape/error behavior, and QR golden digest.
- same-worker RCH ts1 evidence did not clear the real-win gate. Initial clean HEAD baseline -> candidate: 92.168ms [85.270, 102.74] -> 88.441ms [86.331, 90.557], but confirmation was only 80.066ms [78.894, 81.289] -> 78.696ms [77.184, 80.571]. Longer clean HEAD baseline was 80.959ms [79.896, 82.038]; the matching long candidate run moved to `vmi1149989` and was ignored as cross-worker. Best valid conservative speedup was 0.979x; Score 0.5.
- decision: rejected and no production source hunk retained (`crates/fj-lax/src/linalg.rs` restored to HEAD). Behavior proof was not run after the perf gate failed; golden QR digest remains the pass129 sentinel `6119fc5cf4759d8cdcd9c34d89a79de89d205203730814fc06aa52bf57ff262b`.
- evidence: artifacts/performance/evidence/fj_lax_qr_flat_reflector_arena_pass130_2026-06-05.rejected.json.
- next primitive: move deeper to the `frankenjax-p1vbf` Part B blocked-WY / communication-avoiding QR behavior-contract path under JAX oracle tolerance; further bit-preserving reflector storage micro-levers are not the right attack.

## pass131 — fj-core/fj-interpreters persistent Jaxpr equation storage (SHIPPED, bead frankenjax-lsjey)
- target `partial_eval/all_known/1000eq` and `dce/all_used/1000eq`. Lever: add `EquationList` as shared Arc-backed Jaxpr equation storage, preserving Vec-like serde/API behavior while making unchanged PE/DCE equation-list clones shallow.
- same-worker RCH baseline -> candidate: `partial_eval/all_known/1000eq` 48.689us -> 77.127ns = 631.28x midpoint; `dce/all_used/1000eq` 54.853us -> 5.7590us = 9.52x midpoint. Score 8.33.
- validation: fmt, RCH clippy/check/tests for touched crates, `fj-core` Jaxpr tests, `fj-interpreters` partial_eval tests with golden DCE hash, and PE staging oracle passed. UBS nonzero only for pre-existing inventories. Evidence sha256 `d2a409c1aa35f6234315e149050ead1ceb86be06770ec3d507efd6b08880b928`.
- commit: `058247ea`.

## pass132 — fj-interpreters DCE non-empty retained-count path (SHIPPED, bead frankenjax-kqtzy)
- target `dce/all_used/10eq` and `dce/all_used/100eq` after post-pass131 profile showed DCE as the remaining interpreter hotspot. Lever: route every non-empty DCE graph through the retained-count path so all-retained graphs can shallow-clone `EquationList`; empty graphs keep the old zero-iteration branch.
- same-worker RCH ts1 baseline -> candidate: `dce/all_used/10eq` 733.89ns [721.04,750.38] -> 203.16ns [201.05,205.63] = 3.61x midpoint / 3.51x conservative; `dce/all_used/100eq` 6.4882us [6.3945,6.6165] -> 1.1820us [1.1251,1.2404] = 5.49x midpoint / 5.16x conservative. Score 12.0. `1000eq` was not scored because both baseline and candidate already used the retained-count branch there.
- isomorphism: equation ordering, output mask zip/filter behavior, used-input mask construction, tie/RNG exclusions, and no-FP-arithmetic property preserved. Golden DCE sha256 `3729e2d5cc19c0abec46fb5b188cc7576b9853ee7d0cd523f3656b1ac57e8ad8`.
- validation: `cargo fmt --check -p fj-interpreters`; RCH `cargo check -p fj-interpreters --lib`; RCH `cargo test -p fj-interpreters --lib partial_eval`; RCH `cargo test -p fj-conformance --test pe_staging_oracle`; RCH `cargo clippy -p fj-interpreters --all-targets --no-deps -- -D warnings`; `git diff --cached --check`. UBS nonzero only for pre-existing file-wide inventories; embedded build/lint/test sections clean.
- evidence: `artifacts/performance/evidence/frankenjax_kqtzy_dce_nonempty_retention_pass132_2026-06-05.json` sha256 `c8330a7a3a38cfb30d0b1d821af06ee4ce2a1b43fb4429aeb28fed3f965ed1de`.
- commit: `f69ea18a`.

## pass133 — fj-lax threaded random_normal inverse-erf transform (SHIPPED, bead frankenjax-4ypdx)
- target `random/normal_1m_f64` after post-mt4sb profile showed uniform bit generation at about 13ms but normal generation around 284-286ms; the per-element `sqrt2 * erf_inv_approx` transform dominated. Lever: materialize the same `random_uniform` stream, then split large outputs into scoped-thread contiguous chunks and write the exact same expression at the same index.
- same-worker clean-worktree RCH ts2 baseline -> candidate: 295.91ms [292.82,299.18] -> 36.218ms [35.855,36.578] = 8.17x midpoint / 8.00x conservative. Score 12.5. A local fallback run from `/data/tmp` was ignored because RCH rejected the non-canonical worktree path.
- isomorphism: RNG stream/key handling unchanged; output order/indexing preserved by contiguous chunk ranges; each element still computes `sqrt2 * erf_inv_approx(u)` with no reduction or reassociation; no tie-breaking surface exists. Golden random_normal sha256 `982d444c8f93dd7331fff1e2141e40ee967f6698a7a12cbeef104d38b6c6c29b`.
- validation: RCH fj-lax `random_normal` tests 2/2, RCH fj-lax SIMD RNG parity tests 2/2, RCH fj-conformance `random_determinism rng_fixture` 7/7. Package-level `cargo fmt -p fj-lax --check` remains blocked by pre-existing formatting drift outside `threefry.rs`; shared-checkout clippy was blocked by unrelated in-progress `frankenjax-mj9ip` SIMD-exp work in `arithmetic.rs`.
- evidence: `artifacts/performance/evidence/fj_lax_random_normal_threaded_4ypdx_2026-06-05.json`.
- next primitive: `frankenjax-mj9ip`, SLEEF-grade safe-Rust portable-SIMD exp/tanh/sigmoid with oracle-grade error proof.

## pass134 — fj-lax Cholesky dense F64 output storage (SHIPPED, bead frankenjax-axevw)
- target `linalg/cholesky_128x128_f64`. Lever: real F64 Cholesky now emits the already-computed row-major `Vec<f64>` through dense `TensorValue::new_f64_values` instead of rematerializing every element through `Literal`; non-F64 dtypes keep the existing `matrix_to_value` path.
- saved RCH Criterion baseline -> candidate: 668.297us [664.106, 673.976] -> 265.233us [256.069, 274.664] = 2.52x midpoint / 2.42x conservative. Score 8.0.
- isomorphism: extraction, blocked/scalar Cholesky arithmetic, output order, dtype fallback, shape, errors, FP ordering, and RNG/tie exclusions are preserved. Golden sha256 `eae487a2731a82fcd12899895c4623739bd237d3feebf848c23a90f524598dd6`.
- validation: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` passed 12/12; RCH `cargo check -p fj-lax --all-targets` passed before the test-only Jacobi warning cleanup; RCH `cargo clippy -p fj-lax --all-targets --no-deps -- -D warnings` passed after it. File-only `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` passed; package fmt remains blocked by pre-existing fj-lax drift. UBS on linalg remains nonzero only from existing file-wide inventories.
- evidence: `artifacts/performance/evidence/fj_lax_cholesky_dense_output_pass134_2026-06-05.json` (+ `.golden`, `.golden.sha256`).

## pass135 — fj-lax pinv cyclic Jacobi mirror update (REJECTED, bead frankenjax-sf71s)
- target `linalg/pinv_192x192_f64`. Lever tested: mirror row/column Jacobi updates while forming the Gram pseudoinverse, preserving eigenpair order irrelevance and the existing rcond cutoff.
- same-worker RCH evidence did not clear the real-win gate; commit `aa89dfed` recorded the rejection and no production hunk was retained. The hotspot stayed in the pinv/SVD numerical core rather than in that mirror-update loop.
- next primitive selected: direct one-sided Jacobi SVD for square pinv, filed as `frankenjax-ppad8`.

## pass136 — fj-lax direct one-sided SVD pinv (REJECTED, bead frankenjax-ppad8)
- target `linalg/pinv_192x192_f64`. Lever tested: direct one-sided Jacobi SVD pseudoinverse for finite square matrices, avoiding the Gram A^T A pseudoinverse path.
- same-worker RCH ts1 baseline -> unguarded candidate: 107.88ms [104.92, 110.63] -> 84.349ms [80.792, 89.480], but sidecar proof review found mandatory blockers: no non-convergence fallback, `rcond = NaN` behavior drift, and an incomplete rank-deficient Moore-Penrose proof.
- proof-safe guarded candidate added finite-rcond gating, retained-column orthogonality fallback, direct-vs-Gram tests, NaN-rcond proof, and full rank-deficient Moore-Penrose checks. Focused RCH proof tests passed 3/3, but guarded rebench was only 103.56ms [94.304, 113.56], 1.04x midpoint and 0.92x conservative, so Score 0.5 and the lever was rejected.
- source state: no production/test hunk retained; `git diff -- crates/fj-lax/src/linalg.rs` empty after cleanup; file-only rustfmt check passed. Evidence: `artifacts/performance/evidence/fj_lax_pinv_one_sided_svd_ppad8_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`).
- next primitive: Golub-Kahan bidiagonalization plus QR or divide-and-conquer bidiagonal SVD with an explicit convergence certificate and fallback; target >=2x over the current 107.88ms ts1 pinv baseline.

## pass137 — fj-lax rank-revealing QR low-rank pinv (SHIPPED, bead frankenjax-9gvse)
- target `linalg/pinv_192x192_f64`. Lever: finite `m >= n` inputs now try a pivoted modified-Gram-Schmidt low-rank QR pseudoinverse and return only when the residual Frobenius certificate proves omitted directions are below the existing `rcond` cutoff; otherwise the old Gram path runs.
- same-worker RCH ts1 clean baseline -> candidate: 102.58ms [100.15, 105.49] -> 7.9710ms [7.8345, 8.0863] = 12.87x midpoint, 12.38x conservative. Score 5.88.
- isomorphism: NaN/non-finite/rcond-zero/full-rank behavior falls back; pivot ties keep left-to-right order by swapping only on strictly greater residual norm; deterministic FP accumulation order is documented; no RNG surface is introduced. Moore-Penrose residual max 4.22e-15; golden output sha256 `646497ffdc9a13f758b8db6bb07b729fc3e583275c45d3a418b417f028448fb6`.
- validation: RCH focused `pinv_low_rank_qr` tests 3/3, RCH `cargo check -p fj-lax --all-targets` pass, RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass, RCH `cargo test -p fj-lax` pass, `cargo fmt --check --package fj-lax` pass, golden sha256 OK. UBS on linalg remains nonzero on pre-existing file-wide findings while embedded build/lint/test sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_pinv_low_rank_qr_9gvse_2026-06-06.json` (+ `.golden`, `.golden.sha256`).

## pass138 — fj-lax GEMM blocked macro-kernel gate (SHIPPED, bead frankenjax-cl9xa)
- target `linalg/matmul_2d_1024x1024x1024_f64` regression after KC-blocked packed-B routing, plus `2048^3` large-GEMM keep. Lever: keep the existing B-panel pack gate at `k*n >= 131072`, but only route the KC-blocked macro-kernel when `k*n >= 4194304`; smaller L3-resident GEMM stays on the flat packed path where same-worker evidence is faster.
- same-worker RCH ts1 baseline/pre-gate/gated: `1024^3` flat-pack 25.604ms [19.956,30.009], pre-gate blocked 43.351ms [37.575,50.349], gated 21.039ms [20.197,22.279] = 2.06x midpoint restore vs pre-gate. `2048^3` flat-pack 260.28ms [236.97,288.16], gated 146.62ms [133.74,159.79] = 1.78x midpoint, 1.48x conservative vs flat-pack. Score 8.0.
- isomorphism: gate only selects between two already bit-proven kernels; both preserve row-major output and ascending-k per-element accumulation with no reassociation, FMA substitution, tie-breaking, or RNG surface. The blocked proof test now forces four threads, B packing, three KC blocks, and row/column borders so correctness does not depend on production thresholds. Golden sha256 `8327212ba3c56e980ffa56c89ecdc54ad69eb4caa431758d8775e26a2614b37b`.
- validation: RCH focused fj-lax `matmul_2d` tests 7/7 plus 1 ignored timing test; RCH `cargo check -p fj-lax --all-targets` pass; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` pass after replacing the added bool arguments with `MatmulPlan`/`MatmulRhs`; `cargo fmt --check --package fj-lax` pass after shared-tree formatting churn settled; `git diff --check` pass; UBS on `tensor_contraction.rs` exited 0 with only existing file-wide warning inventory.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_blocked_gate_cl9xa_2026-06-06.json` (+ `.golden`, `.golden.sha256`).
- next primitive: do not keep tweaking KC blocking; attack the live cl9xa microkernel target with wider register tiles (MR=6/NR=8) or explicit safe portable-SIMD f64x4 lanes, preserving per-column accumulator independence and separate multiply/add semantics.

## pass139 — fj-lax GEMM NR8 column tile (REJECTED, bead frankenjax-cl9xa)
- target `linalg/matmul_2d_1024x1024x1024_f64`. Profile basis: cl9xa's clean-worker A/B found KC blocking neutral in the L3-resident 1024^3-2048^3 regime; the named residual was MR=NR=4 microkernel L1 arithmetic intensity.
- lever tested: widen only the GEMM column register tile from NR=4 to NR=8, leaving MR=4, packed-B panel layout, threading, KC blocking, and per-cell ascending-k accumulation unchanged.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on ts1 passed 7 focused tests (1 ignored), including packed, threaded, large, and blocked bit-identity tests.
- perf gate failed on same-worker RCH ts1: NR4 control 22.040ms [20.866, 23.286] vs NR8 candidate 23.963ms [21.521, 28.008] = 0.920x midpoint, 0.745x conservative. Initial cross-worker control was vmi1156319 NR4 76.433ms [74.403, 78.682] and was ignored for the keep decision.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Golden sha256 `0bdef3b6360ba55c7e9f32a396d944ea36a3b03addd9a45a6220e4a20688715d`.
- next primitive: stop scalar-tile micro-tuning and attack a different GEMM primitive: Strassen under the existing Dot tolerance contract, or an explicit safe portable-SIMD microkernel with bit-identity proof (`mul` + `add`, no `mul_add`/FMA), target Score>=2.0 on 1024^3-2048^3.

## pass140 — fj-lax GEMM safe-SIMD full-panel lanes (REJECTED, bead frankenjax-vgd3x)
- target `linalg/matmul_2d_1024x1024x1024_f64` and `linalg/matmul_2d_2048x2048x2048_f64`. Profile basis: `frankenjax-vgd3x` after pass139 NR8 rejection named safe portable-SIMD lanes or Strassen/recursive GEMM as the next primitive.
- lever tested: replace the full NR=4 output-column panel update with safe `std::simd` f64x4 lane accumulators, one lane per output column, while keeping row-major output and ascending-k per-cell accumulation. No tile-width, pack-threshold, threading, or public API change was retained.
- baseline: required pre-change RCH baseline ran first, but RCH ignored the `ts1` preference and selected `vmi1264463`; 1024^3 was 485.73ms [338.75, 668.73], too noisy/cross-worker for keep scoring. Prior same-worker ts1 NR4 control from pass139 remained 22.040ms [20.866, 23.286].
- proof before rejection: RCH ts1 `cargo test -p fj-lax --lib matmul_2d -- --nocapture` passed 7 focused tests plus 1 ignored timing test, covering packed, threaded, large, and blocked bit-identity proofs.
- perf gate failed: candidate 1024^3 on ts1 was 21.447ms [19.529, 23.246], only 1.028x midpoint vs prior ts1 control with overlapping intervals and 0.898x conservative. Candidate 2048^3 on ts1 was 274.65ms [245.53, 301.88], not a blocked-path win versus pass138's 146.62ms gated baseline. RCH shared-worker artifact retrieval also narrowed the local candidate diff during the run, so the only safe decision was rejection.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Golden sha256 `2ea200b4960ed9a1c5e244ffc545c91cdddceb27a101fdcafece6e0c5503e231`.
- next primitive: attack Strassen/recursive GEMM behind the existing Dot tolerance contract, leaving `matmul_2d` internal bit-identity tests on the current path. Target >=1.3x on 1024^3-2048^3 Dot large-matrix path with golden sha256 and explicit tolerance/oracle proof.

## pass141 — fj-lax flat GEMM safe-SIMD lanes (SHIPPED, bead frankenjax-vgd3x)
- target `linalg/matmul_2d_1024x1024x1024_f64` after pass140's broad safe-SIMD rejection. Lever: narrow the change to the flat `matmul_2d_row_block` full-panel path only, using `std::simd::Simd<f64,4>` lanes for four independent output columns; the KC-blocked path, row/column remainders, threading, pack thresholds, MR/NR, and public API remain unchanged.
- same-worker RCH ts1 scalar control -> final flat-SIMD candidate: `38.293ms [35.004,43.547]` -> `19.600ms [18.924,20.803]` = 1.954x midpoint and 1.683x conservative. Score 10.0.
- isomorphism: each output cell still accumulates products in ascending k order; SIMD lanes are independent columns; no horizontal reduction, reassociation, `mul_add`, FMA forcing, mixed precision, BLAS/LAPACK/MKL/XLA, unsafe code, tie-breaking, or RNG surface is introduced. Golden sha256 `c9b848a87aeb2e3032e58874217e58d588a05cae19bc8a201cd65247ab378f3e`.
- validation: RCH ts1 focused `cargo test -p fj-lax --lib matmul_2d` passed 7 tests plus 1 ignored timing test; RCH ts1 `cargo check -p fj-lax --all-targets` passed; RCH ts1 `cargo clippy -p fj-lax --all-targets -- -D warnings` passed; `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs` passed; `git diff --check` passed; UBS exited 0 with existing inventory. `cargo fmt --check --package fj-lax` is still blocked by pre-existing `crates/fj-lax/src/linalg.rs` formatting drift, so this one-lever commit leaves it untouched.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_flat_simd_vgd3x_pass141_2026-06-06.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile after landing; if GEMM remains hot, attack a separate KC-blocked/2048 SIMD proof or Dot-only recursive/Strassen path behind an explicit tolerance contract.

## pass142 — fj-lax KC-blocked GEMM SIMD lanes (REJECTED, bead frankenjax-cmqsr)
- target `linalg/matmul_2d_2048x2048x2048_f64`, freshly profiled after pass141 landed: RCH ts1 `114.60ms [112.37,116.94]`.
- lever tested: apply the same safe `std::simd::Simd<f64,4>` independent-column lanes to the KC-blocked full-panel path, seeding lanes from running C on non-first pc blocks. MR/NR/KC, pack thresholds, threading, border paths, and public API stayed unchanged.
- proof before rejection: RCH ts1 focused `cargo test -p fj-lax --lib matmul_2d` passed 7 tests plus 1 ignored timing test. Isomorphism attempt preserved pc-block order and ascending k order per cell, with no horizontal reduction, reassociation, `mul_add`, FMA forcing, tie-breaking, or RNG.
- perf gate failed on same-worker RCH ts1: baseline `114.60ms [112.37,116.94]` vs candidate `129.30ms [119.87,138.31]` = 0.886x midpoint and 0.812x conservative. Score 0.0.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty after restore. Golden sha256 `ea3454dca2676ffc12627333a5c8561a269671cbd1e9bb737553c332e8ad3e34`.
- next primitive: stop lane-only tuning of the KC-blocked path; attack packed-A/register blocking, recursive/cache-oblivious Dot under an explicit tolerance contract, or communication-avoiding GEMM after proving the behavior contract.

## pass143 — fj-lax random_uniform threaded counter chunks (REJECTED, bead frankenjax-jhgap)
- target `random/uniform_1m_f64`. Baseline from the bead's fresh RCH ts1 profile: `9.351ms [9.351,9.540]`, midpoint `9.433ms`.
- lever tested: split the independent ThreeFry counter stream into scoped-thread contiguous chunks for large `random_uniform` calls, preserving key/counter `[0, i]`, 23-bit mantissa extraction, and `minval + unit * scale` per element.
- proof before rejection: RCH ts1 `cargo test -p fj-lax --lib threefry -- --nocapture` passed 75 tests, including threaded uniform bit identity and existing RNG golden SHA checks (`e96748ed0520e1648d412ffe3ccddbf966b23fd0828824e0d924e2fa7108c8f5`, `982d444c8f93dd7331fff1e2141e40ee967f6698a7a12cbeef104d38b6c6c29b`).
- perf gate failed: candidate RCH rebench selected `vmi1264463` and measured `20.409ms [20.409,27.018]`, midpoint `23.531ms`, a clear regression versus the ts1 baseline. Score 0.0.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/threefry.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_random_uniform_parallel_chunks_jhgap_pass143_2026-06-06.rejected.json`.
- next primitive: stop spawn-per-call RNG chunking; attack a fused ThreeFry-to-normal stream / distribution-specific generator that removes intermediate uniform allocation and scalar transform overhead, target >=1.4x on `random/normal_1m_f64`.

## pass144 — fj-lax fused ThreeFry-to-normal stream (REJECTED, bead frankenjax-g8wys)
- target `random/normal_1m_f64`. Fresh RCH baseline on `vmi1227854`: uniform `9.8564..9.9112..9.9684ms`, normal `44.425..45.552..47.102ms`. Recent same-code ts1 baseline from pass143 was normal `24.638..26.629..29.615ms`.
- lever tested: fuse ThreeFry uniform mantissa generation directly into `sqrt(2) * erf_inv_approx(u)` normal writes over contiguous counter chunks, removing the intermediate uniform Vec and parallelizing generation+transform in one pass. This followed the alien-graveyard streaming/fusion/allocation-elimination route, with an explicit key-counter/FP/golden contract.
- proof before rejection: RCH `cargo test -p fj-lax --lib threefry -- --nocapture` on `vmi1149989` passed 75/75, including the temporary fused-vs-materialized-uniform bit test and existing RNG golden SHA checks (`e96748ed0520e1648d412ffe3ccddbf966b23fd0828824e0d924e2fa7108c8f5`, `982d444c8f93dd7331fff1e2141e40ee967f6698a7a12cbeef104d38b6c6c29b`).
- perf gate failed: candidate RCH rebench selected `ts1` and measured normal `52.453..56.986..63.573ms`, worse than both the fresh cross-worker baseline and the recent ts1 same-code baseline. Score 0.0.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/threefry.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_random_normal_fused_stream_g8wys_pass144_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `2a7d1ffc6db27879d70c5d7e73fb60d6f606c7b2e9860bf2c85e7a9d05d4e5b2`).
- next primitive: do not retune fused/parallel normal stream shape. Move to a different distribution-core algorithm only after a behavior-contract bead proves whether normal sample sequence may change; otherwise reprofile and attack a different hot RNG surface such as `random_randint`/`generate_u32_bits` parallelization or categorical/Gumbel fusion.

## pass145 — fj-lax FFT precomputed radix-2 batch plan (REJECTED, bead frankenjax-cchiz)
- target `eval/fft_batch_2048x256_complex128`. Fresh RCH profile on `ts1`: complex batch FFT `9.7791..10.506..10.939ms`, dense-input complex batch FFT `7.1073..7.5395..8.2187ms`, RFFT prime/Bluestein control `9.8610..10.731..11.611ms`.
- lever tested: store a `Radix2Plan` in `BatchFftPlan::Pow2`, reusing precomputed bit reversal and twiddle tables across batched power-of-two rows instead of calling `fft_1d_into` / `ifft_1d_into` per row. This was the alien-graveyard cache/table-reuse primitive applied to a measured loop nest.
- proof before rejection: RCH `cargo test -p fj-lax --lib fft -- --nocapture` on `ts1` passed 40/40, including temporary `batch_pow2_plan_matches_legacy_radix2_bits`, existing `radix2_plan_matches_unplanned_path_by_bits`, FFT/RFFT golden digest tests, and threaded batch FFT/RFFT/IRFFT tests.
- perf gate failed on same-worker `ts1`: target complex batch FFT was flat/slower `10.506ms -> 10.559ms`; prime FFT regressed `128.33us -> 183.56us`; RFFT dense `5.5244ms -> 7.1592ms` and IRFFT `4.9444ms -> 5.3489ms` regressed. Dense complex FFT alone improved `7.5395ms -> 6.6339ms`, but the lever is not a safe keep across affected power-of-two dispatch.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/fft.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_fft_pow2_batch_plan_cchiz_pass145_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `fd141db8fecdc708393831800dbbdb96e3c833756ec7dff327af7c5fc68f34ee`).
- next primitive: stop retuning direct radix-2 plan dispatch. Attack a different FFT primitive: real-specialized Bluestein/RFFT convolution that avoids complex zero-imag work for prime real transforms, or a narrow FFT-boundary complex literal-to-dense promotion only if it does not collide with IcyGlacier's dense-storage ownership.

## pass146 — fj-lax RFFT packed-real extraction (REJECTED, bead frankenjax-rhlr2)
- target `eval/rfft_batch_64x1499_bluestein_f64`. Fresh RCH baseline on `ts1`: prime Bluestein RFFT `7.0963..7.2063..7.3371ms`, mixed-radix control `1.6106..1.6257..1.6418ms`, power-of-two RFFT `7.0444..7.3260..7.6142ms`, dense-input power-of-two `4.2949..4.3387..4.4043ms`.
- lever tested: keep RFFT real inputs in a borrowed/owned `f64` slice through extraction and inject `(value, +0.0)` only at row setup, avoiding an eager full `Vec<(f64,f64)>` zero-imaginary buffer. This was the alien-graveyard data-layout specialization applied to the measured real-Bluestein path.
- proof before rejection: RCH `cargo test -p fj-lax --lib fft -- --nocapture` on `ts1` passed 41/41, including temporary dense/literal real extraction bit-identity tests, FFT/RFFT golden digest tests, and threaded batch FFT/RFFT/IRFFT tests.
- perf gate failed on same-worker `ts1`: target prime Bluestein RFFT regressed `7.2063ms -> 7.4705ms`; mixed-radix control regressed `1.6257ms -> 1.7754ms`; power-of-two literal RFFT regressed `7.3260ms -> 7.9049ms`. Dense-input power-of-two improved `4.3387ms -> 3.5361ms`, but this was not enough to keep a broad RFFT extraction lever.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/fft.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_rfft_real_packed_input_rhlr2_pass146_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `431517ab4fefcd454c5471bd2f750827bf56b5e1392500c9d2d677ec6dacdb1e`).
- next primitive: stop retuning RFFT input extraction. Attack a structural FFT primitive next: prefix-only Bluestein final chirp for RFFT, real-output-aware Bluestein postprocessing, or a specialized half-spectrum path that avoids computing/writing unused second-half outputs without perturbing convolution order.

## pass147 — fj-lax RFFT Bluestein prefix postprocess (REJECTED, bead frankenjax-6srm2)
- target `eval/rfft_batch_64x1499_bluestein_f64`. Fresh RCH same-path baseline on current `2b0895c8` with worker `ts1`: target `7.0036..7.0705..7.1457ms`; dense-input control `4.2867..4.3628..4.4387ms`.
- lever tested: add a Bluestein prefix-output helper and route non-power-of-two RFFT rows through it so the final chirp/write loop only emits the consumed Hermitian half-spectrum. This left the Bluestein convolution itself unchanged and only shortened the final write loop.
- proof before rejection: temporary `bluestein_prefix_matches_full_output_prefix_by_bits` asserted kept prefix values match the full Bluestein path bit-for-bit; focused RCH `cargo test -p fj-lax --lib fft -- --nocapture` passed 40/40 during the proof run, including existing FFT/RFFT golden digest tests.
- perf gate failed on same-worker `ts1`: baseline target `7.0705ms` vs candidate `7.4987ms` from the same detached worktree after applying the lever = 0.942x midpoint. A candidate run on `vmi1149989` measured `6.5757ms`, but it was ignored as cross-worker evidence. An older `52dff6a3` provisional small win was superseded after main advanced.
- decision: rejected and source restored; `git diff -- crates/fj-lax/src/fft.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_rfft_bluestein_prefix_6srm2_pass147_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`).
- next primitive: stop postprocess-prefix tuning. Attack a larger RFFT-specific real-input Bluestein half-spectrum algorithm that avoids zero-imaginary complex work and unused second-half output work, with target >=1.2x on `eval/rfft_batch_64x1499_bluestein_f64`.

## pass148 — fj-lax QR no-pack compact-WY panel apply (REJECTED, bead frankenjax-fh5q1)
- target `linalg/qr_1024x1024_f64`. Fresh bead baseline via RCH on `ts1`: `qr_128x128_f64` `667.56..684.39..696.91us`, `qr_1024x1024_f64` `124.89..137.86..150.94ms`, guard rows `matmul_2d_256` `1.6506..1.8840..2.3412ms` and `matmul_2d_512` `7.2892..7.5897..7.9523ms`.
- lever tested: reintroduced large real-QR compact-WY blocking but replaced the rejected `matmul_2d` panel products with a QR-local no-pack single-thread panel apply. Small `k < 256` real QR stayed on the existing scalar path.
- proof before rejection: RCH `cargo test -p fj-lax --lib qr -- --nocapture` on `vmi1156319` passed 27/27 filtered tests, including existing small QR golden digest `6119fc5cf4759d8cdcd9c34d89a79de89d205203730814fc06aa52bf57ff262b` and a temporary blocked reconstruction/orthonormality proof (`A=QR`, `Q^TQ=I` to `1e-9`).
- perf gate failed: candidate RCH rebench on `vmi1156319` measured `qr_1024x1024_f64` `3.0440..3.1293..3.2175s`, catastrophically slower than the `137.86ms` baseline family; `qr_128x128_f64` stayed small-path at `606.58..620.38..642.70us`. Score 0.0.
- decision: rejected and source/test hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_qr_no_pack_wy_fh5q1_pass148_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `b9dd4bff3ede8c8506360a9ba6db927b666090b62a1d88a7e413f1e44efd19b5`).
- next primitive: stop no-pack compact-WY QR apply. Attack a different QR primitive next: communication-avoiding/tall-skinny blocked QR or cache-oblivious panel QR with a true register-tiled update and an explicit large-QR tolerance contract; target Score >= 2.0 on `linalg/qr_1024x1024_f64`.

## pass149 -- fj-lax Cholesky lower-triangle SYRK update (REJECTED, bead frankenjax-p1vbf.28)
- target `linalg/cholesky_1024x1024_f64`. Fresh RCH reprofile on `ts1`: `57.376..60.661..64.341ms`; same profile showed `qr_1024x1024_f64` `86.953..88.594..91.996ms`, `lu_1024x1024_f64` `56.417..57.182..57.808ms`, and `matmul_2d_1024` `22.536..27.170..31.211ms`.
- lever tested: replace the blocked Cholesky full square `A22 -= L21*L21^T` product/update with a lower-triangle-only SYRK update, because future panels only consume the lower triangle and the strict upper triangle is zeroed before return.
- proof before rejection: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `ts1` passed 12/12, including existing blocked reconstruction/scalar parity coverage.
- perf gate failed on same-worker `ts1`: target regressed from `60.661ms` median to `65.966ms` median (`0.920x`). A provisional `vmi1149989` run also regressed at `67.542ms` median. Score 0.0.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_cholesky_syrk_lower_p1vbf28_pass149_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `9f4d0b7ae4979036be313312754567b636aa399b84418049cfb9c5a702438ab5`).
- next primitive: stop lower-only scalar SYRK tuning. Attack a different Cholesky primitive next: recursive/cache-oblivious Cholesky with packed panel solve plus a true register-tiled full Schur update, or a fused TRSM+GEMM update with explicit large-matrix tolerance proof; target >=1.2x on `linalg/cholesky_1024x1024_f64` or route to the next reprofiled linalg hotspot.

## pass150 -- fj-lax GEMM blocked-KC F64x4 accumulators (REJECTED, bead frankenjax-p1vbf.29)
- target `linalg/matmul_2d_1024x1024x1024_f64`. Fresh RCH baseline on `ts1`: direct GEMM1024 `16.476..18.704..24.512ms`, GEMM512 `4.6340..8.3617..13.402ms`, GEMM256 `934.64..947.48..964.42us`, downstream Cholesky1024 `54.106..54.523..55.187ms`, LU1024 `57.902..58.464..59.117ms`.
- lever tested: replace scalar `[f64; 4]` accumulators in the blocked KC macro-kernel with `F64x4` accumulators, matching the flat row-block microkernel while preserving per-lane ascending-k order.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed 8/8 focused tests (1 ignored timing test), including special-value, packed, threaded, large, and blocked bit-identity coverage.
- perf gate failed on same-worker `ts1`: target GEMM1024 regressed `18.704ms -> 19.439ms` median (`0.962x`); downstream Cholesky1024 regressed `54.523ms -> 57.560ms`; LU1024 stayed flat `58.464ms -> 58.467ms`. Score 0.0. GEMM512 improved against a noisy baseline but is not enough to keep a target-regressing shared kernel lever.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_blocked_f64x4_p1vbf29_pass150_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `1eab494833c56686467d677ea13442075bdb06fb76f2150636b4c050e2e87293`).
- next primitive: stop blocked-KC scalar-vs-F64x4 accumulator tuning. Attack a different GEMM primitive next: packed A panels plus a register-tiled macro-kernel that reduces C reload/store passes, or an autotuned MR/NR/KC family with explicit bit-identity proof; target >=1.2x on `linalg/matmul_2d_1024x1024x1024_f64` and no downstream LU/Cholesky regression.

## pass151 -- fj-lax eig skip unused q_total Schur-vector accumulation (SHIPPED, bead frankenjax-p1vbf.30)
- target `linalg/eig_48x48_f64`. Fresh RCH baseline on `ts1` after pass150 and dense-output reprofile: `44.821..45.351..45.733ms`; guards were Cholesky1024 `54.931..55.635..57.388ms`, QR1024 `35.225..37.336..39.031ms`, LU1024 `53.253..54.838..56.771ms`.
- lever shipped: `eig_qr_iteration` still constructs the same Hessenberg `t` and applies every left/right Givens update to `t`, but calls `hessenberg_qr_step(&mut t, None, n)` so the discarded `q_total` Schur-vector accumulator no longer receives rotations. A small preparatory commit (`42264534`) replaced complex tuple return spellings with existing aliases and `since_deflate % 10 == 0` with `is_multiple_of(10)` so crate clippy could run cleanly.
- proof: RCH `cargo test -p fj-lax --lib eig -- --nocapture` on `ts1` passed 17/17 focused tests. Isomorphism: `t` evolution, block-walk ordering, eigenvalue extraction, inverse-iteration eigenvectors on original `a`, tie-breaking, and RNG surface are unchanged; removed floating-point work mutates only unobserved `q_total`.
- perf gate passed on same-worker `ts1`: target final exact-source rebench `29.044..31.389..33.021ms`; median speedup `1.445x`, conservative speedup `1.357x`, Score `10.0`. Guards on exact source: Cholesky1024 `52.360..52.985..53.850ms`, QR1024 `35.743..37.327..40.427ms`, LU1024 `52.081..52.709..53.575ms`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed; `git diff --check` passed. `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` and `ubs crates/fj-lax/src/linalg.rs` remain blocked by pre-existing broad file inventory outside this lever, while UBS's clippy/fmt/check sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_eig_skip_qtotal_p1vbf30_pass151_2026-06-06.json` (+ `.golden`, `.golden.sha256`, golden sha `9d249038ae2f430ceb8585e22bdc2fde169c22bf272c32a61166f7ab6df41d52`).
- next primitive: reprofile after landing; sidecar alien-graveyard/artifact pass recommends `lu_factor_real_blocked` fused in-place Schur update next, target >=1.15x on `linalg/lu_1024x1024_f64`.

## pass152 -- fj-lax LU fused in-place Schur update (REJECTED, bead frankenjax-p1vbf.31)
- target `linalg/lu_1024x1024_f64`. Profile-backed route after pass151 on `vmi1149989`: LU1024 `41.219..43.812..47.290ms`, Cholesky1024 `61.090..72.636..80.864ms`, matmul1024 `41.186..44.475..48.159ms`. Fixed-worker baseline on `vmi1149989`: LU1024 `41.953..43.860..46.651ms`, Cholesky1024 `44.930..47.709..53.447ms`, matmul1024 `48.335..52.094..56.202ms`.
- lever tested: replace LU's materialized `L21`, `U12`, `prod = matmul_2d(L21, U12)`, and separate product subtract pass with a scoped-threaded in-place Schur update over disjoint trailing rows using independent 4-lane SIMD columns and ascending panel-column accumulation per A22 cell.
- proof before rejection: after a borrow-shape compile fix inside the candidate, RCH `cargo test -p fj-lax --lib lu -- --nocapture` on `vmi1149989` passed 58/58 focused tests, including blocked reconstruction/scalar parity and real-vs-complex-zero-imag coverage. Isomorphism: pivot scan/tie order, row swaps, panel factorization, and U12 solve unchanged; each A22 cell still accumulates t=0..jb in order; no FMA, mixed precision, horizontal reduction, or RNG surface.
- perf gate failed on same-worker `vmi1149989`: LU1024 regressed `43.860ms -> 54.250ms` median (`0.8085x`; conservative `0.7534x`). Cholesky/matmul guards in the same run were Cholesky1024 `41.736..43.159..44.591ms` and matmul1024 `32.092..34.342..36.852ms`, but the target regression fails Score>=2.0. Score 0.0.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_lu_fused_schur_p1vbf31_pass152_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `1b6b7f03f59f83dd6f19bdd49313a1c88f430ed7c4af5520c616dd911ce294a0`).
- next primitive: stop direct fused LU Schur-loop variants; reprofile restored source and attack a different linalg primitive family, either blocked Cholesky full Schur update if `cholesky_1024x1024_f64` remains top or eig values-only Hessenberg reduction if eig re-emerges. Target >=1.15x on the next fixed-worker top linalg row.

## pass153 -- fj-lax Cholesky direct-subtract full Schur update (REJECTED, bead frankenjax-bsae0)
- target `linalg/cholesky_1024x1024_f64`. Restored-source RCH reprofile on fixed worker `vmi1149989`: Cholesky1024 `39.678..41.152..42.218ms`, LU1024 `39.368..40.301..41.299ms`, QR1024 `32.745..33.729..35.300ms`, matmul1024 `29.485..31.458..33.584ms`, eig48 `22.042..22.676..23.225ms`.
- lever tested: keep existing `L21` and `L21^T` panel materialization but replace `prod = matmul_2d(L21, L21^T)` plus separate A22 subtract with a Cholesky-local 4x4 register-tiled microkernel that subtracts each full Schur product directly into A22.
- proof before rejection: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `vmi1149989` passed 12/12, including blocked scalar parity/reconstruction, real-vs-complex fast-path parity, NaN/non-square behavior, and triangular solve roundtrip. Isomorphism: small path, diagonal panel factorization, panel solve, strict-upper zeroing, and output layout unchanged; each product cell accumulates t in ascending order; no FMA, mixed precision, horizontal reduction, pivoting, or RNG surface.
- perf gate failed on same-worker `vmi1149989`: Cholesky1024 regressed `41.152ms -> 116.43ms` median (`0.3535x`; conservative `0.3237x`). Guards also degraded in that run: QR1024 `42.558ms`, LU1024 `85.902ms`, matmul1024 `57.615ms`. Score 0.0.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_cholesky_full_schur_bsae0_pass153_2026-06-06.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `085638c4ea9849193e1f0ab1b9be7118130f6109602cf8fe0a633040f7769772`).
- next primitive: stop Cholesky Schur-subtract loop variants after pass149 and pass153. Reprofile restored source and pivot to a different algorithmic family: eig values-only Hessenberg reduction if eig remains material, or a Cholesky panel/TRSM primitive that preserves the existing `matmul_2d` kernel rather than replacing its product loop. Target >=1.15x on the next fixed-worker top row.

## pass156 -- fj-lax Cholesky panel scratch reuse (REJECTED, bead frankenjax-p1vbf.36)
- target `linalg/cholesky_1024x1024_f64`. Restored-source RCH reprofile on fixed worker `ts1`: Cholesky1024 `79.404..88.914..98.656ms`, LU1024 `51.378..53.343..55.646ms`, QR1024 `35.479..36.111..36.723ms`, eig48 `28.769..29.865..31.524ms`, matmul1024 `17.194..18.936..21.024ms`, pinv192 `8.9569..9.0574..9.1856ms`.
- lever tested: move only the allocation lifetime of Cholesky blocked-panel `L21` and `L21^T` scratch buffers out of the panel loop while preserving panel factorization, panel solve, `matmul_2d(L21, L21^T)`, A22 subtract order, strict-upper zeroing, dtype/shape/error behavior, and RNG absence. Sidecar review confirmed this was a valid one-lever allocation candidate if `L21^T` stayed indexed in transposed layout.
- proof before rejection: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `vmi1167313` passed 12/12 in candidate state, including `cholesky_blocked_matches_scalar_and_reconstructs`, `cholesky_real_fast_path_bit_identical_to_complex_path`, NaN/non-square behavior, and `triangular_solve_cholesky_roundtrip`.
- perf gate failed on same-worker `ts1`: Cholesky1024 regressed from `88.914ms` median to `102.16ms` median (`0.8704x`; conservative `0.8042x`). Guard rows also regressed in the candidate run: LU1024 `108.21ms`, QR1024 `45.239ms`, matmul1024 `61.887ms`. Score 0.0.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_cholesky_scratch_reuse_p1vbf36_pass156_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `c79c2faee113ec843e8378d7681f85e6d2a5259650fb026d7e03cb900cf3cd40`).
- next primitive: do not retune Cholesky scratch allocation. Attack a deeper algorithmic primitive next: recursive/cache-oblivious Cholesky with packed panel solve plus tolerance proof, or CA-QR/TSQR if restored-source reprofile moves QR above Cholesky.

## pass157 -- fj-lax GEMM2048 KC512 tile retune (REJECTED, bead frankenjax-p1vbf.37)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh fixed-worker RCH target-only baseline on `vmi1227854`: `470.60..507.00..549.33ms` after an initial mixed-profile run showed the same target as the top row but too much variance (`434.05..700.29..1027.10ms`).
- lever tested: increase the cache-blocked packed-B GEMM macro-kernel from `KC=256` to `KC=512`, reducing GEMM2048 C read/write passes from eight to four while preserving pc order and ascending inner `l` order for each output element.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1227854` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed on same-worker `vmi1227854`: target regressed from `507.00ms` median to `854.03ms` median (`0.5937x`; conservative `0.5220x`). Score 0.0.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_kc512_p1vbf37_pass157_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `9f7569b62998919d1498c1a59f019fb255680b85a8ebad284ae00192b3454445`).
- next primitive: stop KC retuning. Attack a deeper GEMM primitive next: packed-A/register-blocked macro-kernel or cache-oblivious/recursive GEMM that reduces A reloads and C reload/store pressure while preserving per-output ascending-k accumulation.

## pass158 -- fj-lax GEMM2048 paired-panel macro-kernel (REJECTED, bead frankenjax-o84i1)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh fixed-worker RCH target-only baseline on `vmi1293453`: `368.53..377.68..388.09ms`.
- lever tested: group two adjacent packed-B NR panels inside the blocked GEMM macro-kernel, so each loaded A scalar feeds 16 output columns before the next k-step. This preserves pc order, ascending inner `l` order for every output element, thread row ownership, dtype/shape/error behavior, and RNG absence.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1149989` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed to clear keep threshold on same-worker `vmi1293453`: candidate measured `335.84..357.62..386.10ms`; median speedup `1.0561x`, but conservative speedup `0.9545x` and Score `1.0 < 2.0`. Marginal/noisy wins do not satisfy the campaign gate.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_pair_panels_o84i1_pass158_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `ae5e4c0f52c8a3292d76c2d26cc735e9777a78dfb638adf6724def14c1c259ed`).
- next primitive: stop paired-panel grouping. Attack a true packed-A slab plus JC-blocked macro-kernel or another cache-oblivious GEMM primitive that reduces memory traffic more substantially while preserving per-output ascending-k accumulation.

## pass159 -- fj-lax GEMM2048 packed-A slab (SHIPPED, bead frankenjax-zntc3)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh fixed-worker RCH target-only baseline on `vmi1156319`: `2.1227..2.2458..2.3819s`.
- lever shipped: pack each thread row-block's full MR-row A slab once per KC block in `[row_tile][k][row]` order before sweeping packed-B NR panels. This reduces repeated strided A reloads while keeping packed-B layout, row-block threading, MR/NR/KC values, and border handling unchanged.
- proof: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage. Isomorphism: pc-block order, j-panel order, i-tile order, ascending inner `l` accumulation per output cell, C seed/writeback continuation, row-major output, dtype/shape/error behavior, and RNG absence are unchanged; only the A scalar load address changes after a one-time copy.
- perf gate passed on same-worker `vmi1156319`: target rebench `1.4692..1.6003..1.7549s`; median speedup `1.4033x`, conservative speedup `1.2096x`, Score `8.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `vmi1149989`; `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs`, `git diff --check`, and `ubs crates/fj-lax/src/tensor_contraction.rs` passed. UBS exit 0 retained only pre-existing file-wide warning inventory surfaces; fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_packeda_zntc3_pass159_2026-06-07.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile after landing; continue with a deeper JC-blocked packed-A/packed-B macro-kernel, cache-oblivious GEMM, or the next top linalg row.

## pass160 -- fj-lax Cholesky lower-triangle Schur update (SHIPPED, bead frankenjax-zu5kh)
- target `linalg/cholesky_1024x1024_f64`. Post-packed-A linalg reprofile on fixed worker `ts1`: Cholesky1024 `119.25..121.50..126.40ms`, pinv256x128 `110.58..113.26..119.61ms`, matmul2048 `104.69..106.73..110.28ms`, LU1024 `52.905..54.134..56.520ms`, QR1024 `42.932..43.427..43.833ms`.
- lever shipped: blocked Cholesky now applies the trailing symmetric Schur update only to lower-triangle entries `q <= p` consumed by later panels, skipping unused upper-triangle writes before the existing strict-upper zeroing at return. The update uses scoped threads over disjoint row chunks only when the lower-triangle product count is large enough to amortize fan-out.
- proof: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `ts1` passed 13/13 focused tests, including new bit-equality coverage against the former full-GEMM Schur product on every consumed lower-triangle entry and existing blocked scalar parity/reconstruction coverage. Isomorphism: diagonal panel factorization, panel solve order, per-entry ascending panel-column accumulation, strict-upper zeroing, dtype/shape/error behavior, NaN surface, tie/RNG absence, and output layout are preserved; only unobserved upper-triangle Schur entries are skipped.
- perf gate passed on same-worker `ts1`: target baseline `106.28..117.61..130.30ms`; target rebench `89.706..92.497..95.659ms`; median speedup `1.2715x`, conservative speedup `1.1103x`, Score `8.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ts1`; `git diff --check` and golden sha256 passed. `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` still fails on pre-existing unrelated formatting outside this lever, and `ubs crates/fj-lax/src/linalg.rs` reports pre-existing file-wide inventory while fmt/clippy/check/test-build sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_cholesky_lower_schur_zu5kh_pass160_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `2d4e2185e82faee371572306cb38a9bbba51dd972facb7401b0b61cac96e62ac`).
- next primitive: reprofile after landing because Cholesky's trailing-update cost changed; likely next linalg row is pinv/matmul/QR depending on fixed-worker ranking.

## pass161 -- fj-lax pinv one-sided SVD column-major work buffer (SHIPPED, bead frankenjax-m95v5)
- target `linalg/pinv_256x128_f64`. Post-pass160 fixed-worker RCH profile on `ts1`: pinv256x128 `123.37..124.47..125.21ms`, matmul2048 `98.821..99.983..101.27ms`, Cholesky1024 `88.959..94.968..101.34ms`, LU1024 `52.554..54.525..57.937ms`, eig48 `46.814..47.436..48.021ms`.
- lever shipped: `one_sided_jacobi_svd_real` now stores its private `W` work buffer column-major because every Jacobi sweep streams columns p/q for alpha/beta/gamma and rotations. This is a data-layout/loop-locality primitive; no public storage, API, or algorithmic pair ordering changed.
- proof: RCH `cargo test -p fj-lax --lib svd -- --nocapture` on `ts1` passed 8/8 and RCH `cargo test -p fj-lax --lib pinv -- --nocapture` passed 7/7. New direct test `one_sided_jacobi_svd_real_reconstructs_tall_profile_shape` verifies reconstruction and orthogonality on a full-column-rank tall matrix. Isomorphism: Jacobi pair order, row iteration order for each dot and rotation, V accumulation, singular-value sorting, rcond cutoff, dtype/shape/error behavior, NaN surface, tie/RNG absence, and output layout are preserved; only private W addressing changes from `row*n+col` to `col*m+row`.
- perf gate passed on same-worker `ts1`: target baseline `101.87..102.82..104.03ms`; target rebench `59.421..59.879..60.323ms`; median speedup `1.7171x`, conservative speedup `1.6887x`, Score `10.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ts1`; `git diff --check` and golden sha256 passed. `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` still fails on pre-existing unrelated formatting outside this lever, and `ubs crates/fj-lax/src/linalg.rs` reports pre-existing file-wide inventory while fmt/clippy/check/test-build sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_pinv_svd_colmajor_m95v5_pass161_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `f2ecb968d7bc0ff1cd26d4f9db1296285b5cc42d832e0f5fcb99fc8c04b4dc45`).
- next primitive: reprofile after landing; pinv256 is likely no longer the dominant linalg row, so route to the next fixed-worker top row rather than retuning SVD layout immediately.

## pass162 -- fj-lax GEMM2048 blocked microkernel SIMD (SHIPPED, bead frankenjax-f71wy)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Post-pass161 fixed-worker RCH profile on `ts1`: matmul2048 `106.09..109.51..112.15ms`, pinv256x128 `60.261..61.128..62.049ms`, Cholesky1024 `53.963..54.615..55.292ms`, LU1024 `51.396..52.302..53.027ms`.
- lever shipped: the KC-blocked GEMM full-tile microkernel now uses `Simd<f64,8>` accumulators for each MR row instead of scalar NR arrays. The flat packed path already used the same lane primitive; this applies it to the blocked path that dominates 2048^3.
- proof: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed 9/9 focused tests (1 ignored timing test), and RCH `cargo test -p fj-lax --release --lib matmul_2d -- --nocapture` also passed 9/9. Coverage includes special values, packed/threaded/large bit-identity, blocked bit-identity, and the packed NR8 golden digest. Isomorphism: row ownership, pc-block order, j-panel order, i-tile order, per-output ascending inner-l accumulation, C seed/writeback continuation, packed-A/B layouts, dtype/shape/error behavior, NaN surface, tie/RNG absence, and row-major output are preserved; only the full-tile blocked accumulator representation changes from scalar arrays to SIMD lanes.
- perf gate passed on same-worker `ts1`: target baseline `128.81..142.09..156.23ms`; target rebench `101.99..103.99..106.36ms`; median speedup `1.3664x`, conservative speedup `1.2109x`, Score `8.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ts1`; `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs`, `git diff --check`, and `ubs crates/fj-lax/src/tensor_contraction.rs` passed. UBS exit 0 retained pre-existing file-wide warning inventory, while fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_blocked_simd_f71wy_pass162_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `3462dba3ff49e680b2135f51f2d5b99f50af71aea1b63232863a983a9bf6f3ac`).
- next primitive: reprofile after landing; if GEMM2048 falls below the next linalg row, route to that row instead of another same-family microkernel tweak.

## pass163 -- fj-lax GEMM2048 NR16 register tile (REJECTED, bead frankenjax-7wpc7)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Post-pass162 fixed-worker profile on `ts1`: matmul2048 `90.404..92.631..95.291ms`, pinv256x128 `58.870..59.544..60.567ms`, LU1024 `52.565..54.299..55.516ms`, Cholesky1024 `53.272..53.515..53.967ms`, QR1024 `34.536..35.105..36.093ms`.
- lever tested: widen the blocked GEMM SIMD register tile from `NR=8` to `NR=16`, using `Simd<f64,16>` panel loads/accumulators while preserving packed-A/B layout, row-block threading, pc/j/i loop order, per-output ascending inner-l accumulation, C seed/writeback continuation, dtype/shape/error behavior, and RNG absence.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1156319` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed golden digest coverage.
- perf gate failed on same-worker `vmi1156319`: target baseline `668.59..763.22..879.64ms`; first after-run `633.99..651.66..670.35ms` gave median speedup `1.1712x` but conservative speedup only `0.9974x`; longer confirmation after-run regressed to `719.85..771.45..843.15ms` with median speedup `0.9893x` and conservative speedup `0.7930x`. Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_nr16_7wpc7_pass163_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `2cb7e54d2836da9c4d60500c5ecceb00edc5ae28fc7f74fffee498dc34bd7a56`).
- next primitive: stop widening this same NR tile. Reprofile restored source and attack a deeper GEMM primitive such as cache-oblivious recursion/outer-panel scheduling, or route to the next fixed-worker top linalg row if GEMM no longer dominates.

## pass164 -- fj-lax GEMM2048 packed-A row-slab blocking (REJECTED, bead frankenjax-i7atq)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Restored-source fixed-worker baseline on `vmi1156319`: `595.63..631.25..671.49ms` with the longer Criterion window. The profile route remained the post-pass162 top linalg row because pass163 restored source: GEMM2048 `90.404..92.631..95.291ms` on `ts1`.
- lever tested: tile the blocked GEMM `apack` row slab by a bounded IC count (`16` MR-row tiles) instead of packing the whole thread row chunk for each KC block. This is a polyhedral/locality primitive intended to keep A slabs cache-resident while preserving row ownership, pc order, panel semantics, and per-output ascending-l accumulation.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1156319` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed golden digest coverage.
- perf gate failed on same-worker `vmi1156319`: candidate rebench `749.33..827.30..906.44ms`; median speedup `0.7629x`, conservative speedup `0.6571x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_row_slab_i7atq_pass164_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `a0433e739dc04c8e726f59325db826464ac42f949c7034ff71cd8ff27a539ab2`).
- next primitive: stop row-slab A-pack tiling. The next GEMM swing should change the larger macro-schedule, such as a single-sweep packed full-k path gate, or first file a behavior-contract bead for FP-tolerant recursive/Strassen-style algorithms.

## pass165 -- fj-lax GEMM2048 single-sweep full-k route (REJECTED, bead frankenjax-gemm-fullk-sweep-c3pgz)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh restored-source linalg profile on fixed worker `ts1`: GEMM2048 `100.59..103.92..109.57ms`, pinv256x128 `61.991..63.476..65.462ms`, LU1024 `53.085..55.837..58.225ms`, Cholesky1024 `52.317..54.311..56.718ms`, QR1024 `34.193..35.039..36.730ms`, eig48 `30.307..31.507..32.685ms`.
- lever tested: route the 2048-sized packed-B GEMM to the existing full-k single-sweep packed path instead of the KC-blocked C-carry path. This tested whether avoiding eight C read/write passes beat B-panel reuse while preserving row ownership, row-major output, dtype/shape/error behavior, RNG absence, and per-output ascending-k accumulation.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed on same-worker `ts1`: restored-source baseline `97.070..100.57..105.75ms`; candidate rebench `103.03..105.24..107.35ms`; median speedup `0.9556x`, conservative speedup `0.9042x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_fullk_sweep_c3pgz_pass165_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `691778725b0b7ea8d8c59206c748b5ddd675c34c5b16438a259446959cd976be`).
- next primitive: stop threshold-style full-k routing for GEMM2048. The next GEMM swing should be a behavior-contract bead for FP-tolerant recursive or Strassen-style GEMM, or a fresh profile-backed pivot to the next linalg row if GEMM no longer dominates.

## pass166 -- fj-lax GEMM2048 recursive/Strassen tolerance contract (CONTRACT GATE, bead frankenjax-p1vbf.39)
- target `linalg/matmul_2d_2048x2048x2048_f64`, carried from pass165's fixed-worker profile: GEMM2048 `100.59..103.92..109.57ms` on `ts1`, still above pinv/LU/Cholesky/QR/eig rows.
- contract audit: recursive/cache-oblivious or Strassen-style GEMM would change the floating-point addition tree. Current direct matmul/DotGeneral evidence pins bit-exact ascending-k semantics: `matmul_2d_ikj_bit_identical_to_ijk`, special-value `to_bits` coverage for `-0.0`/`+0.0`/Inf/NaN, packed/large matmul bit identity, and `dot_general_rank2_matmul_f64_matches_row_major_ijk_bits`.
- decision: do not ship FP-reordered GEMM under the current direct matmul contract. `rtol`/`atol` are not admitted for this path without new explicit tolerance fixtures; NaN/signed-zero behavior remains the current `to_bits` contract.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_strassen_contract_p1vbf39_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `c5e72c5479ef631d937a7a48b4bd04e2d1d387e52c002cc17e511775fbf8afe0`).
- next primitive: attack `frankenjax-p1vbf.40`, a KC-major packed-B layout for the blocked GEMM path. It is still a communication-avoiding packed-panel primitive, but keeps per-output ascending-k accumulation and avoids the already rejected KC retune, NR16 widening, paired-panel grouping, row-slab A-pack tiling, and full-k threshold-routing families.

## pass167 -- fj-lax GEMM2048 KC-major B-pack layout (REJECTED, bead frankenjax-p1vbf.40)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Profile-backed current-source row from bead: GEMM2048 `483.42..536.80..582.49ms` on `vmi1293453`, ahead of Cholesky1024, LU1024, pinv256x128, QR1024, and eig48; focused pre-edit baseline on same-worker `ts1`: `114.33..116.07..117.70ms`.
- lever tested: pack B for the blocked GEMM path as `[pc-block][NR-panel][kc-row][NR-lane]` instead of full-k panel-major layout, so each pc sweep reads contiguous B panel chunks. This is a communication-avoiding packed-panel layout change, not a KC retune, NR widening, row-slab A-pack tweak, or full-k threshold route.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed on same-worker `ts1`: candidate rebench `131.21..137.82..146.30ms`; median speedup `0.8422x`, conservative speedup `0.7815x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_kcmajor_packedb_p1vbf40_pass167_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `53387b90f5ee6ca43579e3f099b817ee244bd0882cb2851d09eed24a41a591f2`).
- next primitive: stop blocked-GEMM B-pack layout retuning. Reprofile restored source and pivot to a different primitive; if GEMM still dominates, the next swing should change algorithmic contract/structure rather than repacking the same blocked B stream again.

## pass168 -- fj-lax GEMM2048 parallel B-panel packing (SHIPPED, bead frankenjax-ed0v4)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Restored-source RCH profile on `vmi1149989`: GEMM2048 `789.63..820.45..859.20ms`, ahead of Cholesky1024 `123.11..136.66..150.92ms`, LU1024 `106.73..114.17..119.60ms`, pinv256x128 `77.508..83.177..87.909ms`, QR1024 `57.789..59.582..61.306ms`, and eig48 `46.672..48.828..51.515ms`. Focused pre-edit baseline on `vmi1149989`: `593.69..634.13..683.95ms`.
- lever shipped: large B-panel packing now splits independent NR-panel ranges across the existing bounded matmul thread budget, while preserving the exact panel-major `[panel][k][NR]` bytes and all multiply loop order.
- proof: final-state RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed `9` focused tests with `1` ignored. Coverage includes direct parallel-pack layout bytes, packed/threaded/special-value/large/blocked bit identity, and packed NR8 golden digest.
- perf gate passed on same-worker `ts1`: restored-source baseline `105.49..109.98..114.79ms`; candidate `75.890..77.126..78.164ms`; median speedup `1.4260x`, conservative speedup `1.3496x`, Score `16.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ts1`; `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs`, `git diff --check`, golden sha256, and UBS passed. UBS exit 0 retained pre-existing file-wide warning inventory.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_parallel_bpack_ed0v4_pass168_2026-06-07.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile after landing; if GEMM remains top, continue with a deeper exact-order parallel/data-motion primitive or explicit contract work to unlock FP-tolerant recursive kernels.

## pass168 -- fj-lax pinv256x128 SVD reconstruction U-column layout (SHIPPED, bead frankenjax-p1vbf.41)
- target `linalg/pinv_256x128_f64`. Restored-source RCH `ts1` profile after pass167 rejection: GEMM2048 `112.43..117.02..123.28ms`, pinv256x128 `60.159..60.827..61.341ms`, Cholesky1024 `52.890..53.405..54.187ms`, LU1024 `49.931..51.101..52.691ms`, QR1024 `33.005..33.661..34.500ms`. This deliberately pivots away from the rejected blocked-GEMM B-pack family to the top non-GEMM linalg row.
- lever shipped: `pinv_svd` now transposes the already-computed row-major `U` from `one_sided_jacobi_svd_real` once into `u_cols[l*m + j]`, then reconstructs with contiguous U-column reads while preserving the existing `l -> i -> j` result update order.
- proof: RCH `cargo test -p fj-lax --lib pinv -- --nocapture` on `ts1` passed 8/8 focused pinv tests, including new `pinv_svd_column_major_reconstruction_matches_row_major_bits`, which reconstructs with the old row-major-U read and the new column-major-U read from the same SVD factors and asserts every `f64::to_bits` matches.
- perf gate passed on same-worker `ts1`: target baseline `64.889..66.864..68.778ms`; target rebench `58.658..59.129..59.744ms`; median speedup `1.1308x`, conservative speedup `1.0861x`, Score `6.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `ts1`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ts1`; `git diff --check`, evidence JSON validation, and golden sha256 passed. `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` still fails on pre-existing unrelated formatting outside this lever; `ubs crates/fj-lax/src/linalg.rs` still reports pre-existing file-wide inventory while fmt/clippy/check/test-build sections are clean and no unsafe blocks are reported.
- evidence: `artifacts/performance/evidence/fj_lax_pinv_svd_colmajor_p1vbf41_pass168_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `dfa3b668333d0dd4bc3c850452ef090c8da885fc5a1e0358a4584208f66eb6fb`).
- next primitive: reprofile after landing; if GEMM2048 remains top, route through a contract/structure change rather than more blocked B-pack retuning, otherwise attack the next fixed-worker factorization row.

## pass169 -- fj-lax GEMM2048 MR6 exact-order register microkernel (REJECTED, bead frankenjax-jwkcc)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Post-ed0v4 RCH profile on `ts1`: GEMM2048 `70.643..71.487..73.182ms`, pinv256x128 `55.932..57.285..58.896ms`, Cholesky1024 `51.784..52.308..52.943ms`, LU1024 `51.258..51.964..52.652ms`, QR1024 `34.705..37.398..43.781ms`, eig48 `31.189..32.363..34.002ms`. Focused pre-edit baseline on `ts1`: `73.159..73.665..74.577ms`.
- lever tested: widen the exact-order GEMM row tile from `MR=4` to `MR=6`, so each loaded B vector would be reused across six output rows while every output element still accumulated `l=0..k` in the same order.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `ts1` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed on same-worker `ts1`: candidate rebench `167.05..171.57..177.03ms`; median speedup `0.4294x`, conservative speedup `0.4133x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_mr6_jwkcc_pass169_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `54ad76991474125d263434e046a3af23753ebd375d3ea2598d632e891b83ae0f`).
- next primitive: stop row-tile widening. Reprofile restored source and attack a fundamentally different exact-order primitive, likely thread-pool amortization for GEMM packing/row dispatch or contract work to admit FP-tolerant recursive kernels.

## pass170 -- fj-lax pinv256 V-column reconstruction layout (REJECTED, bead frankenjax-7c19n)
- target `linalg/pinv_256x128_f64`. RCH pre-edit baseline on `vmi1149989`: `59.128..69.656..86.506ms`.
- lever tested: precompute the accepted V-column coefficients scaled by `1/sigma[l]` plus a nonzero flag before the existing `pinv_svd` `l -> i -> j` reconstruction update.
- proof before rejection: RCH `cargo test -p fj-lax --lib pinv -- --nocapture` on `vmi1264463` passed `8/8`, including the bit-level row-major-vs-U-column reconstruction test and low-rank/rank-deficient public pinv paths.
- perf gate failed on official same-worker `vmi1149989`: candidate `69.972..76.645..83.278ms`; median speedup `0.9088x`, conservative speedup `0.7100x`, Score `0.0`. A cross-worker `vmi1264463` diagnostic candidate `123.41..179.44..268.68ms` was not used for the gate.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_pinv_vcol_7c19n_pass170_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `6bd75a113af5bc24ea22e7714e9f93f072938cbb6c75626161654f54375d3b8c`).
- next primitive: stop pinv V-coefficient scratch retuning; reprofile restored source and route to a different profile-backed primitive, likely one-sided Jacobi SVD sweep structure or GEMM exact-order structural work.

## pass171 -- fj-lax pinv/SVD V rotation column-major workspace (SHIPPED, bead frankenjax-p1vbf.42)
- target `linalg/pinv_256x128_f64`. Restored-source RCH linalg route on `vmi1264463` after pass170 rejection: GEMM2048 `1.6109..2.0813..2.6471s`, LU1024 `375.40..503.01..634.53ms`, Cholesky1024 `327.12..424.47..524.13ms`, matmul1024 `201.36..252.30..310.27ms`, pinv256x128 `123.80..168.70..232.02ms`, QR1024 `103.75..113.11..124.82ms`, eig48 `77.500..83.590..92.396ms`. GEMM was still top, but exact-order GEMM micro/layout families were in a rejection streak, so this pass attacked the top non-repeat SVD private-layout hotspot.
- lever shipped: `one_sided_jacobi_svd_real` now stores the private Jacobi V rotation workspace column-major, so each p/q rotation streams V columns contiguously. The public sorted V remains materialized row-major at the function boundary.
- proof: RCH `cargo test -p fj-lax --lib one_sided_jacobi -- --nocapture` on `yto` passed `2/2`, including a new bit-exact row-major-V reference comparison for `sigma`, `U`, and `V`. RCH `cargo test -p fj-lax --lib pinv -- --nocapture` on `yto` passed `8/8` public pinv tests. Isomorphism: Jacobi sweep/pair order, W updates, V scalar equations, total_cmp singular ordering, cutoff behavior, public layouts, dtype/shape/error behavior, NaN/signed-zero surface, and RNG absence are preserved; only private V addressing changes.
- perf gate passed on same-worker `vmi1156319`: target baseline `103.55..109.40..114.51ms`; target rebench `88.632..91.554..95.843ms`; median speedup `1.1949x`, conservative speedup `1.0804x`, Score `6.0`.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `vmi1167313`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `vmi1149989`; `git diff --check`, evidence JSON validation, and golden sha256 passed. `rustfmt --edition 2024 --check crates/fj-lax/src/linalg.rs` still reports pre-existing unrelated formatting regions outside the new SVD hunk. `ubs crates/fj-lax/src/linalg.rs` ran with the known broad inventory; it reported no unsafe blocks and its fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_svd_vcol_p1vbf42_pass171_2026-06-07.json` (+ `.golden`, `.golden.sha256`, golden sha `f22ee4d941ac64a84f1cee00ef39b05a4de2ae76357e55a68a8c1bab0275727e`).
- next primitive: reprofile after landing; if GEMM exact-order remains blocked by rejection streak, route to a different profile-backed structural primitive instead of another B-pack/KC micro-retune.

## pass172 -- fj-lax Cholesky1024 lower-SYRK q-lane SIMD (REJECTED, bead frankenjax-ea94k)
- target `linalg/cholesky_1024x1024_f64`. Post-pass171 RCH linalg profile on `vmi1264463`: GEMM2048 `951.84ms..1.1738s..1.4279s`, Cholesky1024 `555.98..728.80..921.47ms`, LU1024 `431.45..549.35..675.51ms`, matmul1024 `157.84..193.82..230.26ms`, pinv256x128 `114.10..139.36..168.48ms`. GEMM remained top but exact-order GEMM micro/layout families were in a rejection streak, so this pass attacked the top structural non-GEMM row.
- lever tested: safe-Rust `std::simd` 4-lane/register-blocked q-side lower-SYRK dot update inside `cholesky_schur_update_lower_rows`, preserving p row order, q output order, c=0..jb accumulation order per lower-triangle entry, lower-only update surface, dtype/shape/error behavior, NaN/signed-zero propagation, and RNG absence.
- proof before rejection: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `vmi1149989` passed `14/14`, including `cholesky_lower_syrk_simd_matches_scalar_reference_bits`, which compared the candidate against the previous scalar q loop bit-for-bit.
- perf gate failed on same-worker `vmi1264463`: baseline `197.47..257.33..328.71ms`; candidate `329.20..444.44..550.60ms`; median speedup `0.5790x`, conservative speedup `0.3586x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty and `git diff --check` passed. Evidence: `artifacts/performance/evidence/fj_lax_cholesky_syrk_ea94k_pass172_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `a127311e260ca83b139c99db61a3d17ad1a1ce81e0ab563625a3e85512ffc3a7`).
- next primitive: stop q-lane SYRK vectorization. Attack lower-packed Cholesky workspace / recursive lower-triangle storage to reduce full-matrix bandwidth and cache footprint while preserving public row-major L materialization.

## pass173 -- fj-lax Cholesky1024 lower-packed workspace schedule (REJECTED, bead frankenjax-a7sf0)
- target `linalg/cholesky_1024x1024_f64`. Fresh RCH route on `vmi1264463`: GEMM2048 `1.7065..2.1276..2.5435s`, LU1024 `538.11..651.93..776.80ms`, Cholesky1024 `293.59..386.06..481.75ms`, QR1024 `142.30..166.33..192.69ms`, pinv256x128 `98.597..112.56..127.40ms`. GEMM remained top, but `frankenjax-p1vbf.39` still blocks recursive/Strassen FP-reordered GEMM under the current bit-exact ascending-k contract, so this pass attacked the next structural non-GEMM primitive named by pass172.
- lever tested: private lower-packed Cholesky workspace for `cholesky_real_blocked`, with packed lower indexing during diagonal factorization, panel solve, and lower-triangle Schur update, followed by public row-major L materialization.
- proof before rejection: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` on `vmi1149989` passed `13/13`, including packed-lower Schur update comparison against the previous row-major lower update and full-GEMM lower-triangle reference. The candidate preserved diagonal block order, panel row order, lower-triangle p/q traversal, c contribution order, dtype/shape/error behavior, RNG absence, and public row-major L output.
- perf gate failed on same-worker `vmi1149989`: restored-source baseline Cholesky1024 `52.388..57.753..68.652ms`; candidate `71.534..79.414..87.514ms`; median speedup `0.7272x`, conservative speedup `0.5986x`, Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_cholesky_packed_lower_a7sf0_pass173_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `be263e5f05ab24f0330e5e2d0c53258d81d310f69ee3df11cad0bf55cc72920e`).
- next primitive: stop private lower-packed Cholesky workspace as a lever. Reprofile restored source and attack a different structural primitive, such as communication-avoiding/recursive Cholesky panel scheduling with explicit bit-order proof, blocked LU/QR structure if it ranks higher, or a behavior-contract bead if the deeper algorithm requires FP reordering.

## pass173 confirmation -- fj-lax Cholesky1024 lower-packed workspace duplicate (REJECTED, bead frankenjax-p1vbf.43)
- relationship: duplicate confirmation of the pass173/a7sf0 lower-packed private Cholesky workspace lever. The source hunk had already been restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty.
- focused same-worker gate on `vmi1167313`: baseline `152.14..167.61..184.42ms`; candidate `152.08..172.46..196.71ms`; median speedup `0.971878x`; conservative speedup `0.773423x`; Score `0.0`.
- proof carried from candidate state: RCH `cargo test -p fj-lax --lib cholesky -- --nocapture` passed `13/13`, including packed-lower Schur update comparison against the previous row-major lower update and full-GEMM lower-triangle reference. Isomorphism preserved diagonal/panel/lower-SYRK order, public row-major lower output, strict upper zeros, dtype/shape/error behavior, signed-zero/NaN behavior, and RNG absence.
- decision: rejected and closed as duplicate confirmation; evidence: `artifacts/performance/evidence/fj_lax_cholesky_packed_lower_p1vbf43_pass173_confirm_2026-06-07.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `686ac8396f790bc6afa93991ec9801ca8dc24f7e039880e2ccd14d24271c2265`).
- next primitive: do not repeat private lower-packed Cholesky workspace; reprofile restored source and route to a different structural primitive or a behavior-contract bead for FP-reordered algorithms.

## pass174 -- fj-lax GEMM2048 SIMD slice-load microkernel (REJECTED, bead frankenjax-p1vbf.44)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Restored-source RCH route on `vmi1293453`: GEMM2048 `468.50..550.90..632.32ms`, Cholesky1024 `85.734..105.63..131.53ms`, LU1024 `59.685..62.918..65.495ms`, pinv256x128 `48.001..49.386..50.638ms`, QR1024 `39.287..40.409..41.749ms`, eig48 `28.833..32.775..35.874ms`.
- lever tested: replace manual `F64xN::from_array([brow[0], ... brow[7]])` construction from a contiguous B-panel slice with the standard safe `F64xN::from_slice(&values[..NR])` helper in the exact-order MR4/NR8 microkernel.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1293453` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage. Candidate preserved row ownership, row-major output, panel traversal, per-output ascending-k floating-point accumulation, dtype/shape/error behavior, NaN/signed-zero surface, and RNG absence.
- perf gate failed on same-worker `vmi1293453`: restored-source baseline `304.09..314.91..327.85ms`; candidate `346.34..365.91..387.50ms`; median speedup `0.8606x`; conservative speedup `0.7847x`; Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_simd_sliceload_p1vbf44_pass174_2026-06-08.rejected.json` (+ `.golden`, `.golden.sha256`).
- next primitive: stop GEMM SIMD lane-load microtuning. Reprofile restored source and route to a different structural primitive, such as explicit FP-tolerance contract work for recursive/communication-avoiding GEMM or a top-ranked LU/QR/Cholesky structural kernel if GEMM contract work is not yet ready.

## pass175 -- fj-lax GEMM2048 row-morsel blocked scheduling (REJECTED, bead frankenjax-p1vbf.45)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Restored-source RCH linalg profile on `fmd`: GEMM2048 `168.85..171.27..174.19ms`, Cholesky1024 `41.312..41.470..41.667ms`, pinv256x128 `38.121..38.190..38.252ms`, LU1024 `34.331..35.018..35.957ms`, QR1024 `28.583..29.069..29.450ms`, eig48 `22.759..22.918..23.035ms`.
- lever tested: apply an alien-graveyard polyhedral/morsel scheduling primitive by splitting each existing threaded blocked-GEMM row block into fixed 64-row cache-sized morsels, capping private A-pack working sets while preserving row ownership and per-output ascending-k accumulation.
- proof before rejection: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` on `vmi1167313` passed the focused matmul set (`9 passed`, `1 ignored`), including packed, threaded, special-value, large, blocked bit-identity, and packed NR8 golden digest coverage.
- perf gate failed on same-worker `fmd`: restored-source baseline `168.85..171.27..174.19ms`; candidate `188.44..192.50..196.89ms`; median speedup `0.8897x`; conservative speedup `0.8576x`; Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/tensor_contraction.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_gemm_row_morsels_p1vbf45_pass175_2026-06-08.rejected.json` (+ `.golden`, `.golden.sha256`).
- next primitive: stop exact-order GEMM row-morsel scheduling. Route the next GEMM work to explicit FP-tolerance/contract artifacts that can unlock recursive or communication-avoiding GEMM, or reprofile and attack a top non-GEMM BLAS/LAPACK structural primitive.

## pass176 -- fj-lax LU1024 Schur product buffer reuse (SHIPPED, bead frankenjax-p1vbf.46)
- target `linalg/lu_1024x1024_f64`. Initial restored-source baseline on `vmi1293453`: `48.585..50.424..51.712ms`. The paired same-worker gate used `fmd`: restored-source baseline `66.697..68.587..71.219ms`; candidate `35.000..35.327..35.696ms`; median speedup `1.941490x`; conservative speedup `1.868473x`; Score `16.0`. Exact final-source candidate anchor on `vmi1149989`: `46.662..49.510..52.172ms`.
- lever shipped: add `matmul_2d_into` and use it in blocked LU's Schur update so `lu_factor_real_blocked` reuses one `prod` buffer across panels instead of allocating a fresh `rows_below * cols_right` Vec for every panel.
- proof: final-source RCH `cargo test -p fj-lax --lib lu -- --nocapture` on `vmi1264463` passed `62/62`, including `lu_blocked_path_golden_output_digest`, `lu_blocked_path_reconstructs_and_matches_scalar`, and `lu_real_fast_path_bit_identical_to_complex_zero_imag_path`. Final-source RCH `cargo test -p fj-lax --lib matmul_2d_into -- --nocapture` on `vmi1293453` passed `1/1`, asserting every output `f64::to_bits` matches the allocating path.
- isomorphism: panel order, pivot scan/tie-breaking, row swaps, L11 solve order, Schur matmul plan/arithmetic order, trailing subtraction order, dtype/shape/error behavior, NaN/signed-zero surface, and RNG absence are preserved.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `vmi1149989`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `vmi1149989`; `rustfmt --edition 2024 --check crates/fj-lax/src/tensor_contraction.rs` and `git diff --check` passed. Full-file `rustfmt` on `linalg.rs` still reports pre-existing unrelated formatting outside this lever; UBS still reports the known broad inventory while no unsafe blocks were found and its fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_lu_schur_product_reuse_p1vbf46_pass176_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `67fef34cbc2eb697b048e1df3d0718f9b491b7d191172ac8abdd6def2ad66ff6`).
- next primitive: reprofile after landing. If GEMM remains top, route GEMM to explicit FP-tolerance/contract artifacts for recursive/communication-avoiding kernels; otherwise attack the next ranked BLAS/LAPACK structural row with a fundamentally different primitive.

## pass177 -- fj-lax OneHot2048 repeated-patches sparse output (SHIPPED, bead frankenjax-g5u6u)
- target `eval/one_hot_2048x512_f64`. Same-worker RCH baseline on `vmi1153651`: `579.48..597.99..614.23us`; final candidate on `vmi1153651`: `25.052..26.095..26.956us`. Speedup is `22.9159x` midpoint and `21.4973x` conservative; Score `25.0`.
- lever shipped: construct F64 one-hot output as `LiteralBuffer::from_repeated_with_patches(vec![off], total, patches)` plus `TensorValue::new_with_literal_buffer` so the mostly-off output is lazy materialized while preserving public literal order and exact on/off bits. Other dtypes keep the existing dense/literal fill+scatter paths.
- isomorphism: patch generation enumerates input flat indices in the same order as `one_hot_scatter`, uses the same input-stride to output-stride mapping, leaves out-of-range rows all-off, and preserves duplicate-patch last-write semantics through `LiteralBuffer` materialization order. No arithmetic is added; on/off are exactly `literal_for`, preserving `-0.0`, infinities, NaN payload surface, dtype/shape/error behavior, tie-breaking, and RNG absence.
- proof: RCH `cargo test -p fj-lax --lib one_hot_f64_repeated_patches_materialize_like_dense_scatter -- --nocapture` passed on `vmi1293453`; RCH `cargo check -p fj-lax --all-targets` passed on `vmi1149989`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `vmi1152480`; `cargo fmt --check --package fj-lax`, `git diff --check`, and golden checksum passed. UBS was run on changed files and still reports the known broad fj-lax inventory, while its fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_one_hot_repeated_patches_g5u6u_pass177_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `170fda3787441a07736677f824d2d825854df0b9aed532244a90606e299c72b4`).
- next primitive: reprofile after landing and pick the next ready perf bead; peer work has already landed the dense-F32 unary follow-up that appeared during this pass.

## pass178 -- fj-lax Cumsum4M F64 direct-output scan (SHIPPED, bead frankenjax-mcqr.30.37)
- target `eval/cumsum_4m_f64_1d`. Clean-worktree RCH baseline on `vmi1152480`: `25.072..25.818..26.655ms`; final candidate on `vmi1152480`: `23.865..24.125..24.308ms`. Speedup is `1.07018x` midpoint and `1.03143x` conservative; Score `7.5`.
- lever shipped: for large dense F64 contiguous forward cumulative scans, use the existing direct-output `scan_contiguous_lines_to_vec` builder even when there is only one scan line. This removes the full `src.to_vec()` clone before the in-place prefix overwrite. Reverse one-line, small-scan, strided-axis, and non-F64 paths stay unchanged.
- isomorphism: the target still uses one f64 accumulator initialized to `float_init`, visits each line in ascending element order, applies the same `float_op`, and stores each prefix immediately after that operation. No reassociation, parallel prefix, FMA request, mixed precision, tie-breaking, or RNG is introduced.
- proof: RCH `cargo test -p fj-lax cumulative -- --nocapture` passed on `vmi1152480` (`4 passed`, including dense cumulative bit-identity and threaded cumulative reference tests); RCH `cargo check -p fj-lax --all-targets` and `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `vmi1152480`; `cargo fmt --check --package fj-lax`, `git diff --check`, JSON parse, and golden checksum passed. UBS was run on changed files and exits nonzero from pre-existing broad `reduction.rs` inventory, while its fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_cumsum_direct_output_mcqr30_37_pass178_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `71fc096b503aa211618f64789e32694bd04ddfa51925377627dd8a1dc678e962`).
- next primitive: reprofile after landing. If `cumsum_4m_f64_1d` remains the top row, route to an exact-prefix math artifact for order-preserving parallel prefix; otherwise attack the next profile row, likely dense F64 axis reductions or broadcast data movement.

## pass179 -- fj-lax Cumsum4M F64 direct-add specialization (REJECTED, bead frankenjax-4ywuo)
- target `eval/cumsum_4m_f64_1d`. Same-worker RCH baseline on `fmd`: `24.039..24.142..24.234ms`; candidate direct-add specialization on `fmd`: `24.437..24.774..25.168ms`.
- lever tested: pass `Primitive` into the dense cumulative helper and route large forward dense F64 Cumsum through a direct `acc += value` scan instead of the generic `float_op` closure.
- proof before rejection: the candidate preserved the same ordered recurrence, no reassociation, parallel prefix, FMA request, mixed precision, RNG, or tie-breaking; it failed the performance gate.
- decision: rejected and production hunk removed. Remaining source diff is only `large_dense_f64_cumsum_single_line_matches_literal_path`, which locks the existing pass178 direct-output path against the literal path by F64 bits.
- validation: RCH `cargo test -p fj-lax cumulative -- --nocapture` passed on `fmd` (`4 passed`); RCH exact new test passed (`1 passed`); RCH `cargo check -p fj-lax --all-targets` passed; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed after removal; `cargo fmt --check --package fj-lax`, `git diff --check`, JSON parse, and golden checksum passed. UBS still reports broad pre-existing `reduction.rs` inventory while fmt, clippy, check, and test-build sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_cumsum_direct_add_4ywuo_pass179_2026-06-08.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `411e569a97941b559bc8618d7d7bb14826beee5d156084a117a94239c1bb36ff`).
- next primitive: close `frankenjax-4ywuo` as rejected and reprofile. If one-line Cumsum remains top, file a behavior-contract bead for tolerance-based or FP-reordered parallel prefix before trying tree or segmented scans; otherwise attack the next ready profile-backed perf bead.

## pass181 -- fj-lax GEMM2048 skip output prefill (SHIPPED, bead frankenjax-lqjov)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh pinned RCH `fmd` linalg profile after pass179 closeout ranked GEMM2048 first at `163.79..165.25..171.07ms`, far above pinv/lu/qr/cholesky rows.
- lever shipped: `matmul_2d_with_threads_into` no longer clears the entire output buffer before nonzero-k GEMM. Full tile paths already write from explicit zero accumulators; the only `+=` row-remainder path now locally zeroes its row before its unchanged l-major loop.
- same-worker RCH `fmd` gate: baseline `169.62..176.69..178.46ms`; candidate `157.99..158.03..158.17ms`; median speedup `1.11808x`; conservative speedup `1.07239x`; Score `8.0`.
- isomorphism: per-output ascending-k accumulation is unchanged; KC first-block zero seeding is unchanged; row remainder still starts at zero and accumulates in the same l order; `k == 0` still returns zero-filled output; thread row ownership, B-pack layout, tile traversal, dtype/shape/error behavior, signed-zero/NaN surface, and RNG absence are unchanged.
- proof: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` passed on `fmd` (`11 passed`, `1 ignored`), including `matmul_2d_into_matches_allocating_path`, blocked/packed/threaded bit-identity, special-value bit-identity, and the packed NR8 golden digest.
- validation: RCH `cargo check -p fj-lax --all-targets` passed on `fmd`; RCH `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `fmd`; `cargo fmt --check --package fj-lax` and `git diff --check` passed. `ubs crates/fj-lax/src/tensor_contraction.rs` exited 0; it reports no criticals/no unsafe and clean fmt/clippy/check/test-build sections, with the existing broad warning inventory still present.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_skip_output_prefill_lqjov_pass181_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `7a9c7f647c9ad35f8eb38c10481922c5b71e80c763b5fb6af1dc0163d85b775c`).
- next primitive: reprofile after landing. If GEMM remains top, route to a deeper communication-avoiding or tolerance-contract primitive instead of repeating row-tile/MR-width microlevers.

## pass182 -- fj-lax GEMM2048 recursive/CA tolerance contract (REJECTED ROUTE, bead frankenjax-6ykcu)
- target route: post-pass181 pinned RCH `fmd` profile on current main `05b61cd6` kept `linalg/matmul_2d_2048x2048x2048_f64` dominant at `170.08..170.79..173.66ms`, followed by pinv256 `37.359..37.560..38.365ms`, LU1024 `35.507..35.784..36.892ms`, QR1024 `27.011..27.028..27.096ms`, matmul1024 `22.446..22.449..22.459ms`, and eig48 `22.087..22.091..22.107ms`.
- route checked before source edits: recursive/cache-oblivious/communication-avoiding GEMM would reorder floating-point accumulation. Current local `Dot`/`DotGeneral`/`matmul_2d` tests and fast-path contract assert bit-exact ascending-k output for GEMM.
- decision: rejected the route without production source edits. Closed `frankenjax-6ykcu`; do not implement FP-reordered GEMM until a separate public tolerance contract is proven and accepted.
- next primitive: continue attacking exact-order structural GEMM cache/panel layout, or move to the next profile row if GEMM exact-order levers fail the Score gate.

## pass183 -- fj-lax GEMM2048 PC-major blocked B-pack layout (SHIPPED, bead frankenjax-mcqr.58)
- target `linalg/matmul_2d_2048x2048x2048_f64`. Fresh same-worker RCH `fmd` baseline before edits: `177.27..178.28..179.43ms`.
- lever shipped: the k-blocked GEMM path now packs B as `[pc-block][column-panel][kc][NR]`, so each pc slab keeps adjacent column panels contiguous (`kc*NR` stride) instead of jumping by full `k*NR`. The existing flat packed path stays on the original panel-major `[column-panel][k][NR]` layout.
- same-worker RCH `fmd` gate: baseline `177.27..178.28..179.43ms`; candidate `172.03..174.25..176.59ms`; median speedup `1.023128x`; conservative speedup `1.003851x`; intervals non-overlap (`176.59ms < 177.27ms`); Score `3.0`.
- isomorphism: B pack changes only read addresses. The blocked loop order remains pc -> row-superpanel -> column-superpanel -> tile -> jp -> l, each output still accumulates strictly ascending k/l, first-block zero seeding and running-C loads are unchanged, and row ownership, row-major output, FP bits/signed-zero/NaN, dtype/shape/error behavior, and RNG absence are preserved.
- proof: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` passed on `fmd` (`11 passed`, `1 ignored`), including blocked/packed/threaded bit-identity and golden digest. RCH `cargo test -p fj-lax --lib pack_b_pc_panels -- --nocapture` passed on `fmd` (`2 passed`), covering pc-major source mapping and serial-vs-parallel packed-B bit equality.
- validation: RCH `cargo check -p fj-lax --all-targets` and `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `fmd`; `cargo fmt --check --package fj-lax`, `git diff --check`, golden checksum, and UBS passed. UBS still inventories broad pre-existing warnings in `tensor_contraction.rs`, while no criticals/no unsafe and its fmt/clippy/check/test-build sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_pcmajor_bpack_mcqr58_pass183_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `a27213aecb8d6280f8cca7d5240405e38f338007b3d3c678734f832c18c5a672`; golden output digest `e3762befad86e2a81da53a8413643b658a0be2d6136d69e195770b2beba48b3a`).
- next primitive: reprofile after landing. If GEMM remains top, attack another structurally different exact-order panel/cache primitive; any FP-reordered recursive/CA/Strassen route still needs a proven public tolerance contract before source edits.

## pass184 -- fj-lax pinv256 direct U-column SVD materialization (REJECTED, bead frankenjax-x6y94)
- target `linalg/pinv_256x128_f64`. Detached current-HEAD baseline on RCH `fmd`: `38.927..39.008..39.028ms`.
- lever tested: add a private Jacobi SVD U-output layout selector so `pinv_svd` can receive U columns directly and skip the row-major-to-column-major transpose. Public SVD stayed row-major; Jacobi sweep/order and pinv result recurrence stayed unchanged.
- perf gate failed on the same worker `fmd`: candidate `39.453..39.543..39.566ms`; median speedup `0.986497x`; conservative speedup `0.984519x`; Score `0.0`.
- decision: rejected and source hunks restored; `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_pinv_direct_u_columns_x6y94_pass184_2026-06-08.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `4d3a6362481603ecc5f9c2d3053a42547fc405d2985122aa6221067e34bedcde`).
- proof after restoration: RCH `cargo test -p fj-lax --lib pinv -- --nocapture` passed on `fmd` (`8 passed`), including `pinv_svd_column_major_reconstruction_matches_row_major_bits`; `git diff --check`, JSON parse, and golden checksum passed.
- next primitive: stop SVD U-transpose/materialization micro-levers. Reprofile restored source and route to a different structural BLAS/LAPACK primitive, such as a blocked/communication-avoiding SVD artifact with an explicit behavior contract, or the next ranked LU/QR panel hotspot.

## pass185 -- fj-lax QR1024 Householder column tile width (SHIPPED, bead frankenjax-uh7ar)
- target `linalg/qr_1024x1024_f64`. Fresh RCH `fmd` linalg profile after pass184 closeout ranked QR1024 at `26.804..26.872..27.145ms`, after GEMM2048, pinv256, and LU1024.
- lever shipped: widen `QR_REFLECTOR_COL_TILE` from 8 to 16 so `apply_real_householder_columns` handles twice as many adjacent columns per row scan.
- same-worker RCH `fmd` gate: baseline `26.804..26.872..27.145ms`; candidate `25.899..26.353..26.466ms`; median speedup `1.019694x`; conservative speedup `1.012771x`; intervals non-overlap; Score `3.0`.
- isomorphism: each output column still accumulates dot products over `v` in ascending row order, multiplies by `tau` once, and applies row updates in ascending row order. Only the adjacent-column tile width changes. QR reflector creation, Q reverse application, diagonal phase normalization, R lower-zeroing, dtype/shape/error behavior, signed-zero/NaN behavior, and RNG absence are unchanged.
- proof: RCH `cargo test -p fj-lax --lib qr -- --nocapture` passed on `fmd` (`26 passed`, `1 ignored`), including tiled reflector bit identity and real-vs-complex-zero QR goldens. RCH `cargo check -p fj-lax --all-targets` and `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `fmd`; `cargo fmt --check --package fj-lax`, `git diff --check`, JSON parse, and golden checksum passed. UBS still reports the known broad `linalg.rs` inventory, while no unsafe blocks were found and its fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_qr_householder_tile_uh7ar_pass185_2026-06-08.json` (+ `.golden`, `.golden.sha256`, golden sha `972018ac56de40c73d4136ffbb81c47af5f2f6c49bae3e8a7f2149a2553ac204`).
- next primitive: reprofile after landing. If GEMM remains top, route to another exact-order structural cache primitive or the required FP-tolerance contract for recursive/communication-avoiding GEMM; otherwise attack the next ranked LU/SVD structural primitive.

## pass188 -- fj-lax QR-preconditioned DGESVJ SVD route (REJECTED, bead frankenjax-poyvi.1)
- target route: `frankenjax-poyvi.1`, the full QR-preconditioned DGESVJ follow-up after the kept bounded norm-order pass187. Fresh RCH `ovh-a` linalg profile on current source ranked GEMM2048 first at `185.30..185.54..186.52ms`; the relevant poyvi.1 public row was tall SVD-backed `linalg/pinv_256x128_f64` at `38.232..38.466..39.405ms`. Square `linalg/svd_48x48_f64` was `1.0857..1.0883..1.0984ms` and is not affected by a tall QR-preprocess guard.
- lever tested/evaluated: rescue the orphan `a00dc114` QR-preconditioned one-sided Jacobi SVD idea, factoring `A = Q R`, running the existing one-sided Jacobi SVD on `R`, then mapping left singular vectors back through `Q`.
- proof before rejection: RCH `cargo test -p fj-lax --lib qr_preconditioned_svd_real_preserves_tall_contract -- --nocapture` passed on `ovh-a` (`1 passed`), covering singular-value tolerance vs the existing Jacobi reference, deterministic repeat bits for U/S/V, reconstruction tolerance, and U/V orthogonality.
- perf/reuse gate failed: the fresh candidate diagnostic on `vmi1227854` measured `linalg/pinv_256x128_f64` at `47.292..47.713..49.397ms`, and the existing same-family same-worker pass186 full-rank gate already rejected QR preprocessing on `ovh-a`: direct SVD `24.878..24.893..24.897ms` vs QR candidate `26.077..26.119..26.129ms` (`0.953060990x` median, `0.952122163x` conservative). Score `0.0`.
- decision: rejected; production source restored to HEAD and `git diff -- crates/fj-lax/src/linalg.rs` is empty. Evidence: `artifacts/performance/evidence/fj_lax_qr_preconditioned_svd_poyvi1_pass188_2026-06-09.rejected.json` (+ `.golden`, `.golden.sha256`, golden sha `aadec6cebad905ac9d76101332145126fcf3d2fce10128fd5d3fa671277594af`).
- next primitive: do not repeat QR-preprocess-at-2n for pinv/full-rank or square SVD. Route to the next ready profile-backed bead; if SVD returns as a top hotspot, file a fundamentally different SVD-core primitive such as blocked one-sided Jacobi sweeps or Golub-Kahan bidiagonalization with an explicit tolerance contract.

## pass189 -- fj-lax reduce_window monotonic deque max/min (SHIPPED, bead frankenjax-yax7q.1)
- target route: follow-up to `frankenjax-yax7q` after its separable-scan closeout. The existing helper had already avoided the full 2D stencil but still rescanned each 1D output window.
- lever shipped: replace the per-axis 1D window scan in `reduce_window_separable_maxmin` with a monotonic-deque line pass. NaN positions are tracked separately; signed-zero preference matches `f64::max`/`f64::min`.
- same-worker RCH `ovh-a` gate: current separable scan baseline `eval/maxpool_256x256_15x15_separable` `2.7291..2.7303..2.7350ms`; candidate `0.76366..0.78413..0.78924ms`; median speedup `3.482x`; conservative speedup `3.458x`. Direct reference after row was `6.1728..6.1745..6.1749ms`, so deque is `7.875x` faster than the full-window direct reference midpoint. Score `12.5`.
- isomorphism: output order and dispatch gate are unchanged. Routed reductions are max/min only; sum, small windows, dilated windows, non-dense inputs, and non-float outputs stay on prior paths. Dense deque output is bit-identical to boxed generic reduce_window for rank4/rank2, VALID/SAME, stride1/2, max/min, canonical NaN, infinities, and signed zeros. No RNG or tie-index semantics are touched.
- proof: RCH focused `reduce_window_separable_maxmin_matches_generic` passed on `ovh-a`; RCH `cargo test -p fj-lax --lib reduce_window -- --nocapture` passed on `vmi1227854` (`35 passed`). RCH `cargo check -p fj-lax --all-targets` and `cargo clippy -p fj-lax --all-targets -- -D warnings` passed on `ovh-a`; `cargo fmt --package fj-lax -- --check`, `git diff --check`, JSON parse, and golden checksum passed. UBS over touched files exited nonzero on the known broad pre-existing large-file inventory; fmt/clippy/check/test-build sections were clean.
- evidence: `artifacts/performance/evidence/fj_lax_reduce_window_monotonic_deque_yax7q_pass189_2026-06-09.json` (+ `.golden`, `.golden.sha256`, golden sha `8e7ee68bf5d12b2a99159ec85b9a59418f3e414f1fdbb00fc9fdf719158a0306`).
- next primitive: reprofile after landing and select the next ready `[perf]` bead; if reduce_window remains visible, attack a different surface such as larger-rank memory layout or dense F32 storage rather than retuning the deque.

## pass190 -- fj-lax GEMM2048 row-contiguous pc B-pack ingest (SHIPPED, bead frankenjax-zn7ey)
- target `linalg/matmul_2d_2048x2048x2048_f64`. The active bead's post-pass189 RCH profile ranked GEMM2048 first at `196.38..197.63..202.60ms`, ahead of pinv, LU, matmul1024, and QR.
- lever shipped: keep the existing pc-major packed-B layout but fill it by scanning each source B row once and writing every NR panel for that row, instead of packing one column panel at a time. The packed index for each value is unchanged: `jp * (kc * NR) + l * NR + lane`.
- same-worker RCH `vmi1227854` gate: restored old pack-loop baseline `285.35..300.61..317.35ms`; candidate `223.72..230.74..237.98ms`; median speedup `1.302852x`; conservative speedup `1.199092x`; Score `8.0`.
- isomorphism: only B-pack source traversal changed. The packed layout, pc/tile traversal, thread row ownership, output row-major layout, first-block C zeroing, and per-output ascending-k accumulation are unchanged. No FMA request, mixed precision, reassociation, Strassen recursion, tolerance relaxation, RNG, tie-breaking, dtype, shape, or error behavior changed.
- proof: RCH `cargo test -p fj-lax --lib matmul_2d -- --nocapture` passed on `ovh-a` (`13 passed`, `2 ignored`), including the packed NR8 golden digest `e3762befad86e2a81da53a8413643b658a0be2d6136d69e195770b2beba48b3a`; RCH `cargo test -p fj-lax --lib pack_b_pc_panels -- --nocapture` passed on `ovh-a` (`2 passed`); RCH `cargo check -p fj-lax --all-targets` passed on `ovh-a`; `cargo fmt --check --package fj-lax`, `git diff --check`, and UBS over `tensor_contraction.rs` passed. RCH clippy was attempted and failed only because an uncommitted peer-owned `arithmetic.rs` WIP imports `rank2_i64_matmul`; the committed pass stages only the B-pack hunk.
- evidence: `artifacts/performance/evidence/fj_lax_gemm_pc_pack_row_ingest_zn7ey_pass190_2026-06-09.json` (+ `.golden`, `.golden.sha256`; golden output digest `e3762befad86e2a81da53a8413643b658a0be2d6136d69e195770b2beba48b3a`).
- next primitive: if GEMM remains top, use the read-only subagent route for a KC-slab streaming B-pack primitive, or file the separate tolerance-contract bead needed for recursive/communication-avoiding GEMM. Do not repeat the rejected row-morsel scheduling family.

## pass191 -- fj-lax pinv256 low-rank residual cutoff certificate (SHIPPED, bead frankenjax-p1vbf.52)
- target `linalg/pinv_256x128_f64`. Post-v58oy clean-origin RCH linalg profile on `ovh-a` ranked GEMM2048 first at `223.77..239.41..256.39ms`, but the KC-slab streaming B-pack route had just failed; this pass deliberately pivoted to the top non-repeat linalg row, pinv256 at `42.173..44.075..46.095ms`.
- lever shipped: `PINV_LOW_RANK_RESIDUAL_MARGIN` changed from `0.5` to `1.0`, so the existing finite tall-matrix low-rank QR certificate accepts when unresolved residual energy is at or below the same squared singular-value cutoff used by the pseudoinverse.
- same-worker RCH `ovh-a` gate: focused baseline `38.155..38.302..38.513ms`; candidate `5.8039..5.8086..5.8140ms`; median speedup `6.594015770x`; conservative speedup `6.562607499x`; Score `16.0`.
- isomorphism: public shape, dtype, layout, rcond, non-finite/rcond-zero fallback, deterministic pivot scan, strict greater-than tie behavior, RNG absence, and error surfaces are unchanged. The profile matrix is numerically rank 4; the old margin over-certified below-cutoff directions to rank 11, while the new certificate stops at rank 4 under the existing cutoff proof.
- proof: RCH `cargo test -p fj-lax --lib pinv -- --nocapture` passed (`9 passed`), including the new `pinv_low_rank_profile_shape_golden_digest` locked to `aa6633cd1c02444eca8fa95d72870710cafc7dbc9503e9c4056640f3118d492f`. RCH-wrapped `cargo check -p fj-lax --all-targets` and `cargo clippy -p fj-lax --all-targets -- -D warnings` passed via local fallback when workers were unavailable; `cargo fmt --check --package fj-lax`, `git diff --check`, JSON parse, and golden checksum passed. UBS over `linalg.rs` remains nonzero on known broad file-wide inventory while its fmt/check/clippy/test sections are clean.
- evidence: `artifacts/performance/evidence/fj_lax_pinv_low_rank_residual_margin_p1vbf_pass191_2026-06-09.json` (+ `.golden`, `.golden.sha256`; golden file sha `dea8f561204a56060a0453eccb838bf204a9b0ce8ca733374d8b32cb56176224`).
- next primitive: reprofile after landing. If GEMM remains dominant, route to a fundamentally different approved primitive instead of another exact-order B-pack lifetime tweak; otherwise attack the next ranked LU/QR/SVD structural hotspot.

## pass192 -- fj-dispatch f32 while exact-integer closed form (SHIPPED)
- target `vmap_while/batched_scalar_f32_add_128`. Prior dispatch profile and focused Criterion runs showed it remained a residual hot row after the direct f32 while path.
- lever shipped: `batch_while_f32_add_lt_batch0` now solves exact-integer finite f32 Add/Lt lanes in closed form and falls back to the previous repeated-add loop for fractional, NaN, infinite, non-positive-step, out-of-exact-range, or `max_iter`-sensitive cases.
- same-worker RCH keep gate on `vmi1227854`: baseline `9.0930..9.9293..10.966us`; candidate `5.8302..5.9885..6.1917us`; median speedup `1.658x`; Score `3.15`.
- final current-base corroboration: `rch exec -- cargo bench -p fj-dispatch --bench dispatch_baseline -- vmap_while/batched_scalar_f32_add_128` fell open locally because no admissible workers were available and measured `5.8207..5.8766..5.9384us`.
- isomorphism: lane order, output shape/dtype, dense f32 storage, `body_op=add`, `cond_op=lt`, max-iteration error boundary, no-op lanes, fallback/error surfaces, and RNG absence are preserved. Accepted lanes are exact f32 integers in `[-16777216,16777216]`, so closed-form addition produces the same final f32 bits as repeated f32 addition.
- proof: golden output digest `e3de78209337533170c435223a5d2ddccd83a3f299933f379ceebccfa64409e8`; focused tests pass; `cargo check -p fj-dispatch --all-targets` passes through `rch exec`; direct clippy/fmt remain blocked by pre-existing `fj-trace`/`fj-lax`/`fj-dispatch` debt outside this lever; UBS reports no critical findings on the changed file.
- evidence: `artifacts/performance/evidence/frankenjax-boldfalcon-fj-dispatch-while-f32-exact-integer.md`.
- next primitive: reprofile `fj-dispatch` after landing; do not repeat the already-rejected gather/scatter/axis-1 micro-lever family.

## pass193 -- fj-api prepared dispatch metadata guard/closeout (SHIPPED, bead frankenjax-1yn9y)
- target `value_and_grad_runtime/shared/deep_100_nodes`. The bead's profile on commit `7febbeca` had `_int_malloc` 15.45%, `verify_transform_composition_parts` 14.90%, `forward_with_tape` 11.52%, `backward` 10.09%, and SHA-256 digest blocks 7.22%.
- lever shipped: `ValueAndGradWrapped` reuses args-independent dispatch metadata across repeated calls, while `PreparedDispatchMeta` now carries the exact static-input fingerprint (mode, Jaxpr fingerprint, transform stack/evidence, backend, compile options, custom hook, unknown features) and `dispatch_ref` rejects mismatched prepared metadata before execution.
- comparable RCH gate: all runs fell open through the same local fallback path because no admissible workers were available. Baseline `HEAD^` `value_and_grad_runtime/shared/deep_100_nodes` midpoint `71.237us`; guarded candidate midpoint `43.520us`; speedup `1.636x`; Score `8.0`.
- supporting same-binary A/B: `prepared_dispatch_metadata/recompute/deep_100_nodes` midpoint `64.695us`; `prepared/deep_100_nodes` midpoint `52.459us`; speedup `1.233x`.
- isomorphism: execution still uses the live request Jaxpr, transform stack, args, backend, and compile options. Ordering/tie-breaking/FP/RNG behavior are untouched. Cache-key and evidence semantics are preserved by rejecting stale prepared metadata; strict unknown incompatible features remain fail-closed.
- proof: `cargo test -p fj-dispatch prepared_metadata -- --nocapture` passed (`3 passed`); `cargo check -p fj-dispatch --all-targets` and `cargo check -p fj-api --all-targets` passed; scoped clippy with `RUSTFLAGS="--cap-lints allow"` passed for `fj-dispatch` and `fj-api`; `git diff --check`, JSON parse, and golden checksum passed. Uncapped clippy remains blocked by pre-existing dependency-crate lint debt in `fj-trace`, `fj-lax`, `fj-interpreters`, and `fj-ad`.
- evidence: `artifacts/performance/evidence/fj_api_prepared_dispatch_meta_1yn9y_pass193_2026-06-09.json` (+ `.golden`, `.golden.sha256`; golden sha `709c5821c874b7e566a07a00767b053118fc1ec16a2ccf5489c52a56d6755162`).
- next primitive: reprofile after closeout and pick the next ready `[perf]` bead. Avoid `fj-lax` dense/linalg surfaces currently owned by IcyGlacier.

## pass194 -- fj-ad tape inline storage (SHIPPED, bead frankenjax-ow11d)
- target `value_and_grad_runtime/shared/deep_100_nodes`. Fresh current-base profile after pass193 captured 8741 samples and ranked `_int_malloc` 18.84%, `fj_ad::forward_with_tape` 16.25%, `fj_ad::backward` 11.85%, `AdValueStore::for_jaxpr` 3.92%, `malloc` 3.85%, and `malloc_consolidate` 3.79%.
- lever shipped: `TapeEntry` now stores per-equation input/output VarIds and input/output Values in `SmallVec<[T; 2]>`, and the multi-output backward scratch uses the same inline storage. Arity >2 still spills through SmallVec's existing heap fallback.
- comparable gate: both baseline and candidate ran through `rch exec` local fallback after the same RCH remote-preflight failure on clean worktrees at current base `b0ba6372`. Baseline `42.367us` midpoint; candidate `39.763us` midpoint; median speedup `1.065x`, conservative speedup `1.049x`; Score `4.0`.
- isomorphism: tape entry order, input/output VarId order, reverse traversal, VJP calls, FP arithmetic, RNG absence, fallback behavior, and error surfaces are unchanged. Only the small container storage strategy changed.
- proof: `cargo test -p fj-ad scalar_f64_forward_tape_matches_eval_primitive_bits -- --nocapture` and `cargo test -p fj-ad grad_sum_x2_plus_x_1k_golden_sha256 -- --nocapture` passed; golden digest `5282853e2bd187c1c1bfdfa612bd74776fb403e6b767eb0a8bf0c8bcd2fe2a19`. `cargo fmt --check --package fj-ad`, `cargo check -p fj-ad --all-targets`, scoped clippy with `RUSTFLAGS="--cap-lints allow"`, `git diff --check`, JSON parse, and golden checksum passed. UBS over `fj-ad/src/lib.rs` remains nonzero on pre-existing file-wide inventory, while its fmt/check/clippy/test-build/audit/deny subchecks were clean.
- evidence: `artifacts/performance/evidence/fj_ad_tape_inline_storage_pass194_2026-06-09.json` (+ `.golden`, `.golden.sha256`; golden file sha `43f4d45a6ab3efc469849412e1dd838d0e73fd1b50895e66b2b44b0b8de7349f`).
- next primitive: reprofile `value_and_grad`/`fj-ad` again; if allocation remains dominant, avoid another SmallVec micro-lever and route to an arena/compiled-tape primitive or a different top row.

## pass195 -- fj-ad scalar F64 Add/Mul reverse fast path (SHIPPED, bead frankenjax-f2s2l)
- target `value_and_grad_runtime/shared/deep_100_nodes`. Fresh current-base profile after pass193 ranked `_int_malloc` 17.90%, `fj_ad::forward_with_tape` 16.82%, `fj_ad::backward` 11.83%, `AdValueStore::for_jaxpr` 4.41%, and `Vec<Option<Value>>::extend_with` 1.99%.
- lever shipped: guarded scalar-F64 Add/Mul `value_and_grad` path evaluates primals and reverse adjoints in dense `Vec<Option<f64>>` slots, bypassing per-equation `Value`/`TapeEntry` allocation for param-free, effect-free, single-output scalar Add/Mul Jaxprs with built-in VJPs.
- comparable gate: clean detached worktrees at current base `b0ba6372`, both invoked through `rch exec` and both fell open locally after the same `vmi1153651` topology preflight failure. Baseline `44.257..44.812..45.318us`; candidate `5.0334..5.0885..5.1676us`; median speedup `8.806x`, conservative interval speedup `8.565x`; Score `12.5`.
- isomorphism: guard rejects custom primitive/Jaxpr VJPs, constvars, non-scalar/non-F64 args, non-Add/Mul primitives, params, sub-Jaxprs, effects, multi-output equations, and sparse VarId layouts. Forward equation order, reverse equation order, Add cotangent accumulation order, repeated-Mul cotangent order, IEEE F64 operation order, signed-zero zero-skip behavior, fallback/error surfaces, and RNG absence are preserved.
- proof: full `cargo test -p fj-ad --lib` passed (`364 passed`), including `scalar_f64_add_mul_fast_path_matches_tape_reverse_bits`, `value_and_grad_deep_scalar_add_mul_golden_sha256`, and the existing `grad_sum_x2_plus_x_1k_golden_sha256`. Golden fixture JSON digest is `2b1b03372322b4e6042ed0af95c4bfe87c0e15c25e9621c28a2da846689a9e25` for `["7ff0000000000000","7ff0000000000000"]`. `rustfmt --edition 2024 --check crates/fj-ad/src/lib.rs`, `cargo check -p fj-ad --all-targets`, and scoped clippy with only known pre-existing dependency/file lint classes allowed passed. Unadjusted clippy remains blocked by pre-existing `fj-trace`/`fj-lax` lint debt; UBS over `fj-ad/src/lib.rs` remains nonzero on broad pre-existing file inventory while its fmt/check/clippy/test-build/audit/deny sections are clean.
- evidence: `artifacts/performance/evidence/fj_ad_scalar_value_and_grad_f2s2l_pass195_2026-06-09.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile `value_and_grad` after landing. If AD remains top, attack a compiled/arena scalar tape or broader primitive-specific reverse pass, not another container-only allocation tweak.

## pass196 -- fj-ad scalar fast-path on-demand slots (SHIPPED, bead frankenjax-5u1l7)
- target `value_and_grad_runtime/shared/deep_100_nodes`. Post-pass195 profile ranked `fj_ad::value_and_grad_jaxpr_inner_with_custom_vjp_key` 36.66%, `fj_ad::max_var_index` 22.04%, and dense `Option<f64>` initialization 2.73%.
- lever shipped: remove the separate `max_var_index` scan from the guarded scalar-F64 Add/Mul fast path. Dense scalar slots grow on demand with the same `DENSE_AD_VALUE_STORE_MAX_SLOTS` fallback threshold.
- comparable gate: clean detached worktrees at live base `8ef49b7e`, both invoked through `rch exec` and both fell open locally after the same `vmi1153651` topology preflight failure. Baseline `4.8194..4.9041..4.9786us`; candidate `3.9823..4.0400..4.1096us`; median speedup `1.214x`, conservative interval speedup `1.173x`; Score `6.0`.
- isomorphism: forward equation order, reverse equation order, Add/Mul cotangent order, output/gradient order, IEEE F64 operation order, RNG absence, and tie behavior are unchanged. High VarIds still fall back when they exceed the dense-slot threshold; unsupported dtype/literal/primitive/arity/params/effects/custom-VJP/sub-Jaxpr cases keep existing fallback/error behavior.
- proof: full `cargo test -p fj-ad --lib` passed (`364 passed`), including the scalar fast-path bit-equivalence and deep golden tests. Golden fixture JSON digest remains `2b1b03372322b4e6042ed0af95c4bfe87c0e15c25e9621c28a2da846689a9e25`; golden file sha `518acf1317d616ba5ae83e4cf76d437e008350648b737e4da9b24eaa1f1ad08b`. `rustfmt --edition 2024 --check crates/fj-ad/src/lib.rs`, `cargo check -p fj-ad --all-targets`, and scoped clippy passed. UBS over `fj-ad/src/lib.rs` remains nonzero on broad pre-existing file inventory while its fmt/check/clippy/test-build/audit/deny sections are clean.
- evidence: `artifacts/performance/evidence/fj_ad_scalar_layout_scan_5u1l7_pass196_2026-06-09.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile `value_and_grad`; if fj-ad remains top, use cached scalar fast-path metadata or a compiled/arena scalar tape rather than another scan-only tweak.

## pass197 -- fj-ad scalar fast-path pre-sized slots (SHIPPED, bead frankenjax-jizp8)
- target `value_and_grad_runtime/shared/deep_100_nodes`. Post-pass196 profile captured 22,500 samples and ranked `fj_ad::value_and_grad_jaxpr_inner_with_custom_vjp_key` 21.94%, `fj_ad::scalar_f64_ensure_slot` 5.98%, `fj_dispatch::thread_jaxpr_effect_tokens` 2.15%, `fj_dispatch::dispatch_ref` 1.48%, and dense `Option<f64>` initialization 1.04%.
- lever shipped: pre-size the guarded scalar-F64 Add/Mul fast-path values vector from static Jaxpr counts, then use the grow/fallback helper only for sparse or high VarIds. The direct dense write uses `get_mut`, so UBS does not gain a new direct-index finding.
- comparable gate: clean scratch worktree at `e191c615`, with the score based on local comparable rows after RCH worker selection became mixed. Baseline was RCH-invoked local fail-open (`vmi1153651` topology preflight failure) at `3.9158..3.9586..4.0060us`; final candidate local confirmation was `3.7301..3.7697..3.8145us`; median speedup `1.050x`, conservative interval speedup `1.027x`; Score `6.0`. Remote-only `vmi1227854` candidate rows were recorded but not scored because no same-worker baseline was available.
- isomorphism: allocation sizing changed only for dense sequential VarIds. Forward equation order, reverse equation order, Add/Mul cotangent order, output/gradient order, IEEE F64 operation order, RNG absence, and tie behavior are unchanged. Sparse VarIds still grow through `scalar_f64_ensure_slot`; VarIds at/above `DENSE_AD_VALUE_STORE_MAX_SLOTS` still return `Ok(None)` and fall back to the generic tape path.
- proof: full `cargo test -p fj-ad --lib` passed (`365 passed`), including new `scalar_f64_add_mul_fast_path_falls_back_for_high_var_ids` and the deep golden test. Golden fixture JSON digest remains `2b1b03372322b4e6042ed0af95c4bfe87c0e15c25e9621c28a2da846689a9e25`; golden file sha `5cedc4b6bb23543b6551ad248a358c7a0dd8114875483289abd2a51d4df9ef9e`. `rustfmt --edition 2024 --check crates/fj-ad/src/lib.rs`, `cargo check -p fj-ad --all-targets`, and scoped clippy passed. UBS over `fj-ad/src/lib.rs` remains nonzero on broad pre-existing file inventory while its fmt/check/clippy/test-build/audit/deny sections are clean.
- evidence: `artifacts/performance/evidence/fj_ad_scalar_presized_slots_jizp8_pass197_2026-06-09.json` (+ `.golden`, `.golden.sha256`).
- next primitive: reprofile `value_and_grad`; if fj-ad remains top, stop shrinking slot helpers and attack cached scalar fast-path metadata or a compiled scalar Add/Mul tape.

## pass198 -- fj-ad scalar prepared-plan metadata (REJECTED, bead frankenjax-971vp)
- target `prepared_dispatch_metadata/prepared/deep_100_nodes` after pass197 suggested static scalar Add/Mul value_and_grad metadata as the next possible `fj-ad` lever.
- rejected lever: cache args-independent scalar F64 Add/Mul value_and_grad plan metadata in `PreparedDispatchMeta` and replay the prepared plan for repeated prepared dispatch calls.
- comparable gate: clean-HEAD baseline worktree `/data/projects/.scratch/frankenjax-971vp-baseline-20260609T121056`, both benchmark invocations used `rch exec` but fell open locally because no admissible remote worker was available. Baseline `prepared/deep_100_nodes` mean `3.8466us`; candidate mean `4.2910us`. Baseline `recompute/deep_100_nodes` mean `16.694us`; candidate mean `17.551us`. Score `0.0`.
- isomorphism: candidate was bit-preserving by construction (same equation order, same Add/Mul FP expression order, same output/gradient order, no RNG), but behavior proof is irrelevant for a regressing lever. Source was restored.
- evidence: `artifacts/performance/evidence/frankenjax-971vp_scalar_plan_rejected_2026-06-09.json`.
- next primitive: do not repeat static metadata caching for this row. Re-profile and attack a different profile-backed primitive, avoiding peer-owned `fj-lax` dense/linalg work.

## pass199 -- fj-ad dense F64 square-plus-linear reduce-sum reverse fast path (SHIPPED, bead frankenjax-b6vaq)
- target `ad/grad_sum_x2_plus_x_1k`. Fresh rch Criterion profile ranked this as the dominant fj-ad row at about `4.9748us`; focused clean-base baseline at `af5f01b7` measured `5.0007..5.0526..5.1148us`.
- rejected intermediate: a generic dense Add/Mul/ReduceSum tape with `Vec<f64>` slots regressed to `5.7334..5.8193..5.9064us`, so it was removed before commit.
- lever shipped: a guarded dense-F64 expression-template reverse path for exactly `Mul(x,x) -> Add(square,x) -> ReduceSum`, computing the primal output and gradient in one pass over the dense F64 slice and falling back for custom VJPs, constvars, params, effects, sub-Jaxprs, non-F64/non-dense tensors, or any different Jaxpr shape/order.
- comparable gate: both scored baseline and final candidate used `rch exec` and both fell open locally because no admissible workers were available. Final candidate `3.2571..3.2775..3.3029us`; median speedup `1.5416x`, conservative speedup `1.5140x`; Score `6.0`.
- isomorphism: forward order is unchanged (`x*x`, then `square+x`, then left-to-right full ReduceSum). Backward accumulation order is unchanged (`1.0`, then `+x`, then `+x`). No tie-breaking, RNG, reassociation, dtype, shape, or error-surface changes. `dense_f64_square_plus_linear_reducesum_matches_generic_bits` compares output and gradient bits against the forced generic path, including signed zero, infinity, and NaN payload coverage.
- proof: full `cargo test -p fj-ad` passed (`366 passed` plus doc-tests); focused bit comparison and existing `grad_sum_x2_plus_x_1k_golden_sha256` passed; golden digest remains `5282853e2bd187c1c1bfdfa612bd74776fb403e6b767eb0a8bf0c8bcd2fe2a19`. `cargo fmt --check -p fj-ad`, `cargo check -p fj-ad --all-targets`, `cargo clippy -p fj-ad --all-targets --no-deps -- -D warnings`, and `git diff --check` passed. Clippy still reports unchanged `fj-trace`/`fj-lax` dependency warnings before checking `fj-ad`; UBS over `fj-ad/src/lib.rs` remains nonzero on broad pre-existing file inventory while style/build sections were clean.
- evidence: `artifacts/performance/evidence/frankenjax-b6vaq_dense_f64_square_reduce_2026-06-09.json`.
- next primitive: reprofile after landing. If this AD row drops, pick the next non-`fj-lax` top row or coordinate before touching active dense/linalg surfaces.

## pass200 -- fj-dispatch vmap rank2 axis1 direct dispatch bypass (REJECTED, bead frankenjax-vr084)
- target: no ready non-owned perf beads were open after `frankenjax-b6vaq`; fresh RCH profile on `vmi1227854` identified `dispatch_latency/vmap/rank2_add_one_in_axes_1` at `12.546..12.960..13.515us`, versus same-run `rank2_add_one` at `3.7282..3.7506..3.7660us`.
- lever tried: exact dispatch-level direct path for one-input rank2 I64 `Add(var, i64_literal)` with `in_axes=1`, bypassing BatchTrace setup while preserving output shape `[cols, rows]`, wrapping add, element order, errors, and fallback surface. Focused golden/parity test passed against the forced `[Vmap, Jit]` fallback.
- result: same-worker RCH candidate regressed to `13.414..13.623..13.860us` (`0.95x` mean, `0.91x` conservative). Source lever removed before commit.
- decision: close `frankenjax-vr084` rejected/no-ship. Next deeper primitive should attack the actual transpose/materialization cost, e.g. a blocked transpose-map kernel or typed-buffer path, not another BatchTrace-bypass micro-route.
- evidence: `artifacts/performance/evidence/frankenjax-vr084_dispatch_vmap_axis1_reject_2026-06-09.json`.

## pass201 -- fj-interpreters dense f32 eval_jaxpr fusion (SHIPPED, bead frankenjax-jalko)
- target: extend the shipped a8nbp chunked eval_jaxpr fusion from dense f64 to dense f32, the JAX default float dtype, while leaving half/i64/broadcast follow-ups for later passes.
- baseline/route: pre-pass RCH `ovh-a` control for existing f64 fusion was `unfused=32.362ms fused=1.978ms speedup=16.37x`; final same-invocation RCH A/B on `vmi1227854` measured the new f32 row against its forced-unfused `eval_primitive` reference.
- lever shipped: `a08a761d` adds a dense-f32 fused-run sibling in `crates/fj-interpreters/src/lib.rs`, mirroring fj-lax's f32 contract by widening taps to f64, applying Add/Sub/Mul/Div/Neg in primitive order, and rounding each primitive result back to f32.
- comparable gate: original RCH `vmi1227854` keep row was `EVAL_FUSION_SPEED_F32 n=1048576 ops=8 unfused=7.805ms fused=0.949ms speedup=8.22x`; post-cleanup RCH rebench on the same worker was `unfused=7.391ms fused=1.024ms speedup=7.22x`. Score `12.5`.
- isomorphism: output dtype/storage stays F32, per-element operation order is unchanged, signed zero/infinity/subnormal/NaN payload behavior is bit-compared against forced-unfused `eval_jaxpr_hashed_env`, no RNG/tie/order surface is introduced, and unsupported shapes/dtypes still fall back to the normal interpreter path.
- proof: focused fusion tests passed, `rch exec -- cargo check -p fj-interpreters --all-targets` passed remotely, `cargo fmt --check -p fj-interpreters` passed, strict `rch exec -- cargo clippy -p fj-interpreters --all-targets --no-deps -- -D warnings` passed remotely, and post-cleanup `rch exec -- cargo test -p fj-interpreters --lib` passed (`147 passed`; RCH fell open locally due no admissible worker). Golden SHA `fef28624a52e5647abc35f0d388072b443cf081e5941243c6c58a8bd91f40a84`.
- evidence: `artifacts/performance/evidence/frankenjax-jalko-f32-eval-fusion-pass201.md`. Remaining half/i64/broadcast scope moved to `frankenjax-coax7`.

## pass202 -- fj-interpreters f32 row-broadcast eval_jaxpr fusion (SHIPPED, bead frankenjax-coax7)
- target: extend the dense F32 eval_jaxpr fusion path to rank-2 row-bias broadcast `[rows, cols] op [cols]`, while leaving half/I64/column/general broadcast and Max/Min follow-ups for a separate bead.
- baseline/route: pre-fast-path RCH benchmark-only baseline on `ovh-a` measured `EVAL_FUSION_SPEED_F32_ROW_BROADCAST rows=1024 cols=1024 ops=8 unfused=9.920ms fused=9.063ms speedup=1.09x`.
- lever shipped: `F32Operand::RowBroadcast` classifies dense `[cols]` operands against a full `[rows, cols]` chain shape and gathers them by row-major column index inside the existing fused F32 chunk evaluator.
- comparable gate: same-worker RCH `ovh-a` candidate measured `unfused=8.935ms fused=6.875ms speedup=1.30x`; scored eval_jaxpr row path delta is `9.063ms -> 6.875ms`, `1.32x`. Supporting RCH `vmi1227854` row measured `unfused=9.519ms fused=7.478ms speedup=1.27x`. Score `6.0`.
- rejected intermediate: naive I64 same-shape fusion was measured and removed. RCH `vmi1227854` baseline was `unfused=8.074ms fused=7.018ms speedup=1.15x`; the candidate regressed to `unfused=6.989ms fused=8.633ms speedup=0.81x`.
- isomorphism: equation order, per-element primitive order, F32 widen/apply/round contract, row-major broadcast gather, fallback/error surfaces, and RNG absence are unchanged. The proof test compares the fused output bits against forced-unfused `eval_jaxpr_hashed_env` with NaN, signed-zero, infinity, and subnormal coverage. Golden SHA `1f742aad15797ada82394f8d78c5b2d488ac650c272e8a81330a694621a64494`.
- proof: `rustfmt --edition 2024 --check` on touched files passed; focused row-broadcast golden test passed; `rch exec -- cargo check -p fj-interpreters --all-targets`, `rch exec -- cargo test -p fj-interpreters` (`148 passed`; doc-tests `0`), strict `rch exec -- cargo clippy -p fj-interpreters --all-targets --no-deps -- -D warnings`, and scoped `git diff --check` passed. UBS over touched files remains nonzero on broad pre-existing test/bench inventory while its fmt/check/clippy/test-build/audit/deny sections were clean.
- evidence: `artifacts/performance/evidence/frankenjax-coax7-f32-row-broadcast-pass202.md`. Remaining half/I64/non-row-broadcast scope moved to `frankenjax-lcozh`.

## pass203 -- fj-interpreters f32 column-broadcast eval_jaxpr fusion (SHIPPED, bead frankenjax-lcozh)
- target: extend dense F32 eval_jaxpr fusion from row-bias broadcast to rank-2 column-bias broadcast `[rows, cols] op [rows, 1]`, while keeping half/I64/general broadcast and Max/Min for follow-up work.
- baseline/route: pre-fast-path RCH benchmark-only baseline on `ovh-a` measured `EVAL_FUSION_SPEED_F32_COL_BROADCAST rows=1024 cols=1024 ops=8 unfused=9.054ms fused=8.677ms speedup=1.04x`.
- lever shipped: `F32Operand::ColBroadcast { idx, cols }` classifies dense F32 operands shaped `[rows, 1]` against a full `[rows, cols]` chain shape and gathers them by row-major row index `linear_index / cols` inside the existing fused F32 chunk evaluator.
- comparable gate: same-worker RCH `ovh-a` candidate measured `unfused=8.958ms fused=6.940ms speedup=1.29x`; scored eval_jaxpr column path delta is `8.677ms -> 6.940ms`, `1.25x`. Score `6.0`.
- isomorphism: equation order, per-element primitive order, F32 widen/apply/round contract, row-major column-broadcast gather, fallback/error surfaces, and RNG absence are unchanged. The proof test compares fused output bits against forced-unfused `eval_jaxpr_hashed_env` with NaN, signed-zero, infinity, and subnormal coverage. Golden SHA `5762f3ec4614f491d21407cbb09c5cd92915840f65d145070f8d8b5e8c7c5e3a`.
- proof: `rustfmt --edition 2024 --check` on touched files passed; focused column-broadcast golden test passed; `rch exec -- cargo check -p fj-interpreters --all-targets`, `rch exec -- cargo test -p fj-interpreters` (`149 passed`; doc-tests `0`), strict `rch exec -- cargo clippy -p fj-interpreters --all-targets --no-deps -- -D warnings`, and scoped `git diff --check` passed. UBS over touched files remains nonzero on broad pre-existing test/bench inventory while its fmt/check/clippy/test-build/audit/deny sections were clean.
- evidence: `artifacts/performance/evidence/frankenjax-lcozh-f32-col-broadcast-pass203.md`. Remaining half/I64/general-broadcast scope moved to a fresh follow-up bead.

## pass204 -- fj-interpreters f64 row-broadcast direct fusion (REJECTED, bead frankenjax-wfc3w)
- target: test whether the F64 eval_jaxpr fusion scanner should mirror the F32 rank-2 row-broadcast path for `[rows, cols] op [cols]`.
- baseline/route: benchmark-only row on RCH `ovh-a` measured `EVAL_FUSION_SPEED_F64_ROW_BROADCAST rows=1024 cols=1024 ops=8 unfused=7.626ms fused=5.462ms speedup=1.40x` before any source fast path.
- rejected lever: `FOperand::RowBroadcast` with per-element row gather `(base + offset) % row.len()`.
- proof: the source candidate's focused forced-unfused bit proof passed and produced golden SHA `ea879236c0bd2823d6161300f66a8a1b0e7122bc6ebc4ad41fa21c2d3bc950e5`.
- rejection evidence: RCH selected `vmi1227854` for the after row despite the `ovh-a` hint, so it was not comparable to the baseline and was directionally bad: `unfused=6.523ms fused=8.687ms speedup=0.75x`. Score `0.0`.
- decision: source and benchmark changes removed before commit. Do not repeat the per-element modulo row-gather route; attack packed-u16 BF16/F16 fusion or a broadcast plan that eliminates modulus from the hot loop.
- evidence: `artifacts/performance/evidence/frankenjax-wfc3w-f64-row-broadcast-reject-pass204.md`.

## pass205 -- fj-interpreters BF16/F16 packed-u16 eval_jaxpr fusion (REJECTED, bead frankenjax-wfc3w)
- target: test whether the existing eval_jaxpr fusion tape should gain a same-shape BF16/F16 sibling over packed `u16` tensor storage.
- baseline/route: benchmark-only BF16 row on RCH `ovh-a` measured `EVAL_FUSION_SPEED_BF16 n=1048576 ops=8 unfused=44.743ms fused=43.336ms speedup=1.03x` before any source half fast path.
- rejected lever: strict same-shape half scanner requiring one dtype throughout the run, widening through `Literal::{BF16,F16}Bits(...).as_f64()`, applying Add/Sub/Mul/Div/Neg in original order, rounding after every primitive, and materializing packed half output.
- proof: focused forced-unfused bit proof passed on `ovh-a`; golden SHAs were BF16 `46926fd8cdda6d8c45a18cc3178dbb5ff3fa2fdd2021e2d985199e0f01218cf6` and F16 `3dceb054f3551c13f4650094c80289ed81187534813220a0dbcc7ba34475efab`.
- rejection evidence: same-worker RCH `ovh-a` candidate measured `EVAL_FUSION_SPEED_BF16 n=1048576 ops=8 unfused=44.075ms fused=57.110ms speedup=0.77x`; scored eval_jaxpr path delta `43.336ms -> 57.110ms`, Score `0.0`.
- decision: source, test, and benchmark changes removed before commit. Do not repeat the per-step packed-half widen/apply/round fusion route; it is proof-clean but scalarizes conversion work into the hot tape. Next pass should attack a different profile-backed primitive or a fundamentally different half strategy that reduces conversion count.
- evidence: `artifacts/performance/evidence/frankenjax-wfc3w-bf16-fusion-reject-pass205.md`.

## pass206 -- fj-interpreters f32 broadcast gather strength reduction (SHIPPED, bead frankenjax-86cks)
- target: shipped F32 row/column broadcast eval_jaxpr fusion still spent about 8ms in fused gather loops after passes 202 and 203.
- baseline/route: fresh RCH `ovh-a` baseline measured row-broadcast `unfused=10.125ms fused=8.364ms speedup=1.21x` and column-broadcast `unfused=9.724ms fused=8.359ms speedup=1.16x`.
- lever shipped: replace per-element row modulo and column division in the existing F32 broadcast fusion chunk evaluator with row-major contiguous spans. Row-broadcast helpers copy/apply contiguous row slices; column-broadcast helpers fill/apply row spans with the same row scalar. Empty output chunks remain no-ops.
- comparable gate: final exact-source RCH `ovh-a` rebench measured row-broadcast `unfused=10.221ms fused=0.975ms speedup=10.49x` and column-broadcast `unfused=10.569ms fused=0.799ms speedup=13.23x`. Scored fused deltas are row `8.364ms -> 0.975ms` (`8.58x`) and column `8.359ms -> 0.799ms` (`10.46x`). Score `12.5`.
- isomorphism: equation order, per-element primitive order, F32 widen/apply/round contract, broadcast indexing, fallback/error surfaces, RNG absence, and empty-output behavior are unchanged. The new helpers partition the same row-major linear index stream into contiguous spans instead of recomputing modulo/division for each element.
- proof: focused `fusion_f32_` tests passed with existing golden SHAs `fef28624a52e5647abc35f0d388072b443cf081e5941243c6c58a8bd91f40a84`, `1f742aad15797ada82394f8d78c5b2d488ac650c272e8a81330a694621a64494`, and `5762f3ec4614f491d21407cbb09c5cd92915840f65d145070f8d8b5e8c7c5e3a`. `cargo fmt -p fj-interpreters --check`, `git diff --check`, `cargo check -p fj-interpreters --all-targets`, `cargo test -p fj-interpreters` (`149 passed`; doc-tests `0`), and strict crate-scoped clippy passed. UBS over `crates/fj-interpreters/src/lib.rs` remains nonzero on broad pre-existing file inventory while its fmt/check/clippy/test-build/audit/deny sections were clean.
- evidence: `artifacts/performance/evidence/frankenjax-86cks-f32-broadcast-strength-reduce-pass206.md`.

## pass207 -- fj-lax sqrt/rsqrt threaded dense unary path (SHIPPED, bead frankenjax-1isgp)
- target: `Sqrt` and `Rsqrt` still dispatched through serial `eval_unary_elementwise`, whose F64 path built boxed `Vec<Literal>` output and never used the existing dense/threaded unary machinery.
- lever shipped: route `Sqrt` and `Rsqrt` through `eval_unary_elementwise_parallel`, preserving the existing fallback surface for scalar, half, complex, and below-threshold cases while using dense F64/F32/BF16/F16 output and chunked threading for large tensors.
- comparable gate: same-binary RCH A/B over `n = 1 << 22` F64 measured `sqrt` `135.4ms -> 11.8ms` (`11.49x`) and `rsqrt` `137.9ms -> 11.8ms` (`11.66x`). Score `12.5`.
- isomorphism: every accepted element still applies exactly the same unary `sqrt` or reciprocal-sqrt expression at the same row-major index; no reassociation, FMA request, mixed precision, RNG, tie-breaking, dtype/shape/error behavior, or fallback behavior changed.
- proof: `sqrt_rsqrt_parallel_bit_identical_and_faster` compares old and new outputs by `f64::to_bits()` after `as_f64()` extraction, and the existing sqrt tests remained green through RCH. Commit `835644d3` contains the production lever and closes `frankenjax-1isgp`.

## pass209 -- fj-dispatch dense F64 outputs for batched SVD (SHIPPED, bead frankenjax-9cpxs)
- target: fresh fj-dispatch profiles ranked `vmap_svd/batched_matrix_3x2` as the top unowned dispatch-side row after fj-lax dense/linalg work was already reserved by IcyGlacier. The row measured `71.517..72.517..73.389us` on `ovh-a` and `53.201..53.890..54.974us` on `vmi1227854`.
- lever shipped: for `DType::F64` only, `batch_svd_multi` now accumulates U/S/Vt into `Vec<f64>` and emits dense `TensorValue::new_f64_values` outputs. The 3x2 thin SVD arithmetic, generic SVD fallback arithmetic, batch order, sign/tie behavior, and non-F64 output path are unchanged.
- comparable gate: pre-edit focused `vmi1227854` baseline measured `59.900..61.038..62.424us`, but the same-worker after run timed out in the RCH queue. The scored same-worker-family comparison uses the pre-edit `ovh-a` profile row and post-edit `ovh-a` candidate `45.272..45.387..45.495us`: median `72.517us -> 45.387us` (`1.60x`), conservative `71.517us -> 45.495us` (`1.57x`). Score `3.2`.
- isomorphism: each accepted F64 element is read in the same row-major order and each slice calls the same `svd_decompose_matrix_3x2_thin` or `svd_decompose_matrix` implementation as before. Only the output container changes from boxed literals to dense f64 storage; output shapes, dtype, singular-value ordering, SVD sign conventions, tie behavior, floating-point operations, RNG absence, and fallback/error surfaces are preserved.
- proof: focused RCH `cargo test -p fj-dispatch --lib svd -- --nocapture` passed (`6 passed`); RCH `cargo check -p fj-dispatch --all-targets` passed; `git diff --check` passed; `ubs crates/fj-dispatch/src/batching.rs` reported critical `0`. Direct `rustfmt --edition 2024 --check crates/fj-dispatch/src/batching.rs` still fails on pre-existing unrelated formatting drift in this large file; the touched hunk follows rustfmt style. Direct RCH clippy could not start after three attempts because no admissible workers were available, while UBS' shadow clippy section was clean for the touched file.
- golden: `test_batch_trace_svd_multi_leading_batch_dim_golden_sha256` freezes U/S/Vt shapes and f64 bit patterns at `165205f8b8911fcc1d544aeb134f92fddf303cebe7ef7770c7718a80735eabbe`.
- evidence: `artifacts/performance/evidence/frankenjax-9cpxs-svd-dense-f64-outputs-2026-06-09.json`.
- next primitive: reprofile after landing; avoid fj-lax dense/linalg files while IcyGlacier owns them, and route to a different dispatch/eval primitive or a deeper alien-graveyard algorithm if micro-dispatch rows stop scoring.

## pass210 -- fj-dispatch dense F64 outputs for batched Eigh (REJECTED, bead frankenjax-cf6go)
- target: after `frankenjax-9cpxs`, no unowned ready `[perf]` bead was available and IcyGlacier still owned the fj-lax dense/linalg roots. Prior RCH profiles left `vmap_eigh/batched_matrix_3x3` as the next unowned dispatch row (`99.086us` median on `vmi1227854`; `108.70us` on `ovh-a`).
- baseline: focused RCH remote baseline on `vmi1227854` measured `98.763..99.651..100.51us`.
- rejected lever: a F64-only `batch_eigh_multi` dense-output path accumulated W/V into `Vec<f64>` and emitted `TensorValue::new_f64_values`, while preserving the existing analytic/Jacobi arithmetic, eigenvalue ordering, eigenvector sign behavior, fallback paths, and errors.
- proof-clean candidate: focused RCH remote `cargo test -j 1 -p fj-dispatch --lib eigh -- --nocapture` passed (`6 passed`), including existing golden SHAs `de40295687095bc622bd73074d24337004f440bdd2cc65d8a8759dfb5cf0b106` and `9c8554df967d304b2570460fc5db4fca86602577232fcf8b01177fcd41cd365f`.
- rejection gate: same-worker RCH candidate measured `96.836..97.288..97.878us`, only `1.024x` median and `1.009x` conservative. Score `0.0`, below the `>=2.0` keep gate, so the source/test hunks were removed before commit.
- evidence: `artifacts/performance/evidence/frankenjax-cf6go-eigh-dense-f64-output-reject-2026-06-09.json`.
- next primitive: do not repeat Eigh output-storage micro-tuning. Reprofile and target a different unowned dispatch primitive, or route to an alien-graveyard algorithmic primitive if the remaining dispatch micro-levers do not score.

## pass211 -- fj-dispatch paired I64 vmap dot leading-axis bypass (REJECTED, bead frankenjax-p2e0x)
- target: post-pass210 profile on `vmi1227854` ranked `vmap_dot_i64/paired_vectors` as the largest unowned non-Eigh fj-dispatch row (`53.826..55.547..58.157us`), while IcyGlacier still owned fj-lax dense/linalg files.
- baseline: focused RCH remote baseline on `vmi1227854` measured `55.969..57.983..60.092us`.
- rejected lever: when both operands already had batch dim 0, call `batch_paired_i64_vector_dot` directly on borrowed Values before the existing `move_batch_dim_to_front(..., 0)` clone path. Non-matching surfaces fell through to the old path.
- proof-clean candidate: focused RCH remote `cargo test -j 1 -p fj-dispatch --lib dispatch_vmap_dot_i64_paired_vectors_wraps_like_lax -- --nocapture` passed (`1 passed`) with golden SHA `060c94e0fe0eed0748b3adfc77d8b95bd7d99e936c44427283815b25e9bc100d`.
- rejection gate: RCH candidate on `vmi1227854` measured `55.942..58.204..60.749us`, a `0.996x` median and `0.921x` conservative speedup versus baseline. Score `0.0`, below the `>=2.0` keep gate, so the source/test hunks were removed before commit.
- evidence: `artifacts/performance/evidence/frankenjax-p2e0x-zero-copy-paired-i64-dot-reject-2026-06-09.json`.
- next primitive: do not repeat leading-axis no-op clone bypass for paired I64 dot. Reprofile and attack a different dispatch primitive, or move to a deeper alien-graveyard layout/algorithm route if remaining dispatch micro-levers stay flat.

## pass212 -- fj-core dense I32 tensor storage (SHIPPED, bead frankenjax-a6fm7)
- target: ready perf bead `frankenjax-a6fm7` from cc identified that `TensorValue::new(DType::I32, Vec<Literal::I64>)` boxed i32 tensors, making `as_i64_slice()` return `None` and skipping existing dense i64-backed i32 fast paths.
- baseline: same-worker RCH `vmi1227854` Criterion measured `eval/matmul_512x512_i32_dense` at `13.938..14.522..15.319ms` and the production boxed constructor row `eval/matmul_512x512_i32_literal_ref` at `557.47..576.72..592.42ms`.
- lever shipped: `TensorValue::new` now densifies all-I64 `DType::I32` tensors into the existing dense i64 buffer while preserving dtype `I32`; `TensorValue::new_i32_values` exposes the same contract explicitly.
- comparable gate: same-worker RCH candidate measured dense `11.267..11.560..12.150ms` and production literal-ref `12.555..12.903..13.313ms`. Scored production row improved `576.72ms -> 12.903ms` (`44.70x` median, `41.87x` conservative). Score `12.5`.
- isomorphism: dtype, shape, element count, and materialized `Literal::I64` sequence are unchanged; only backing storage changes from boxed literals to dense i64. Existing i32 arithmetic/matmul/reduction order and `narrow_i32_tensor_result` wrapping are preserved. No FP, RNG, tie-breaking, or error-class surface changed.
- proof: RCH `cargo test -j 1 -p fj-core i32_tensor_constructor_uses_dense_i64_storage -- --nocapture` passed with golden SHA `a5749ee53dedc45fde6e86c9ec1b6fa9bc13cde391b6eac6a6c1f5d0a8d54daa`; RCH `cargo test -j 1 -p fj-lax i32 -- --nocapture` passed (`10 passed`); RCH `cargo check -j 1 -p fj-core -p fj-lax --all-targets` passed; RCH strict `cargo clippy -j 1 -p fj-core --all-targets -- -D warnings` passed; `cargo fmt -p fj-core --check` and `git diff --check` passed. UBS over `crates/fj-core/src/lib.rs` remains nonzero on broad pre-existing inventory while fmt/check/clippy/test-build/audit/deny sections were clean.
- evidence: `artifacts/performance/evidence/frankenjax-a6fm7-dense-i32-storage-2026-06-09.json`.
- next primitive: reprofile after landing; likely follow-up targets are remaining dense-I32 producer/compare/elementwise coverage and any i32 producers that still bypass the dense constructor.

## pass213 -- fj-core/fj-lax bit-packed Bool storage (REJECTED, bead frankenjax-prbo6)
- target: cc-filed follow-up from the rejected SIMD compare path: reduce Bool mask output cost by changing Bool tensor storage from byte-per-element `Vec<bool>` to packed `u64` words and emitting same-shape F64 comparison masks directly.
- baseline: same-worker RCH `vmi1227854` Criterion measured `eval/lt_same_shape_1024x1024_f64` at `356.94..392.19..427.34us`, `eval/select_1024x1024_f64` at `565.46..579.86..600.81us`, and `eval/reduce_and_64k_bool_vec` at `1.0225..1.0313..1.0361us`.
- rejected lever: candidate1 used a generic per-index bit packer and regressed the target row to `1.7276ms`; candidate2 used chunked `u64` word emission and still measured `485.08..515.63..543.15us` for compare, `711.02..737.54..758.43us` for select, and `1.0578..1.0603..1.0639us` for reduce.
- proof-clean candidate: RCH `cargo test -j 1 -p fj-core bool_tensor_uses_bit_packed_storage_with_ordered_materialization -- --nocapture` passed, including word order and dirty-tail masking; RCH `cargo test -j 1 -p fj-lax prbo6_bitpacked_f64_compare_matches_literal_bool_consumers -- --nocapture` passed across word-boundary shapes, all six comparisons, NaN/signed-zero/infinity cases, and compare-result consumers (`select`, `bitwise_not`, `reduce_and`, `reduce_or`). Golden SHA `14bd0f823ccd1be6e58cbebc3cfd7eb7e7f4e4a8ac1451cfb4a83f6a64513a69`.
- rejection gate: best candidate was `0.76x` on the target comparison row and slowed adjacent Bool consumer rows. Score `0.0`, so all source/test hunks were removed before commit.
- evidence: `artifacts/performance/evidence/frankenjax-prbo6-bitpacked-bool-reject-2026-06-10.json`.
- next primitive: use already-filed `frankenjax-lu4yw` for fused word-mask compare consumers. Do not repeat storage-only Bool packing; attack compare-to-select and compare-to-reduce fusion so consumers operate on mask words without materializing `Vec<bool>`.
